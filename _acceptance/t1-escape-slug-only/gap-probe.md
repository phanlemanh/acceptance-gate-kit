---
slug: t1-escape-slug-only
at: 2026-08-12T09:48:39Z
verdict: findings
p0: 4
p1: 4
p2: 4
---

## Findings

| Sev | Artifact | Thiếu gì | Kịch bản fail | Thước đo | Xử lý |
|---|---|---|---|---|---|
| P0 | evals + rang.sh | E1–E4 ghim chuỗi `PASS: TE21a/b/c`, `PASS: TE22a/b`, `PASS: TE23a` vào **stdout của `t1_escape_rang`**, nhưng `rang.sh` nuốt stdout suite vào biến `$OUT` và KHÔNG bao giờ in lại. Chạy thật hôm nay: stdout của `rang.sh` đúng hai dòng (`DOT-BIEN OK…`, `RANG OK…`). Nơi DUY NHẤT sáu chuỗi đó xuất hiện trong stdout `rang.sh` là dòng HỎNG `RANG LOI: khong thay 'PASS: TE21a' — case khong chay`. Oracle bị ĐẢO NGƯỢC. E2/E3/E4 lại không đòi exit 0, nên chỉ còn phép grep thuần. | Sáu tháng sau, một dev dọn suite thấy TE21/TE22/TE23 chậm (mỗi ca dựng một repo git) nên xoá đi. Suite vẫn `exit 0` (chỉ còn 674 passed, 0 failed → E5/E7/E9 xanh nguyên). `rang.sh` in ba dòng `RANG LOI: khong thay 'PASS: TE21a' …`. Người/agent verify vòng sau grep đúng chuỗi ghi trong `expected` của E2/E3/E4 trên stdout của đúng cmd đã khai → thấy đủ → PASS. Cổng xanh trong khi KHÔNG một ca phản chứng nào còn chạy. Chính evidence-report vòng 2 đã lộ triệu chứng: khối `output` của E1–E4 không chứa chuỗi ghim, người verify phải trỏ chéo sang stdout của E5 và ghi chú tay — tức `expected` như đã khai không kiểm được bằng máy. | (a) `rang.sh` phải `echo` đúng sáu dòng đó khi PASS (in ra, không chỉ grep), và `keu()` phải in dạng KHÔNG chứa chuỗi ghim (ví dụ `THIEU dong: TE21a`); (b) mọi eval trỏ `t1_escape_rang` phải khai `exit 0` trong `expected`; (c) thêm một eval âm: xoá TE21–TE23 khỏi suite trên bản sao → `rang.sh` phải ĐỎ. | known-limit (owner 2026-08-12) |
| P0 | vật + evals | Nhánh mới `_acceptance/*/*` chỉ đòi **một dấu `/` bất kỳ**, không đòi đó là slug. Bất kỳ thư mục con nào dưới `_acceptance/` — không cần `contract.md`, không cần được luật per-slug ngó tới — đều bật `gate_touched=1`. Đo thật: `_acceptance/tmpjunk/note.txt` + `src/app.js` → **exit 0**; và `_acceptance/junk/n.txt` + một file trong `t3_paths` → cũng **exit 0**. Không AC, không eval, không ca thử nào chạm hình dạng "thư mục con KHÔNG phải slug". | CI của một repo tiêu thụ đỏ vì răng T1-escape. Dev đang gấp, đọc thông điệp `carries NO _acceptance/<slug>/ artifacts`, gõ `mkdir -p _acceptance/wip && echo todo > _acceptance/wip/notes.md`, commit. Cổng xanh, PR 30 file mã (kể cả file trong `t3_paths`) merge. Luật per-slug không kêu vì `_acceptance/wip/` không có `contract.md` nên không phải slug — đúng cái "chất lượng" mà comment ở dòng 997 hứa là "per-slug checks above judge their quality" lại không hề chạm tới. Lỗ cũ tái sinh sâu hơn đúng một cấp thư mục, giá một lệnh `touch`. | Một ca thử `_acceptance/<dir-khong-co-contract.md>/x` + `src/app.js` phải VẪN nổ (hoặc: hợp đồng phải khai tường minh đây là sàn được chấp nhận, và có eval âm chốt sàn đó). Sàn mạnh hơn: `gate_touched=1` chỉ khi thư mục đó chứa `contract.md` trong cây đang merge. | known-limit (owner 2026-08-12) |
| P0 | vật + contract | Bản vá gỡ QUYỀN-LÀM-BẰNG-CHỨNG của `_acceptance/config.yaml` nhưng giữ nguyên QUYỀN-CẦM-LUẬT của nó: nhánh thứ hai `_acceptance/*` vẫn `continue` vô điều kiện, và `t1_skip_globs` vẫn đọc từ cây đang merge. Đo thật: MỘT PR sửa `t1_skip_globs` thành `- "**"` **và** sửa `src/app.js` → không có `VIOLATION [PR]: non-T1` (chỉ còn răng `t3_paths` nổ, và răng đó chỉ tồn tại nếu repo có khai `t3_paths` — phần lớn repo tiêu thụ mới thì không). | Cùng nhân vật của PR #6 floorplanstudio, một tháng sau. PR bị chặn. Lần này thay vì chạm `config.yaml` cho có, anh ta sửa đúng một dòng: thêm `- "src/**"` vào `t1_skip_globs`, cùng commit với 30 file mã. Cổng xanh, không một `VIOLATION` nào, không một artifact nghiệm thu nào. Câu chuyện gốc lặp lại nguyên vẹn với một ký tự khác. Hợp đồng khai "cấu hình cổng không phải bằng chứng" và mục Out of scope KHÔNG hề nêu kênh này, nên người ký sẽ đọc hồ sơ như thể lỗ đã đóng hẳn. | Một ca thử: PR nới `t1_skip_globs` (hoặc đổi `enforcement: strict → warn`) trong cùng diff với file mã non-T1, không mang slug nào → phải nổ (ví dụ: nếu `_acceptance/config.yaml` nằm trong diff thì chính nó bị coi là non-T1, hoặc luật đọc config từ **base**, không phải từ HEAD). Tối thiểu: một dòng Out of scope tường minh + eval âm ghi nhận residual. | known-limit (owner 2026-08-12) |
| P0 | rang.sh + evals | Toàn bộ sức phân biệt của hồ sơ (chính Analyst viết: "Sức phân biệt của hồ sơ nằm ở E1..E4 và E6") nằm trong `rang.sh` — một file mà (i) người viết vá tự viết, (ii) nằm dưới `_acceptance/t1-escape-slug-only/` nên **tự miễn trừ chính răng T1-escape**, (iii) không nằm trong `ALLOWED_REMOVALS`/DV5 additive-only, (iv) không bị ghim hash bởi eval nào, (v) verdict của E6 đúng bằng một chuỗi `rang.sh` tự `echo`. Không phép đo nào trong hồ sơ đọc *nội dung* `rang.sh`. | Vòng sau, hồ sơ đỏ vì lớp đột biến gãy (vật đổi hình → `MUTANT-LOI`). Agent implement muốn xanh nhanh, sửa `rang.sh`: bỏ khối python, thay bằng `echo "DOT-BIEN OK: ban cu lot (lo tai xuat hien), ban va chan"`, và `echo` sẵn sáu dòng `PASS: TE2x`. `rang.sh` exit 0. E1–E4, E6 xanh hết, mirror + version vẫn xanh, suite vẫn xanh. Người ký đọc evidence-report thấy 10/10 eval máy xanh và ký. Không phép đo nào từng chạm `pre-merge-check.sh`. | (a) DV5 additive-only (hoặc một `no-drift` cùng nếp `no-vat-cam-drift.sh` đã có trong repo) phải canh `rang.sh` — dòng đã khai chỉ được THÊM; (b) một eval ghim sha256 của `rang.sh` vào evidence-report; (c) lớp đột biến phải nằm ngoài file mà người vá sở hữu (ví dụ trong `tests/scripts/`, nơi DV5 đã canh). | known-limit (owner 2026-08-12) |
| P1 | vật + evals | "Một đại lượng hai chủ" — grep thật cho ba định nghĩa khác nhau của "đường dẫn `_acceptance/`" trong CÙNG file: dòng 393 `stale_files()` giữ hình dạng **CŨ** (`_acceptance/*|*/_acceptance/*) continue`), dòng 478 `slug_in_diff()` đòi trọn `_acceptance/<slug>/`, dòng 1025–1026 (mới) tách hai nhánh. `_acceptance/config.yaml` vì thế mang ba nghĩa khác nhau trong một lần chạy. Không eval nào canh sự lệch này; E12 (judgment) chỉ khẳng định `stale_files()` "byte-identical với main" — đúng, và đó CHÍNH là vấn đề. | Slug X verify xong, `verified_commit` ghim ở commit C. Trước khi merge, một commit sửa `_acceptance/config.yaml` trỏ `executors.test.scripts` sang một CLI khác (đúng hành vi đã quan sát ở PR #6). Luật staleness KHÔNG nổ (dòng 393 coi config là gate artifact → `continue`), răng T1-escape KHÔNG nổ (dòng 1026 → `continue`). Bằng chứng vẫn "tươi", trong khi bộ executor đã sinh ra bằng chứng đó không còn tồn tại. Cổng xanh trên một cấu hình chưa từng được nghiệm thu. | Một ca thử: verify → ghim `verified_commit` → sửa `_acceptance/config.yaml` (khối `executors:`) → luật staleness phải nổ. Hoặc: tách hằng chung `is_gate_artifact()` dùng ở cả ba chỗ, cộng một ca chứng minh ba chỗ cùng một định nghĩa. | known-limit (owner 2026-08-12) |
| P1 | evals | TE23 là **ca DUY NHẤT** canh chiều siết-quá-tay, và nó là một assert ÂM thuần: `nothas TE23a "VIOLATION [PR]: non-T1" "$TE23"` — không kiểm mã thoát, không có assert DƯƠNG nào chứng minh luật đã chạy. `nothas` PASS trên chuỗi rỗng. Trong chính file này đã có tiền lệ chống-XANH-RỖNG (TE2a3/TE2a4, kèm ghi chú "Chống XANH-RỖNG… phân biệt 'đúng' với 'không chạy'"); TE23 không theo. | Ai đó sửa `pre-merge-check.sh` khiến script chết sớm ở một repo có `_acceptance/<slug>/` (ví dụ một `set -e` mới trong khối per-slug, hoặc exit 2 vì parse config). `$TE23` rỗng → TE23a PASS. `rang.sh` thấy `PASS: TE23a` → xanh. Chiều "miễn trừ THẬT phải còn nguyên" — thứ duy nhất chặn một bản vá biến MỌI PR có gate thành đỏ vĩnh viễn trên MỌI repo tiêu thụ — không còn ai canh. | Thêm vào TE23: `check TE23b 0 $?` (mã thoát 0) và `hasout TE23c "ran t1-escape" "$TE23"` (hoặc `"pre-merge-check: clean"`) — đo thật hôm nay fixture này cho exit 0 + `pre-merge-check: clean`, nên assert được ngay. | known-limit (owner 2026-08-12) |
| P1 | evals | Hình dạng **xoá** và **đổi tên** slug không có ca thử. `git diff --name-only` liệt cả đường dẫn bị xoá và cả hai vế của rename, nên chúng đều khớp `_acceptance/*/*` → `gate_touched=1`. Đo thật: PR xoá `_acceptance/oldslug/` + sửa `src/app.js` → **exit 0**; PR `git mv _acceptance/oldname _acceptance/newname` + sửa `src/app.js` → **exit 0**. | Một PR dọn dẹp "xoá các hồ sơ đã ship" đụng luôn 20 file mã non-T1 (refactor kèm theo). Diff có `_acceptance/da-ship-2025/*` (toàn dòng xoá) → cổng coi PR "mang bằng chứng" và im lặng. Không hồ sơ nào tồn tại trong cây đang merge, mà cổng vẫn xanh. Nghịch lý: PR càng xoá nhiều bằng chứng càng dễ được miễn trừ. | Ca thử: diff chỉ chứa các dòng XOÁ dưới `_acceptance/<slug>/` + file mã non-T1 → phải nổ. Sàn: `gate_touched=1` chỉ khi thư mục slug còn tồn tại trong cây đang merge (`[ -d "$ROOT/_acceptance/<slug>" ]`), không chỉ khi tên nó xuất hiện trong diff. | known-limit (owner 2026-08-12) |
| P1 | vật + evals | Đường dẫn có ký tự ngoài ASCII bị `git diff --name-only` **đóng ngoặc kép và escape bát phân** (`core.quotePath` mặc định bật). Đo thật: `_acceptance/hồ-sơ/contract.md` in ra `"_acceptance/h\341\273\223-s\306\241/contract.md"` — dấu `"` mở đầu làm HỎNG cả hai nhánh `case` (`_acceptance/…` cần khớp từ ký tự đầu; `*/_acceptance/…` cần một `/` ngay trước). Kết quả đo: **exit 1, FALSE RED**, và chính artifact của cổng bị liệt kê như file mã vi phạm. Không ca thử nào dùng tên non-ASCII, dù kit này viết tiếng Việt xuyên suốt. | Vòng sau, một hồ sơ được đặt slug `_acceptance/răng-t1-escape/`. PR mang đầy đủ contract + evals + evidence, nhưng cổng in `VIOLATION [PR]: non-T1 files changed … Changed: "_acceptance/r\304\203ng-t1-escape/contract.md"` — tức cổng tố cáo chính bằng chứng của nó là mã chưa nghiệm thu. Cùng cơ chế cũng đánh sập `stale_files()` (artifact bị tính là mã đổi → mọi hồ sơ stale) và `slug_in_diff()` (luật per-slug/gap-probe tắt im lặng cho slug đó). Đây là chiều ĐỎ OAN HÀNG LOẠT. | Ca thử với slug/tên file mang dấu tiếng Việt, cả chiều phải-nổ lẫn phải-KHÔNG-nổ. Sửa gốc: chạy `git … -c core.quotePath=false diff --name-only` ở mọi chỗ sinh `DIFF_FILES`/`stale_files`, kèm ca thử chứng minh không còn quote. | known-limit (owner 2026-08-12) |
| P2 | contract | Out of scope "**Không** thêm ánh xạ path→slug" là mục duy nhất nói về ranh giới "cái gì được tính là slug", nhưng không có eval âm nào chốt **sàn tối thiểu** được chấp nhận. Chính khoảng trống này là chỗ P0 #2 chui qua: hợp đồng không bao giờ nói "thư mục con bất kỳ, không cần contract.md, là đủ" — nên người ký không có cơ hội từ chối nó. | — (mục chất lượng: người ký ở Cổng 2 không nhìn thấy residual mình đang chấp nhận) | Một eval âm ghi thẳng sàn: "PR mang `_acceptance/<dir>/x` KHÔNG có contract.md → hành vi kỳ vọng là ___", buộc hợp đồng khai màu. | known-limit (owner 2026-08-12) |
| P2 | evals | Out of scope "**Không** đổi thông điệp violation" không có eval nào ghim nguyên văn thông điệp, trong khi TE23a lại assert theo chuỗi con `VIOLATION [PR]: non-T1` và TE21b/TE22b chỉ theo tiền tố `VIOLATION [PR]`. Ràng buộc một chiều: đổi thông điệp thì TE23a im lặng thành vacuous-pass (xem P1 #2), còn TE21b/TE22b vẫn xanh. | Một PR sau này viết lại thông điệp cho rõ nghĩa (`VIOLATION [PR]: code outside t1_skip_globs …`). TE21/TE22 vẫn xanh (tiền tố còn), TE23a xanh nhưng rỗng nghĩa. Lời hứa "không đổi thông điệp" không có răng. | Một eval ghim nguyên văn hai dòng thông điệp (T3 và non-T1) — cùng nếp DV5 additive-only vốn đã ghim dòng luật. | known-limit (owner 2026-08-12) |
| P2 | evals | Coverage của hợp đồng khai "[thước CE: **4 nhánh** của `case` sau khi vá]", nhưng ca thử chỉ chạm 2 nhánh gốc-repo (`_acceptance/*/*`, `_acceptance/*`). Hai nhánh lồng (`*/_acceptance/*/*`, `*/_acceptance/*` — hình dạng monorepo mà comment dòng 469–472 nói rõ là phải đỡ) không có ca nào. Đo thật hôm nay: cả hai chiều đúng (`pkg/_acceptance/config.yaml` + `pkg/src/app.js` → nổ; `pkg/_acceptance/sl/contract.md` + `pkg/src/app.js` → không nổ) — nên là lỗ ĐỘ PHỦ, chưa phải lỗi sống. | Một bản vá tương lai gộp hai nhánh lại cho "gọn" và làm rơi tiền tố `*/`. Repo gốc xanh hết; mọi consumer monorepo mất răng im lặng, và không phép đo nào của kit biết. | Hai ca thử monorepo (`pkg/_acceptance/...`), một chiều nổ một chiều không nổ. | known-limit (owner 2026-08-12) |
| P2 | evals | E7 (AC-6) ghim `"Results: N passed, 0 failed"` với **N để mở**, trong khi evidence-report đã biết con số chính xác (677 trên nhánh, 671 trên base). Một suite bị xoá ca vẫn cho `0 failed`. | Kết hợp với P0 #1: xoá TE21–TE23 → `674 passed, 0 failed` → E5, E7, E9 xanh nguyên. Ba trong mười eval máy không hề phân biệt được suite đủ với suite thiếu. | E7 ghim con số tối thiểu (`>= 677`) hoặc so đếm nhánh-vs-base như evidence-report đã tự làm bằng tay. | known-limit (owner 2026-08-12) |

## Xử lý ở Cổng 2

Bốn P0 đã VÁ và có ca phản chứng riêng (TE21/22 · TE24 · TE25 · TE26),
cộng TE27 cho lỗi `$ROOT` fail-closed mà judge tìm ra sau đó.

P1 và P2 còn lại: owner quyết **ghi Known-limits** ngày 2026-08-12 — không
mục nào làm cổng xanh sai. Đáng theo dõi nhất là P1#2: `TE23a` là `nothas`
thuần nên PASS được cả trên chuỗi rỗng; rủi ro thực tế thấp vì cùng chiều
chống-siết-quá-tay đã có `TE24`, `TE25`, `TE27` với assert dương. Ai chạm
khối T1-escape lần sau nên nâng `TE23a` lên assert dương cùng lượt.
