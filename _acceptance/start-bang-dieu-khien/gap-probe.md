---
slug: start-bang-dieu-khien
at: 2026-08-23T01:27:11Z
verdict: findings
p0: 0
p1: 4
p2: 1
---

## Findings

| Sev | Artifact | Thiếu gì | Kịch bản fail | Thước đo | Xử lý |
|---|---|---|---|---|---|
| P1 | contract | Trục C có năm hình dạng lỗi nhưng «giả» không có AC nào và cũng không có dòng nào trong `## Out of scope`. Hai vật cụ thể của trục này bị bỏ trống: hố 4 phần iii của hồ sơ cơ hội — Cổng Giá trị luôn đứng đầu thẻ vì mốc rỗng, `start-scan.mjs:232-234` cộng sort `:295`, ghi là C4 ở hàng 15 bảng §4 design — và dòng gộp «cũ nhất X ngày» mà AC-1 bắt GIỮ NGUYÊN trong khi chính design §2 đo được 6 trên 7 ý cùng một dấu thời gian đổ stub | Owner đọc mục Coverage thấy đủ ba trục và bảy AC chia đều, duyệt phạm vi với niềm tin bốn hố của hồ sơ cơ hội đều có người nhận. Vòng ship. Phiên `/start` đầu tiên vẫn mở bằng hai hồ sơ `cho-cong-gia-tri` vì `since` rỗng được sort xếp lên đầu, và vẫn in «cũ nhất 2 ngày» cho một nhóm mà sáu phần tử cùng một dấu thời gian. Mọi AC xanh, mọi eval xanh, nhưng thứ tự thẻ vẫn không mang tin — đúng lời than mở hồ sơ cơ hội. Cổng Giá trị chấm iterate cho một vòng đã nghiệm thu máy sạch | Một AC cộng một ca trên fixture code-sinh: hồ sơ có `since` rỗng phải rơi về cuối nhóm «chưa biết tuổi», KHÔNG lên đầu; và dòng gộp phải nói «chưa rõ tuổi» khi từ hai ý trở lên chung một dấu thời gian. Chiều đỏ: bản sao khôi phục sort coi rỗng là mới nhất phải đỏ ghim đúng slug bị đẩy lên đầu. Nếu cố ý không làm thì phải có một dòng có tên trong `## Out of scope` để owner thấy lúc duyệt | fixed: AC-12 mới (mốc rỗng xếp cuối · ageTied) + eval E14 ba assert có đối chứng dương và ba chiều đỏ; Coverage nêu rõ cả năm giá trị trục C đều có AC nhận |
| P1 | contract | `## Đường đo` có bảy dòng phủ trọn năm điều kiện SỐNG, nhưng không dòng nào phủ điều kiện CHẾT (a) của `opportunity.md` — thẻ dài tới mức owner bỏ qua một nhóm từ một lần trở lên — và cũng không phủ điều kiện CHẾT (c) — máy tự cắt hoặc tự xếp hạng theo thước tự chế và bị owner veto. Đây đúng là rủi ro số một của một ô mà nội dung chính là BỎ giới hạn hiển thị | Hết ba phiên, owner nói «phần việc-vừa-xong tôi không đọc» — điều kiện CHẾT (a) đã bật. Phiên nghiệm thu đặt số cạnh ngưỡng đã khai: bảy dòng Đường đo đều đạt đích (0 lần rời thẻ, 100 phần trăm ý hiện, lệch veto 0, một lần chạm dòng mới, 0 lượt gọi người thêm). Không dòng nào đếm số nhóm bị bỏ qua nên không có số nào để đặt cạnh (a); người ký thấy bảng toàn xanh và ký release cho một ô mà ngưỡng CHẾT đã bật | Thêm hai dòng vào `## Đường đo` theo đúng khuôn ba phần: thước «số nhóm owner khai đã bỏ qua trong một phiên» đích 0, số từ owner nói trong phiên, bảo đảm bởi AC-1 và AC-4; thước «số lần owner veto một lần cắt hoặc một thứ tự do máy tự chọn» đích 0, số từ owner đếm. Mỗi dòng nêu rõ AC bảo đảm, hoặc khai thẳng là dòng chỉ đo bằng người | fixed: thêm hai dòng vào ## Đường đo cho điều kiện CHẾT (a) và (c), khai THẲNG là đo bằng người, mỗi dòng nêu AC bảo đảm |
| P1 | evals | E8 dựng fixture bằng code nhưng dựng đúng khuôn mà bên ĐỌC đang chờ: một `evidence-report.md` mang sáu điều kiện xanh-sạch do chính test viết ra, không có chân nào round-trip từ writer thật của S4. Cả AC-8 lẫn design §8 chỉ đòi round-trip GIỮA bốn bộ đọc, tức đo reader với reader, không đo writer với reader. Cùng lớp với [het-gio-khong-phai-truot#F1] | Test sinh report đặt `verdict` và `bypass_used` ở đúng khoá mà máy quét đang đọc. Bốn bộ đọc nhất trí, 0 trên 4 mời ký, đối chứng dương với `bypass_used: true` cũng xanh, E8 xanh. Trên cây thật, report do harness S4 sinh đặt cùng sự thật ở heading hoặc khoá khác một chút, nên vị từ xanh-sạch không khớp và mọi hồ sơ rơi về `cho-cong-bang-chung`: `gate-card.js` lại in «máy đã xong — ký nhanh» và `acceptance-status` lại in «Chờ người ký» — đúng cái AC-8 sinh ra để giết — trong khi toàn bộ eval máy vẫn xanh và nghiệm thu vẫn PASS | Thêm một chân lấy `evidence-report.md` THẬT của một hồ sơ `signed-off` đang có trong cây, hoặc sinh report bằng chính bộ writer của S4, rồi cho cả bốn bộ đọc đọc và assert cùng chữ. Đặt khuôn sáu điều kiện xanh-sạch ở MỘT chỗ có marker rồi test round-trip rút-từ-writer đọc-bằng-reader, theo mẫu `OOC-ITEM-TEMPLATE` với ca P55 | fixed: E8 nay rút khuôn sáu điều kiện từ chính nguồn bên VIẾT (evidence-report-template.md, khối marker) rồi dựng fixture từ đó, cộng một chân neo vào evidence-report.md THẬT của hồ sơ signed-off trong cây; gỡ một khoá khỏi khuôn writer là chiều đỏ chứng minh round-trip sống |
| P1 | evals | AC-1 hứa «không còn con số trần nào giới hạn danh sách» và AC-7 hứa «không bộ đọc nào tự chế chuỗi» — cả hai là QUAN HỆ trên không gian mở, nhưng E1 đo bằng hai mẫu regex «tối đa số» và «số tên», còn E7 đo bằng đếm 0 hit của đúng hai chuỗi «Chờ người ký» và «Chờ code». Danh sách đen trên không gian mở, không có ca đỏ NGOÀI danh sách | Người thi công viết lại giới hạn bằng chữ, «tối đa ba ý gần nhất» hoặc «chỉ liệt ba dòng đầu»: hai regex của E1 ra 0 hit nên E1 xanh, thẻ vẫn cắt backlog và hố số một của hồ sơ cơ hội còn nguyên. Song song, `acceptance-status.md` bỏ đúng hai chuỗi bị cấm nhưng viết một chuỗi tự chế mới, «Đợi chữ ký», cho cùng một sự thật: E7 xanh vì hai chuỗi cũ đã biến mất, còn bên viết và bên đọc lại trôi khỏi nhau — điều kiện CHẾT (b) bật ngay phiên đầu mà suite vẫn xanh | Đo quan hệ chứ không đo từ vựng: rút MỌI cụm nhãn ứng viên trong `acceptance-status.md` bằng một khuôn có marker rồi assert tập đó là tập con của bảng trạng-thái→chữ, ca đỏ tiêm một nhãn tự chế mới NGOÀI danh sách đen phải đỏ nêu đúng nhãn. Với E1, assert mệnh đề dương «in mọi phần tử» có mặt, cộng ca đỏ tiêm cap viết bằng CHỮ chứ không bằng chữ số | fixed: E1 đổi sang đo mệnh đề DƯƠNG + round-trip số dòng === groups.considering.length, chiều đỏ (2) tiêm cap viết bằng CHỮ; E7 đổi blacklist sang ALLOWLIST (tập nhãn rút bằng marker phải là tập con của bảng), chiều đỏ (3) tiêm nhãn tự chế MỚI ngoài mọi chuỗi bị cấm |
| P2 | evals | Hằng ghim vào vật sẽ đổi, hai chỗ cùng một lớp: E5 ghim «vetoOpen có đúng 16 phần tử» cạnh phép so tập-bằng-tập vốn đã mang trọn sự thật, và E10 ghim hash của khối mã `discoverySkill` trong suite plugins vĩnh viễn. Cùng lớp với [d-20260823T003521Z-4606] | Con số này đã trôi trong đúng một ngày và ba artifact đang ghi hai số khác nhau: `decisions.jsonl` và `opportunity.md` ghi 14, contract và design ghi 16. Một làn song song merge thêm một hồ sơ `veto_state: mo` trước S4 của vòng này, hoặc chính hồ sơ này nhận `veto_state: mo` ở làn V, thì E5 đỏ trên một bản cài ĐÚNG và cách chữa duy nhất là sửa con số — đúng nếp vá thước thay vì đọc thước. Với E10, sau khi ô này merge, một PR bất kỳ đổi tên biến hoặc format lại khối ổ cắm làm BDK3 đỏ vĩnh viễn trong suite chung, dù ý nghĩa ổ cắm không đổi một chữ | Bỏ hằng, giữ quan hệ. E5 chỉ assert tập slug bằng tập `grep` cộng phép phá thử cùng tăng 1, sàn đếm để ở «lớn hơn hoặc bằng 1» thay vì bằng 16. E10 assert ổ cắm còn ĐÚNG tên khoá và đúng giá trị mặc định — một quan hệ đọc được — thay vì hash toàn khối, hoặc chuyển ca hash sang răng hồ sơ chết-khi-merge như năm ca kia đã quyết ở `d-20260823T012029Z-10755` | fixed: E5 bỏ hằng 16, assert tập-bằng-tập + quan hệ «chứa ≥1 hồ sơ signed-off» + sàn đếm; E10 bỏ hash toàn khối, đổi sang fixture config ba hình dạng đo đường fallback còn sống; contract ## Notes ghi rõ 16 là quan sát 23/08 chứ không phải hằng ghim, và hai file đã ký giữ nguyên số 14 |
