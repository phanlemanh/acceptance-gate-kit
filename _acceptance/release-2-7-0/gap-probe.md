---
slug: release-2-7-0
at: 2026-09-02T16:52:26Z
verdict: findings
p0: 1
p1: 4
p2: 7
by: phiên tươi độc lập (subagent context sạch, không chia sẻ context với phiên dựng hồ sơ)
---

## Findings

| Sev | Artifact | Thiếu gì | Kịch bản fail | Thước đo | Xử lý |
|---|---|---|---|---|---|
| P0 | .claude-plugin/plugin.json mục v2.7.0 vế (2) + contract.md Context mục 2 | Vế (2) khai «khối PHÁN QUYẾT ĐỐI KHÁNG trên THẺ CỔNG 2 … khi đối kháng không chạy được (probe-failed, vắng dưới gap_probe: required, không đọc được) thì thẻ nói thẳng và KHÔNG điền sẵn gì — fail-closed». SAI cho thẻ Cổng 2. Rơi bậc chỉ có ở thẻ Cổng 1: AC-4 của #136 viết «When dựng thẻ Cổng 1»; roiBac tính trong nhánh Cổng 1 (gate-card.js 410–413, 563, 638); cả 6 ca LM06 + LM08 đều chạy --gate 1; oneShotG2 (dòng 811) chỉ phụ thuộc verdict PASS/PENDING-JUDGMENT (dòng 659), không nhìn gap-probe. Dựng tại chỗ 5 trạng thái trên fixture Cổng 2 (đối chứng dương findings · vắng+required · probe-failed · verdict token lạ · frontmatter vỡ): cả 5 đều in «cắt/hoãn: đồng ý cắt; ký hay trả: ___» điền sẵn, không cờ MSG_ROI_BAC; token lạ «co-le» được in NGUYÊN VĂN vào ô «Phản biện context sạch:» như thể là một phán quyết | Người ký Cổng 2 ở repo tiêu thụ đọc mô tả 2.7.0, tin rằng hễ dòng lệnh còn điền sẵn thì đối kháng đã chạy; thực tế thẻ vẫn mời ký với ô điền sẵn khi đối kháng vắng hoặc hỏng — đúng «chữ ký = tin-suông khoác áo thẩm định» mà luật lời-mời 01/09 và chính vòng #136 sinh ra để chặn. Token lạ in như phán quyết là một đường fail-quiet mới cùng họ ba đường vế (3) vừa đóng | Mọi câu trong manifest mô tả HÀNH VI phải có ca ghim hành vi đó trên đúng cổng được nêu; ở đây ca ghim tồn tại cho cổng KHÁC. Lớp «bên viết trôi khỏi bên đọc», cùng lớp P0 thứ nhất của gap-probe 2.6.0 | proposed: sửa CHỮ (không sửa mã — mốc phát hành không dựng răng): vế (2) manifest và mục 2 Context ghi rõ «rơi bậc fail-closed ở THẺ CỔNG 1 (approve); thẻ Cổng 2 in trạng thái gap-probe nguyên văn, KHÔNG rơi bậc» và thêm vào Declared limits; mở ô cho mốc kế: Cổng 2 rơi bậc + token lạ không in như phán quyết |
| P1 | contract.md · bảng Ba dòng số, ô «Lượt gọi người» của #136 | Hai văn bản của CÙNG vòng khai hai bộ số: evidence-report.md đã ký (Known limits #9) ghi «trong thiết kế 3 (đúng trần) · ngoài thiết kế ≥6» = ≥9; hợp đồng mốc ghi «4 · ≥7» = ≥11 mà không nói vì sao khác. Phần tách «≥4 lần phiên dừng phải nhắc tiếp tục/resume» không có vết: nguồn dẫn (findings 2026-09-02-may-khai-dang-chay-va-bo-goal) trích owner bắt «hai lần trong cùng phiên». Đếm lại từ sổ: 2 dừng-vá trình người (4002 · 6002→8001) · ≥2 lần phiên dừng/khai «đang chạy» sai (findings) · 1 lượt owner đưa ảnh Radar (7004) · 1 lượt owner truy nguyên «nghi thức đã bỏ» (7005) = ≥6 có vết; ≥7 thì không. Về «trong thiết kế»: Cổng Đáng của #136 ký 01/09 14:35Z — TRƯỚC mốc mở cửa sổ 93da8752 (01/09 23:39Z), nên đếm trong cửa sổ thì 3 (Phạm vi 31ce4371 · Gate 1.5 3541b2aa · Bằng chứng 86029a3d), đếm theo vòng thì 4 | Con số nuôi mục tiêu ≤3 lệch giữa hồ sơ đã ký và hồ sơ phát hành; lần sau ai đếm lại sẽ ra số thứ ba. Chiều lệch lần này BẤT LỢI cho kit (≥11 > ≥9) nên không phải lách, nhưng cùng lớp P1 «5 vs 6» của gap-probe 2.6.0: bộ đếm khép ở hai thời điểm khác nhau cho hai số khác nhau | Mỗi lượt đếm phải có MỘT vết (entry sổ, commit, hoặc trích dẫn) — bảng lượt→vết, không phải tổng | proposed: thay hai con số bằng bảng lượt→vết (mỗi dòng một vết), chốt MỘT tổng và nói rõ vì sao khác Known limits #9 của hồ sơ đã ký (không sửa hồ sơ đã ký) |
| P1 | contract.md · Ba dòng số, câu «trong thiết kế 4 … đúng số cổng T3» + «trượt ở hạ tầng phiên, không ở vật» | Luật (c) CLAUDE.md khai mục tiêu ≤3 và định nghĩa 3 = Đáng · Phạm vi · Bằng chứng — KHÔNG có vế T3. Gate 1.5 có thật trong thiết kế feature-loop (SKILL.md dòng 10 «Gate 1.5 (T3)»), nhưng luật (c) chưa từng đếm nó. Hợp đồng KHÔNG tuyên đạt (ghi TRƯỢT — đúng), nhưng gán toàn bộ phần trượt cho «hạ tầng phiên»: một vòng T3 với 0 lượt ngoài thiết kế vẫn là 4 > 3, tức trượt ở chính thiết kế cổng, và câu «đúng số cổng T3» là định ngữ thêm SAU khi thấy số — cùng hình dạng câu «vòng meta, loại vòng ngưỡng không nhắm tới» bị cắt ở phản biện 2.6.0, nhẹ hơn vì chưa dùng để tuyên đạt | Mốc sau lặp lại: T3 đếm 4 «trong thiết kế», gọi là đúng, và mục tiêu ≤3 thành chữ không ai đối chiếu ở đúng hạng vòng đắt nhất. Ngược lại nếu luật thật sự muốn 4 cho T3 thì mọi mốc T3 sẽ bị ghi TRƯỢT oan | Ngưỡng khai trước chỉ được diễn giải lại bằng một quyết định người ghi sổ, không bằng một câu trong bảng | proposed: đây là câu hỏi thật duy nhất của mốc cho owner — «luật (c) có đếm Gate 1.5 vào trần T3 không?» (đồng ý → sửa luật thành ≤3 T2 / ≤4 T3, ghi entry; không → giữ 3 và sửa câu thành «T3 trượt mục tiêu ngay cả khi 0 lượt ngoài thiết kế»); bỏ vế «không ở vật» |
| P1 | contract.md frontmatter + decisions.jsonl (cả 3 dòng) — và sổ của hồ sơ #136 được dẫn làm nguồn | Cả 3 entry và veto_opened_at đều 2026-09-02T16:45:00Z — số tròn, giống hệt nhau, và nằm SAU commit chứa chúng (15ebbbc6 = 16:34:46Z) khoảng 10 phút. TÁI PHÁT LẦN 3: gap-probe 2.5.0 (P1) và 2.6.0 (P1, đề nghị «ghi sổ lớp này là tái phát lần 2 để lần sau thành phép đo») — không phép đo nào được thêm. Cùng lớp ở hồ sơ đã ký #136: entry 7001/8001/gate2seal khai 18:00Z–20:37Z nhưng commit 86029a3d là 16:16Z; 9001 khai 21:00Z, commit 1fe8749c 16:22Z; 7004 khai 19:00Z, commit 656f55c8 15:25Z — lệch 2–5 giờ về tương lai. Cùng chỗ: Context ghi «mốc 2.6.0 (93da8752, 01/09)» theo UTC trong khi mọi giờ trong bảng Ba dòng số là giờ VN (+07) | Bộ đối chiếu sổ↔git thấy quyết định được ghi sau khi đã commit, ở cả hồ sơ phát hành lẫn hồ sơ nó dẫn làm bằng chứng; ba mốc liên tiếp cùng lỗi nghĩa là lời dặn không thành cơ chế — đúng điều luật «cấm dặn-bằng-lời làm nghiệm» nói | Dấu thời gian trong hồ sơ phải lấy từ đồng hồ lúc ghi và không lớn hơn thời điểm commit chứa nó; mốc ngày dùng MỘT múi giờ | proposed: điền at thật cho 3 entry + veto_opened_at của mốc này (rẻ, chưa ký); hồ sơ #136 đã ký → chỉ ghi sổ, không kéo vào diff; ghi lớp này là tái phát lần 3 và gọi tên nó làm ứng viên phép đo (so at với thời điểm commit) cho cửa sổ kế thay vì dặn lần thứ tư |
| P1 | .claude-plugin/plugin.json mục v2.7.0 vế (1), câu «Measured on a consumer repo the day before release … under this card the same dossier shows 2 blanks» | Hai vế không tái lập được từ repo: (a) «shows 2 blanks» ngụ ý thẻ 2.7 đã được dựng trên hồ sơ Radar — không có bản chụp, run-log hay file nào chứng; entry 7004 tự khai nó SUY từ luật định tuyến (E8 là judgment → hỏi; cắt/hoãn + Treo → báo), không phải một lần render; (b) «the day before release»: Radar ký 21:10 giờ VN 02/09, hồ sơ mốc mở 23:34 giờ VN 02/09 — cùng ngày. Nguồn gốc của cả cụm số là entry 7004, lời khai từ ảnh chụp hội thoại ở kho khác — hạng đúng là ĐIỂM DỮ LIỆU KHAI, không phải phép đo | Consumer đọc ghi chú phát hành như một phép đo trước/sau trên hồ sơ thật; nếu về sau dựng thẻ 2.7 trên hồ sơ Radar ra 3 ô (vì một mục Treo/Ngoài không như suy) thì ghi chú phát hành sai và mọi «measured» sau này mất tín nhiệm | Câu «measured/shows» trong manifest phải trỏ được tới một vật (render, log, ảnh trong hồ sơ); suy luận thì viết là suy luận | proposed: đổi thành «Observed on a consumer repo the same day (reported by the owner): … under this card's routing rule the same dossier WOULD show 2 blanks» — hoặc dựng thẻ 2.7 trên bản sao hồ sơ Radar và ghim bản chụp vào hồ sơ mốc rồi giữ chữ «shows» |
| P2 | contract.md · bảng Ba dòng số, ô «Vòng chấm 8 (dừng-vá nổ ba lần, ship với giới hạn)» của Radar | Không có nguồn nào trong repo: quét docs/ và _acceptance/ tìm «Radar», «8 vòng», «8 chấm», «dừng-vá nổ ba» → 0 hit ngoài chính hợp đồng; entry 7004 và evidence-report #136 chỉ khai 4 ô/2 chạm, không khai số vòng. Ô bên cạnh đã ghi «chưa đếm tay» nhưng ô này ghi một số cụ thể | Số 8 vào sổ nhớ và bảng luật (a) của mốc kế như thể đã đếm; hợp đồng vừa nói «chưa đếm tay trọn vòng» vừa cho một con số | Mỗi ô của bảng Ba dòng số phải ghi nguồn đếm (nếp từ gap-probe 2.5.0) | proposed: đổi thành «chưa đếm (owner khai ~8, chưa có vết trong repo này)» hoặc bỏ số |
| P2 | contract.md Context, câu «scripts/gate-card.js (207 dòng)» | 207 là số dòng ĐỔI trong diffstat (173 thêm + 34 bớt), không phải cỡ file: file 853 dòng ở 93da8752 → 992 dòng ở eed998ab. Đặt trong ngoặc sau tên file, câu đọc như cỡ file | Người đọc hình dung vật nhỏ hơn 4–5 lần thực tế; ai kiểm bằng wc thấy lệch và nghi luôn 11 số còn lại (tất cả đúng — đã đếm) | Số kèm tên file phải nói nó đếm gì | proposed: «(+173/−34 dòng, file 992 dòng)» |
| P2 | evals.yaml E3e | Vẫn viết thẳng «node scripts/product-map.mjs --check» trong khi config.yaml khai cùng phép đo ở khoá executors.script.product_map với «--root . --check» và suite_keys dùng khoá đó — đúng P2 của gap-probe 2.6.0, disposition «proposed» chưa được áp | Hai cách viết cho một phép đo; khoá đổi cờ thì eval không đi theo | Một phép đo một chỗ khai | proposed: đổi E3e sang config:executors.script.product_map (tái phát lần 2) |
| P2 | contract.md AC-1 + evals.yaml E1/E2/E6 — liên quan câu hỏi «ba số trùng 2.7.0 có làm P200 mù không» | Đã kiểm: chuỗi «v2.7.0» xuất hiện ĐÚNG MỘT lần trong mô tả acceptance-gate (vị trí 26962, sau «v2.6.0» ở 24954) và hai lần trong mô tả feature-loop đều nằm trong mục mới; diagram-design không có mục v-số. Vì muc() lấy indexOf đầu tiên, sự trùng số không làm 5 đột biến chọn nhầm mục — KHÔNG mù ở cây này. Điểm mù còn lại vẫn là điểm đã khai: cây quên bump CẢ HAI số vẫn xanh vì mọi số đọc từ manifest, chỉ khai ở comment đầu evals.yaml; hợp đồng vẫn không có dòng Known limits (P2 2.6.0 đề nghị, chưa áp). Rủi ro có tên cho mốc SAU: hễ một mục lịch sử nhắc «v<số hiện tại>» (ví dụ nhắc diagram-design v2.7.0 trong mục v2.6.0) thì indexOf sẽ chọn mục lịch sử và đột biến «thiếu mục» đổi cả hai chỗ thành vXXX nên vẫn đỏ đúng, nhưng vế AG «có mục v…» sẽ xanh oan | Mốc quên bump cả hai số vẫn xanh; mốc sau có mục lịch sử nhắc số hiện tại thì vế «có mục» xanh oan | P200 cố ý không ghim mốc di động; muc() lấy lần xuất hiện đầu | accepted (đề xuất): thêm một dòng Known limits vào hợp đồng cho khớp evals.yaml; ghi rủi ro indexOf-đầu vào ô P200 cho mốc kế (đổi sang lastIndexOf hoặc đòi «v<số> —» đầu mục) — không thêm răng trong mốc |
| P2 | .claude-plugin/plugin.json mục v2.7.0 vế (4) | Câu «approve/signoff write the identity straight away … no Enter round-trip» trình như cơ chế; thực thể là khối chỉ dẫn markdown IDENTITY-ECHO-RULE cho model đọc (3 bản md5 trùng — đã kiểm), lưới LM17 chỉ đo VĂN BẢN luật; Known limits #7 của #136 tự khai «AC-8 chỉ đo văn bản luật, không đo hành vi hội thoại». Declared limits của manifest nói «gate invitation message still has no permanent net» nhưng đặt cách xa vế (4) | Consumer tin có cơ chế; một phiên bỏ qua chỉ dẫn thì không gì đỏ | Mô tả phải phân biệt cơ chế máy giữ với chỉ dẫn máy đọc | accepted (đề xuất): thêm nửa câu «(rule the model follows; text-level net only)» vào vế (4) |
| P2 | .claude-plugin/plugin.json vế (1) «HTML line is byte-equal to the export» + LM09/LM15 | Đã kiểm: LM09 (Cổng 2) và LM15 (Cổng 1) so ĐẲNG THỨC phần trong thẻ b của dòng «Dòng lệnh» với one_shot của --extract. Nhưng HTML đi qua esc() nên chỉ byte-equal khi one_shot không chứa & < > " — đúng với slug hợp lệ và các token cố định hiện có, chưa được ghim | Một slug hay nhãn Ngoài-n mang ký tự đặc biệt sẽ làm hai mặt lệch mà không ca nào đỏ trước khi người thấy | Lời «byte-equal» cần một ca có ký tự bị esc | accepted: đúng cho miền slug hợp lệ; ghi giới hạn, không thêm răng trong mốc |
| P2 | phạm vi phiên phản biện này | Chưa chạy bốn suite (E3/E3b/E3c/E3d) — theo đề bài, đã chạy riêng. Phiên này chỉ đọc mã P200 (không chạy), chạy gate-card --extract Cổng 1 trên chính hồ sơ này (trước khi có file này: roi_bac on, reason vang — đúng luật rơi bậc; sau khi ghi file này phải tắt) và dựng 5 fixture Cổng 2 cho dòng P0 | Nếu một suite đỏ thì AC-3 chưa đạt và phiên này không phát hiện được | Vòng chấm S4 chạy đủ bốn suite là chỗ đóng dòng này | proposed: để S4 đóng; ghi ở đây để không ai nhầm là đã kiểm |

## Đã kiểm (ngoài các dòng trên)

- **Mười hai file, chia 6/2/3/1 đúng**: numstat 93da8752..eed998ab (trừ
  _acceptance, docs) ra đúng 12 file; sáu file hành vi · hai file chép luật ·
  ba file lưới · PRODUCT-MAP.md khớp từng tên. Bài học P1 của 2.6.0 (đếm lưới
  trong nhà vào cột consumer) ĐÃ được áp: ba file lưới được dán nhãn «repo tiêu
  thụ không chạy suite của kit».
- **feature-loop đổi đúng hai câu chép luật**: numstat 2/2 chỉ trên
  feature-loop/skills/feature-loop/SKILL.md; hai dòng là hai bản chép
  GATE-INVITE-CLAUSE (Cổng 1 + Cổng 2), khớp nguyên văn với nguồn ở
  human-facing-language.md dòng 133 và hai bản chép trong skills/acceptance/SKILL.md;
  không workflow, không script. Mục v2.7.0 của feature-loop khai đúng như vậy và
  tự khai cặp «acceptance-gate >= 2.7.0».
- **33 ca / 10 ca đúng**: gate-card-lmcms.test.mjs = LM01–03b (4) + LM04×2 +
  LM05×5 + LM06×6 + LM07–12 (7) + LM13/20/14/15/16/17/18/18c/19 (9) = 33;
  out-of-contract.test.mjs = ma trận 8 + 2 = 10.
- **Mốc giờ đúng**: d859a830 = 11:52:36+07 (implemented), 86029a3d = 23:16:14+07
  (ký) → 11h24; 873db3de = 22:08:07+07 → 1h08. Cửa sổ neo vào mốc GỘP 93da8752
  (P2 của 2.6.0 đã sửa); trong cửa sổ chỉ MỘT commit ký Cổng 2 (86029a3d) → «đúng
  một vòng đã ký» đúng.
- **Bằng chứng không cũ**: 873db3de → eed998ab chỉ đổi PRODUCT-MAP.md (máy sinh)
  ngoài _acceptance và docs; mã được ship đúng là mã đã chấm.
- **Vế (1)(3) của manifest khớp vật**: ONE-SHOT-CMD (gate-card.js 91–96),
  ROUTING-RULE (98–116), suspect_empty (out-of-contract.js 128) đều có — câu
  «Verify by CONTENT» trỏ đúng chuỗi tồn tại (lỗi 2.5.0 không tái phát); one_shot
  xuất ở --extract cả hai cổng (564, 815); MSG_OOC_SUSPECT gọi tên
  OOC-ITEM-TEMPLATE (87); token lạ in nguyên văn kèm ba token (LM03/LM03b);
  cột SẼ/KHÔNG dò mệnh đề đầu (HEAD_NEG_RE, chú giải 279–280; LM04 trên hai hồ
  sơ thật + LM05 ma trận 5 ô hai chiều). Khối PHÁN QUYẾT ĐỐI KHÁNG có ca LM11.
- **AC-2**: GUIDE.md dòng 5 «Khớp phiên bản: acceptance-gate 2.7.0 · feature-loop
  2.7.0 · diagram-design 2.7.0.» khớp đúng câu P200 dẫn xuất từ ba manifest.
- **t3_paths**: lib/** nằm trong danh sách (config.yaml 249–254) → #136 đi T3 đúng.
- **P200 không đổi trong cửa sổ**: 28/4 dòng đổi của tests/plugins/run-tests.sh
  nằm ở ca đọc-cũ P150 và hai ca round-trip dòng mẫu (8905, 9503) — khối P200
  (10528+) nguyên vẹn.
