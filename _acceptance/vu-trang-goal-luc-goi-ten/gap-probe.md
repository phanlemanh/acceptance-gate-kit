---
slug: vu-trang-goal-luc-goi-ten
at: 2026-09-03T03:57:09Z
round: 2
verdict: findings
p0: 0
p1: 2
p2: 4
by: phiên tươi độc lập vòng 2 (subagent context sạch; đọc lại trọn bộ artifact SAU sửa S1 + vật thật SKILL feature-loop / GUIDE / gate-card.js / P85 / lmcms / acceptance-verify.js / superpowers brainstorming 6.3.0 bản đang cài; KHÔNG chạy suite; bản vòng 1 at 2026-09-03T03:31:18Z bị ghi đè — nội dung 11 finding cũ giữ ở bảng trạng thái dưới)
---

## Trạng thái 11 finding vòng 1

| # | Vòng 1 | Trạng thái | Bằng chứng từ chữ hợp đồng / thiết kế / sổ |
|---|---|---|---|
| 1 | P0 — điểm vũ trang «lúc làm / S0» kết ngay câu hỏi brainstorm đầu | ĐÓNG MỘT NỬA | (1) Phạm vi 2 + D3(a) viết lại: điểm đầu = «câu xác nhận thiết kế cuối brainstorm», câu «làn V T2 chỉ cần lần đầu» thành «lượt người CUỐI của cả vòng» ✓. (2) D5 + AC-4(b): gap-probe S1#7 «đồng bộ / chờ trong lượt, KHÔNG nền» ✓. (3) giả định 1 vẫn «Chưa thử — khai Known limits nếu không đo được» — chưa có chỗ ghi (contract không có `## Notes`, xem #11). (4) «trần 15 lượt nay phủ thêm S1» — không dòng nào trong contract/design/sổ. Phần còn lại của điểm (a) — câu xin duyệt của brainstorm có hình dạng gì, gộp vào Gate 1 hay không, brainstorm 0 câu hỏi — xem finding P1 đầu bảng dưới. |
| 2 | P1 — «khớp TỪNG BYTE» không có nghĩa cho bản JS | ĐÓNG | AC-1: «so ba khối sau `strip()` (đúng nghĩa P85 hiện hành) và đếm dòng, Then ba khối bằng nhau từng ký tự và cùng 6 dòng»; E1: «P85 VE: <file> khop (6 dong) — so sau strip». Sổ 1006. Phần dư nhỏ (định nghĩa payload JS, cách đột biến) → P2 cuối bảng dưới. |
| 3 | P1 — Gate 1.5 không có mục riêng, chỉ lớp lời; vật phủ 0/2 lần dừng có vết của T3 | ĐÓNG MỘT NỬA | AC-4(d) neo «mệnh đề «T3: GATE 1.5» trong S2#3 có vế «kèm dòng /goal»» ✓ (SKILL dòng 143 đúng là mệnh đề trong S2#3); D3(c) khai «lớp lời, không thẻ» ✓. Dòng số «T3: vật phủ 0/2 lần dừng có vết» KHÔNG nằm ở đâu trong contract (không có `## Notes`) — bản chấm S4 sẽ không chép được. |
| 4 | P1 — «S4 qua Workflow» là lời hứa không vật | ĐÓNG (vật) · MỞ LẠI (thước) | AC-7 + E9 + Đường đo «AC-7 làm chứng bằng run_id» có mặt ✓; hình dạng vết đúng như acceptance-verify.js đúc (kiểm ở «Đã kiểm» câu 5). Nhưng E9 như đang viết là fail-open và hẹp hơn bên viết → finding P1 thứ hai bảng dưới. |
| 5 | P1 — số «≥4 dừng do tiến trình nền (S3 ×1, S4 ×2)» không có vết | ĐÓNG | Opportunity «Vấn đề»: «2 lần máy giao tiến trình nền rồi … ngừng nói, cả hai ở S3 (lần đầu kèm lời khai «đang chạy» sai); còn lại là 1 rớt mạng và 2 phanh dừng-vá» — khớp findings 02/09 dòng 3–4 («hai lần trong cùng phiên», «Lần đầu Task dừng…») + entry 3008 (S3) + 4002/6002 (dừng-vá). D3(a) không còn vế «chính ca gây dừng» ✓. |
| 6 | P2 — `replace` chỉ thay `<slug>` lần đầu | ĐÓNG | AC-2: «thay MỌI lần xuất hiện của `<slug>` (khuôn có hai) … phép thay độc lập và assert `goal_line` không còn chuỗi `<slug>`»; E2 «(moi <slug> thay, mot dong, 0 <slug> sot)»; D2 «replaceAll». |
| 7 | P2 — «ngay dưới» chưa đo được; câu thẻ «sau khi duyệt» | ĐÓNG | AC-3: «`<div class="mach goal">` đứng ngay sau `<div class="mach">` của dòng lệnh duyệt … đối chứng ba chiều»; D2: câu thẻ «Sau khi trả lời (duyệt hay sửa), dán dòng này…»; D2 nói rõ với làn V điểm vũ trang thật là D3(a). Cách đo «ngay sau» chốt ở «Đã kiểm» câu 4. |
| 8 | P2 — bốn câu «hai bản» sẽ lỗi thời | ĐÓNG | AC-5: «các câu «hai bản» quanh khuôn (SKILL dòng chú thích, GUIDE, tiêu đề P85) đổi thành «ba bản»»; E5 «0 cau ‹hai ban› con sot o SKILL/GUIDE/P85». Hiện trạng còn 4 chỗ: SKILL 121 · GUIDE 312 · run-tests.sh 1384 («2 ban») · 1401 («hai ban») — E5 phải quét theo lớp, không chỉ tiêu đề. Manifest để mốc phát hành ✓. |
| 9 | P2 — id sổ mã hoá 03:25 sau commit | ĐÓNG | 1001–1004: id `…T031200Z` = `at` 03:12:00Z ✓; 1005–1008: id 033500/033600/033700/033800 = `at` từng dòng ✓; commit chứa chúng `db973ee9` = 2026-09-03T10:40:16+07:00 = 03:40:16Z → mọi `at` và id ĐỀU TRƯỚC commit. (Đổi id của 4 dòng đã commit ở `de9a4b78` — luật append-only nói về quyết định, không có gì trỏ id cũ; ghi để biết, không phải lỗ.) |
| 10 | P2 — dòng dõi Ngoài-5 #136 không nhắc | ĐÓNG | Opportunity «Nguồn»: «entry `d-20260902T203500Z-ngoai5` của #136 … ô này lấy nửa sau; nửa «Ngưỡng lột markdown» vẫn mở, chưa có ô»; contract Out of scope: «Nửa còn lại của Ngoài-5 #136 («khối Ngưỡng không lột markdown») — chưa có ô» ✓. |
| 11 | P2 — AC-4 «mục Gate 1.5» không có; thứ tự bước S0; không có `## Notes` để gom Known limits | ĐÓNG MỘT NỬA | (a) ✓ AC-4(d) neo mệnh đề S2#3. (b) ✓ vô hiệu vì điểm S0 đã bỏ. (c) ✗ contract vẫn không có `## Notes` dù sổ 1007 khai «đóng 6 P2 bằng chữ» — nay đã có ≥7 giới hạn không nhà → P2 bảng dưới. |

Tổng: ĐÓNG 8 · ĐÓNG MỘT NỬA 3 (#1 · #3 · #11) · CHƯA ĐÓNG 0.

## Findings

| Sev | Artifact | Thiếu gì | Kịch bản fail | Thước đo | Xử lý |
|---|---|---|---|---|---|
| P1 | design D3(a) + contract AC-4(a) + GUIDE «Khi nào» (AC-5) + Out of scope hai file (vế «lúc «làm» là skill in») | Câu hỏi 1, trả lời bằng chữ của vật: điểm (a) CÓ THẬT trong `superpowers:brainstorming` 6.3.0 (bản đang cài, `~/.claude/plugins/cache/claude-plugins-official/superpowers/6.3.0`) — path bounded bước 4 «Get approval — STOP and wait for an explicit yes» (dòng 91); path architectural «Ask after each section whether it looks right so far» (dòng 185) rồi cổng đọc spec có câu GHIM SẴN duy nhất của cả skill (dòng 224 «Spec written and committed to `<path>`. Please review it…»); spike «a nod is enough» (dòng 83). Nên D3(a) KHÔNG phải điểm không tồn tại — nhưng bốn chỗ hợp đồng chưa khớp vật: (i) câu XIN DUYỆT thiết kế không có chữ ghim — «hình dạng biết trước» chỉ là VỊ TRÍ trong checklist; câu ghim duy nhất (dòng 224) thuộc bước feature-loop S1#4 đã thay (design doc do S1#4 sinh CÙNG LÚC contract/evals), nên không dùng được làm mỏ neo; (ii) architectural = N câu xin duyệt (mỗi mục) + vòng «no, revise» → không có «câu cuối» biết trước, chỉ có «câu người nói yes»; D3 lại ghi «dặn cho câu XÁC NHẬN, không phải cho mọi câu hỏi» — đúng ý nhưng chữ «cuối» dẫn máy in một lần; (iii) SKILL S1#5 ghi «(HARD-GATE của brainstorming và Gate 1A/1B của kit gộp thành MỘT Gate 1)» — đọc thẳng là câu xin duyệt đó bị gộp mất, tức điểm (a) không tồn tại trong feature-loop; AC-4(a) chỉ grep chữ nên xanh ở cả hai cách đọc; (iv) brainstorm KHÔNG có gì để hỏi (hồ sơ cơ hội đã grill — S0#3 «không hỏi lại owner điều đã grill»; chính vòng này: 0 câu hỏi, «gọi tên» 03:10Z → commit `de9a4b78` 03:17Z trọn S1) → giữa lệnh gọi và Cổng 1 làn V không có lượt người nào → không có điểm vũ trang nào trong vòng; Out of scope contract và design vẫn nói «lúc «làm» là skill in» — điểm D3 đã bỏ, và vòng 1 đã chứng nó kết ở câu hỏi đầu | Architectural: owner trả lời «sửa mục 2» kèm goal → goal kết ở câu hỏi kế (vô hại) nhưng máy theo chữ «câu cuối» không in lại → owner «yes» → đuôi S1 + S2→S4 chạy KHÔNG goal ở làn V; hoặc vòng từ Cổng Đáng 0 câu hỏi → cả vòng không goal. Ba dòng số 2.8.0 vẫn ghi «goal đã bật» vì SKILL có bước in → mở lớp 2 (hook Stop) oan, hoặc đổ cho giả định 2 «owner không dán» | Kê mọi câu xin duyệt của brainstorming 6.3.0 theo 3 path và đối chiếu với câu AC-4(a); E4 grep phải neo «MỌI câu xin duyệt thiết kế» (không phải «câu cuối») và neo câu khai ca 0-câu-hỏi; câu S1#5 phải nói brainstorm-approval CÒN là lượt người | proposed: (1) AC-4(a) + D3(a): «mỗi câu XIN DUYỆT thiết kế của brainstorm (bước Get approval / mỗi câu «looks right so far») in kèm dòng goal — câu cuối là câu người nói yes, máy không cần biết trước»; (2) S1#5 thêm nửa câu «câu xin duyệt của brainstorm vẫn là lượt người — chỉ 1A/1B của kit gộp»; (3) ca 0-câu-hỏi: hoặc (a′) «brainstorm không có gì hỏi → dòng goal đứng trong tin đầu S1, máy KHÔNG chờ» + Known limits «người đã rời máy thì không ai dán — thẻ Cổng Đáng là ô 528caaa8», hoặc khai thẳng ca này chưa phủ; (4) GUIDE «Khi nào» + câu «làn V T2 … lần duy nhất» chép cùng nghĩa; (5) bỏ vế «lúc «làm» là skill in» ở Out of scope hai file |
| P1 | contract AC-7 + evals E9 + Đường đo | Câu hỏi 5. Hình dạng vết ĐÚNG như AC-7 tả, đọc từ `feature-loop/workflows/acceptance-verify.js`: eval → `minted-<slug>-<evalId>-r<round>` (dòng 702); lệnh suite KHÔNG có eval → `minted-<slug>-SUITE-<tên>-r<round>` (dòng 679, thêm `__<6 hex>` khi trùng tên); MỌI dòng `ts` = `args.invokedAt` (một đồng hồ mỗi vòng, `s4-args.mjs` dòng 256; xưởng: cham-dung-cay-dung-cho-dung 3 vòng, mỗi vòng đúng 1 ts); dòng `kind: panel / baseline / vang-mat` KHÔNG có run_id; dòng `kind: repin` hình dạng khác. E9 lệch vật ở bốn chỗ: (1) FAIL-OPEN khi vắng: run-log của vòng chỉ được main loop append SAU khi Workflow trả về (SKILL S4#3 «(1) append result.runLog … (2) Write evidence-report»), nên lúc verifier chạy E9 ở vòng 1 file chưa tồn tại → «chua co run-log, exit 0» → PASS; vòng chấm TAY ở vòng 1 (đúng ca AC-7 canh) cho CÙNG màu → assertion âm-tính-một-mình; một vòng PASS = AC-7 chưa bao giờ đo mà bản chấm ghi PASS; (2) lệch thời điểm: Given «lúc trình Cổng 2» nhưng E9 chạy TRONG S4 → nhiều nhất đo vòng N−1; (3) regex hẹp hơn bên viết: `feature_loop.suite_keys` của config có `executors.script.product_map` mà hợp đồng này KHÔNG có eval nào cho nó → workflow CHẮC CHẮN đúc `minted-vu-trang-goal-luc-goi-ten-SUITE-node_scripts_product_map_mjs_root_check-r<n>` (hình dạng thật ở run-log nhanh-chinh-khong-ten-main, 13 dòng trong xưởng) → `E\w+` không khớp → ĐỎ GIẢ ở vòng 2; evalId cũng không buộc bắt đầu bằng E (xưởng có `J1`, `M1`); dòng không run_id phải bỏ qua, dòng repin phải bỏ qua; (4) script chưa tồn tại, chưa có cặp đo hai chiều | Vòng này PASS ở vòng 1 → AC-7 «PASS» với output «chua co run-log» → ba dòng số 2.8.0 chép «S4 qua Workflow: có vết» từ một eval chưa đo — đúng lớp «máy tự khai» AC-7 sinh ra để thay; hoặc vòng 2 REJECT vì dòng SUITE product_map — một vòng đốt vì phép đo, không vì vật | Script có ĐÚNG ba thông điệp ghim + mã thoát: «minted OK: <k> dong · 1 ts · round <n>» exit 0 · «run_id tay: <id>» hoặc «ts lech: <a> ≠ <b>» exit 1 · «chua co run-log (vong 1)» exit 0; cặp đo hai chiều trên fixture CODE-SINH: minted + 1 ts → xanh · id kiểu `lmcms-E3-r1` → đỏ ghim id · hai ts một vòng → đỏ ghim cặp · dòng SUITE được nhận · dòng không run_id bị bỏ qua · dòng repin bị bỏ qua · vắng file → thông điệp vòng 1 | proposed: (1) `expected` E9 ghim OUTPUT (E9 đã khai `evidence_required: output` — dùng nó), không chỉ exit; (2) hình dạng chấp nhận = `^minted-<slug>-(<id có trong evals.yaml> hoặc SUITE-<tên>(__hex)?)-r<round lớn nhất>$`; AC-7 «mọi dòng» → «mọi dòng CÓ run_id của vòng lớn nhất, bỏ kind repin»; (3) bước riêng của vòng này (ghi vào design «Thước», KHÔNG vào SKILL): sau khi append run-log và TRƯỚC Cổng 2, main loop chạy lại script trên log vừa ghi, dán output vào gói Cổng 2 — AC-7 được đọc từ vật kể cả khi chỉ một vòng; output không phải «minted OK» → AC-7 vào Known limits (mục này vốn đã không rỗng → không thêm lượt người); (4) ca `tests/scripts/run-log-minted.test.mjs` với cặp trên (runner tự nối `*.test.mjs`); (5) tuỳ chọn: wire `executors.script.run_log_minted` để cmd là `config:` ref như luật S1#4 — hook L2 đã nhận `.mjs` (evidence-core dòng 285), tiền lệ `node scripts/product-map.mjs --check` ×7, s4-args cho qua cmd không-config (dòng 133) → không chặn, chỉ cho đồng nhất |
| P2 | contract `feature:` + Context; opportunity `feature:`; design tiêu đề «Bốn quyết định»; Out of scope hai file | Dòng `feature:` của contract VÀ opportunity vẫn «điểm in dời về lúc «làm» · Cổng 1 · Gate 1.5» — chính điểm gap-probe vòng 1 bác và D3 đã thay; Context contract «(điểm in S0 · Gate 1 · Gate 1.5; …)»; design ghi «## Bốn quyết định thiết kế» mà có D1–D5. `gate-card.js` lấy `feature` làm tiêu đề thẻ (featurePlain) → tiêu đề thẻ Cổng 1 kể thiết kế đã bị bác | Người duyệt đọc tiêu đề + Context thấy «S0 / lúc làm», đọc AC-4 thấy «cuối brainstorm» → không biết bản nào là ý định; hồ sơ mốc 2.8.0 trích tiêu đề sai | 0 lần «lúc «làm»» hoặc «điểm in S0» trong contract.md + opportunity.md + design, trừ câu thuật lịch sử có chữ «đã bác» | proposed: sửa hai dòng `feature:` + Context thành «câu xin duyệt thiết kế của brainstorm · Cổng 1 · Gate 1.5»; tiêu đề «Năm quyết định»; vế «lúc «làm» là skill in» giải quyết theo P1 đầu bảng |
| P2 | contract (thiếu `## Notes`) + sổ 1005 / 1006 / 1007 | Sổ 1005 hứa «Known limits» cho ca máy-nhận-câu-xác-nhận, 1006 khai «AC-7 chỉ đo từ vòng 2», 1007 ghi «đóng 6 P2 bằng chữ» — nhưng contract vẫn không có `## Notes` (khuôn contract-template dòng 104; SKILL Gate 2: Known limits = bullet trong `## Notes`). Giới hạn đã khai nằm rải, không nhà: đo chỉ dẫn ×3 (AC-4/5/6) · giả định 1 chưa thử · AC-7 vòng 1 · trần 15 lượt nay phủ thêm S1 (vòng 1 mục 4, chưa có dòng nào) · T3 «vật phủ 0/2 lần dừng có vết» (vòng 1 P1) · ca brainstorm 0-câu-hỏi (P1 trên). Bản chấm S4 phải chép Known limits từ một chỗ — không có chỗ thì synthesize tự bịa hoặc để RỖNG | Report ghi Known limits rỗng → đủ sáu điều kiện xanh-sạch → máy đi tiếp qua Cổng 2 với bảy giới hạn chưa ai ký — xanh-sạch giả | `## Notes` có mặt với ≥7 dòng trên; bản chấm S4 chép nguyên văn; bộ đọc «Known limits hiện-diện-và-rỗng» của xanh-sạch tự dừng đúng ở Cổng 2 | proposed: thêm `## Notes` gom 7 dòng; ghi rõ vòng này KHÔNG xanh-sạch theo thiết kế → Cổng 2 là lượt người (tổng vòng: Đáng 1 · Phạm vi làn V 0 · Bằng chứng 1 = 2 ≤ 3) |
| P2 | design D5 + GUIDE câu «làn V T2: lượt cuối brainstorm là lần duy nhất» (AC-5) + Gate 1 khối «Hình tại điểm quyết định» bước [3] | Câu hỏi 2, kê từ chữ SKILL mọi lượt giữa câu xin duyệt và Cổng 1: S1#2 Explore fan-out «trước khi đề xuất approach» → TRƯỚC câu xin duyệt, Workflow trả về trong lượt → 0; S1#3 morphological-scan «KHÔNG dừng phỏng vấn», `[GIẢ ĐỊNH]` gom về Coverage «không thêm lượt gọi nào» → 0; S1#4 ui_standards_skill + Đặc tả UX «máy điền MỘT lượt, không gọi owner» → 0; S1#7 sau D5 → 0. NHƯNG (a) S1-D design-pass (MỌI feature chạm UI): «Mặc định là KHÔNG ĐỒNG BỘ: … gửi gói rồi đi làm việc khác» + design-pass §4 bước 3 «Chờ owner phản ứng bằng lời — lúc rảnh» = một chỗ «chờ input người» mà khuôn goal coi là HOÀN THÀNH → goal kết trước gap-probe/thẻ; (b) khối Hình bước [3] «dispatch subagent tươi (Agent tool …)» không có chữ chế độ — cùng lớp «nền báo xong» D5 vừa đóng ở S1#7; chỉ chạy khi cổng thật sự dừng (T3 hoặc T2 không-V) nên không phá D5 ở làn V, nhưng D4 lời phải gánh. D5 và GUIDE tuyên «không còn lượt trở về nào» chỉ đúng cho T2 KHÔNG chạm UI (vòng này: cli) — chưa khai điều kiện | T2 làn V chạm UI: owner duyệt thiết kế + dán goal → S1-D gửi gói, «chờ phản ứng» → goal kết → gap-probe, thẻ, S2→S4 không goal; hoặc checker đọc «mơ hồ» và máy tự chấm thẩm mỹ thay owner (design-pass cấm «tôi thấy đẹp rồi») | Câu GUIDE có điều kiện «T2 không chạm UI»; AC-4(b) liệt kê CẢ HAI điểm dispatch Agent của SKILL (S1#7 và Hình [3]) với chữ «chờ trong lượt» | proposed: AC-5 thêm «(T2 không chạm UI; chạm UI: gói design-pass là lượt người kế — dán lại ở đó)»; AC-4(b) thêm bước [3] cùng câu (một file, một lớp) hoặc ghi Known limits |
| P2 | contract AC-1 + evals E1 | Câu hỏi 3 (AC-1): «nội dung template literal» chưa định nghĩa cách rút. Nếu P85 rút bằng regex rồi bỏ thụt lề từng dòng (để literal viết đẹp trong hàm) thì AC-1 xanh với bản JS có 2 dấu cách đầu các dòng 2–6 — giá trị runtime khác SKILL, chỉ AC-2 mới đỏ (hai AC cùng đỏ vì một lỗi, nhưng AC-1 khai «bằng nhau từng ký tự» đã sai). «Gọi tên đúng bản lệch với hai bản còn lại» chưa nói ca cả ba khác nhau đôi một. Chiều đỏ P85 hiện `skill_t.replace("sau 15 turns", …, 1)` trên CẢ FILE — với gate-card.js, chuỗi mồi xuất hiện trong chú thích trước khối thì đột biến trượt khối (assert `block(mutated) != others` đỏ lúc viết ca — hướng an toàn, nhưng là ca chết cho tới khi ai đó nhìn). Không lỗ chuẩn-hoá-trong-dòng: chữ AC đã là «từng ký tự sau strip» — chỉ cần cấm trim từng dòng | Bản JS thụt lề → E1 xanh, thẻ in goal_line có dấu cách đôi → owner dán, checker vẫn hiểu (vô hại) nhưng hợp đồng khai «từng ký tự» đã sai — thước tự dối ở đúng chỗ vòng 1 vá | Payload JS = văn bản NGUYÊN giữa dấu «`» mở + xuống dòng và dấu «`» đóng, KHÔNG trim từng dòng; ba khối sau strip() bằng nhau; đột biến thực hiện trên payload đã rút rồi ghép lại vào nguồn (không replace trên cả file); ca ba-khác-nhau-đôi-một → thông điệp nêu cả ba | proposed: viết hai câu đó vào AC-1 và `expected` E1 (một dòng mỗi chỗ) |

## Đã kiểm (ngoài các dòng trên)

- **Câu 1 — chốt:** brainstorm 6.3.0 CÓ câu xin duyệt (dòng 83 / 91 / 185), nên
  D3(a) tồn tại; thứ thiếu là chữ ghim (chỉ dòng 224 có, và thuộc bước đã thay),
  vòng revise của architectural, câu S1#5 «gộp thành MỘT Gate 1», và ca 0 câu
  hỏi. Xếp P1, không P0: AC vẫn đúng chiều, thẻ Cổng 1 không duyệt sai; hệ quả
  rơi vào ba dòng số 2.8.0 và giả định 1.
- **Câu 2 — ngoài S1:** `superpowers:executing-plans` có lượt người theo thiết
  kế («If concerns: Raise them with your human partner before starting» · «STOP
  executing immediately when…» · «Ask for clarification rather than guessing»);
  `subagent-driven-development` thì «Do not pause to check in … Four things
  stop you». S3 đi đường nào quyết định goal (b)/(c) phải sống qua bao nhiêu chỗ
  «chờ input người» — phép thử rẻ của giả định 1 nên chạy với S3 theo
  subagent-driven (đường #136 đã đi). Không phải lỗ hợp đồng; ghi cho S3.
- **Câu 3 — AC-2 đủ:** kỳ vọng dựng độc lập + đẳng thức + 0 `<slug>` sót + một
  dòng; mối «bên đọc chép bên viết» (test rút khuôn từ chính gate-card.js) đóng
  bằng chuỗi AC-1 (SKILL ⇔ JS) → AC-2 (JS ⇒ goal_line). Slug fixture không được
  là chuỗi con của khuôn (lmcms dùng `g` — được).
- **Câu 4 — «ngay sau» đo bằng gì:** gate-card.js 648 đẩy CẢ khối trong MỘT
  chuỗi `P.push`: `<div class="lab">👉 VIỆC CỦA ANH</div><div class="grp gdo">…</div><div class="mach">Dòng lệnh …: <b>esc(one_shot)</b></div>`,
  rồi 649 `<div class="foot">…`; `P.join('\n')`. Dòng lệnh đứng NGOÀI `.grp gdo`
  → «cùng khối .grp gdo» là mỏ neo SAI. Cách đo đúng = KỀ NHAU trong chuỗi HTML:
  định vị `.mach` bằng `<b>` == `esc(one_shot)` lấy từ `--extract` (round-trip,
  không neo chữ «Dòng lệnh»), rồi đòi `</b></div>` theo sau chỉ bởi `\s*` rồi
  `<div class="mach goal">` — chèn bất kỳ phần tử nào giữa → đỏ; đổi class → đỏ;
  nối đuôi `<b>` → GL02 đỏ. Ràng buộc từ lưới đang sống mà S3 phải biết: P185
  đếm `<p class="li">` trong `html.slice(iYm, iFoot)` và đòi ĐÚNG MỘT mục
  không-phải-mẫu → phần tử goal KHÔNG được dùng `<p class="li">`; P185 canh
  «hứa phút» bằng `/[0-9~]\s*phút|…/` — khuôn có «15 turns», không «phút» ✓;
  các regex `Dòng lệnh[^<]*<b>` (lmcms 230/336, run-tests 8912/9511) lấy khớp
  ĐẦU → phần tử goal đứng sau nên vô hại; P150 so byte-identical với bản base
  trên thẻ CỔNG 2 (fixture `status: implemented`) → đổi Cổng 1 không chạm nó,
  và GL04 giữ Cổng 2 nguyên vẹn ✓.
- **Câu 5 — vật:** `tests/scripts/run-log-minted.mjs` chưa tồn tại (đúng khai:
  S3 viết). Một `ts` mỗi vòng là bất biến DO MÃ (mọi dòng `ts: invokedAt`) —
  đo bằng «tập ts của vòng lớn nhất có đúng 1 phần tử». L2 nhận verifier
  `.mjs` (evidence-core dòng 285 regex `(py|mjs|js|sh)`).
- **Câu 6 — timestamp & mệnh đề:** `at` 1005–1008 = 03:35–03:38Z, id cùng phút;
  commit `db973ee9` = 03:40:16Z → mọi `at`/id TRƯỚC commit ✓; gap-probe vòng 1
  `at` 03:31:18Z < commit ✓; `decided_at` 03:10Z < `de9a4b78` 03:17:49Z ✓. Không
  kiểm được từ repo (như vòng 1): «owner gọi tên 03/09 sau hình v28-01» — vết là
  frontmatter máy điền. Các số ở «Vấn đề» nay đều có vết (findings 02/09, entry
  3008/4002/6002, hợp đồng 2.7.0 dòng 76).
- **Câu 7 — 0 P0.** Điều kiện «gap-probe còn P0 → Gate 1 dừng chờ người» của
  SKILL không còn bị kích; hai P1 là thước và chữ, đều sửa được trong lượt.
- **Hiện trạng vật mà AC-4/5/6 sẽ đổi (để E4/E5/E6 có đối chứng dương trước
  khi sửa):** SKILL 10 câu bất biến dừng chưa có vế «tiến trình nền báo xong»;
  S1#7 hiện «dispatch 1 subagent fresh (Agent tool; …)» — không chữ chế độ; SKILL
  143 «**T3: GATE 1.5** — trình tóm tắt plan … chờ duyệt» chưa có «kèm dòng
  /goal»; GUIDE 303 «Khi nào: ngay sau khi bạn duyệt Gate 1». Khuôn hai bản
  SKILL 110–119 = GUIDE 314–323 nguyên vẹn, 6 dòng.
- **Chưa làm ở phiên này:** không chạy bốn suite; không chạy `gate-card
  --extract`. Sau khi file này tồn tại, thẻ Cổng 1 hiện 0 P0 · 2 P1 · 4 P2 và
  `roi_bac.on: false`; máy tự đọc P1 trước khi tin thẻ (sổ 1008).

## Tóm tắt

1. Verdict `findings` — **P0 0 · P1 2 · P2 4**. Không còn P0 → làn V T2 không bị chặn bởi gap-probe.
2. 11 finding vòng 1: ĐÓNG 8 · ĐÓNG MỘT NỬA 3 (#1 điểm vũ trang · #3 dòng số T3 · #11 `## Notes`) · CHƯA ĐÓNG 0.
3. SAI: «làn V T2: lượt cuối brainstorm là lần duy nhất» — đúng chỉ khi T2 không chạm UI VÀ brainstorm có câu xin duyệt; chữ đúng: «mỗi câu xin duyệt thiết kế của brainstorm in kèm dòng goal, câu cuối là câu người nói yes; T2 không chạm UI; brainstorm không hỏi gì → ca chưa phủ (thẻ Cổng Đáng 528caaa8)».
4. SAI: tiêu đề `feature:` hai file «điểm in dời về lúc «làm»» + Context «điểm in S0» + Out of scope «lúc «làm» là skill in» — chữ đúng: «câu xin duyệt thiết kế của brainstorm · Cổng 1 · Gate 1.5».
5. SAI theo nghĩa đo: E9 «vòng đầu chưa có run-log → exit 0» là fail-open trùng màu với ca nó canh; regex `E\w+` không nhận dòng SUITE product_map mà workflow chắc chắn đúc cho hồ sơ này → đỏ giả vòng 2.
6. Thiếu: `## Notes` chưa có dù sổ 1007 khai đóng — 7 giới hạn không nhà → xanh-sạch giả nếu bản chấm để rỗng.
7. Đúng: sổ 1005–1008 `at` và id ≤ commit 03:40:16Z; hình dạng run_id + một ts/vòng đúng như AC-7 tả; mỏ neo `.mach.goal` đo được bằng kề-nhau trong chuỗi HTML (không phải cùng `.grp gdo`); AC-2 đủ.
