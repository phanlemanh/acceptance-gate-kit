---
slug: vu-trang-goal-luc-goi-ten
at: 2026-09-03T03:31:18Z
verdict: findings
p0: 1
p1: 4
p2: 6
by: phiên tươi độc lập (subagent context sạch, không chia sẻ context với phiên dựng hồ sơ; đọc 4 artifact + opportunity + vật thật SKILL/GUIDE/gate-card/P85/lmcms; KHÔNG chạy suite)
---

## Findings

| Sev | Artifact | Thiếu gì | Kịch bản fail | Thước đo | Xử lý |
|---|---|---|---|---|---|
| P0 | design D3(a) + contract Phạm vi 2 câu «làn V T2 chỉ cần lần đầu» + AC-4 + opportunity giả định 1 | Tiền đề «S0 → S1 là một đoạn máy» SAI theo chính SKILL: S1#1 là `superpowers:brainstorming — hỏi đáp làm rõ như thường lệ` (SKILL dòng 90), S0#2 hỏi «XÁC NHẬN kết luận T1», S0#3 «BẮT đổi slug mới» — đều là lượt chờ người NẰM GIỮA S0 và tiến trình nền gap-probe (S1#7). Khuôn goal đọc nguyên văn: «Loop đã escalate cho user (REJECT quá 3 round / BLOCKED / chờ input người) cũng coi là HOÀN THÀNH — để dừng» → goal dán ở S0 KẾT ngay câu hỏi brainstorm đầu tiên (chính D3(b) thừa nhận: «goal kết ở mỗi cổng»), trước khi đoạn máy nó phải đỡ bắt đầu. Chiều ngược cũng xấu: vế «mơ hồ = CHƯA hoàn thành» có thể làm checker nhắc «tiếp» khi máy đang chờ câu trả lời brainstorm → máy tự trả lời câu hỏi của chính nó. AC-4 chỉ grep chữ «TRƯỚC khi S1» nên xanh trong cả hai kịch bản. Danh sách điểm vũ trang lại của D3 (S0 · Cổng 1 · Gate 1.5) thiếu đúng lớp «cổng giữa» phổ biến nhất | Vòng T2 từ mô tả thô (không opportunity): owner dán goal ở S0 → Q1 brainstorm → goal kết → owner trả lời hết → máy sinh artifact, dispatch gap-probe nền → agent báo xong, máy «báo cáo rồi ngừng nói» — y nguyên lỗi 02/09 — mà ba dòng số 2.8.0 đếm vòng này là «goal đã bật» ⇒ ngưỡng «>0 → mở lớp 2 hook Stop» nổ oan, hoặc bị gán cho giả định 2 «owner không dán» | Mọi điểm vũ trang phải nằm SAU lượt người cuối cùng trước đoạn máy; kiểm bằng cách kê mọi chỗ SKILL kết lượt chờ người giữa S0 và Gate 1 (máy kê được từ văn bản: S0#2 · S0#3 · S1#1 · S1-D) và đối chiếu với danh sách của D3 | proposed: (1) sửa Phạm vi 2 + D3(a): S0 in goal chỉ phủ đường KHÔNG hỏi (vòng từ Cổng Đáng, opportunity đã grill) — nói thẳng, bỏ câu «làn V T2 chỉ cần lần đầu»; (2) đóng lớp dừng của S1 bằng cấu trúc thay vì goal: S1#7 dispatch gap-probe ĐỒNG BỘ (`run_in_background: false` — bước kế phụ thuộc kết quả, đúng khế ước của Agent tool) → S1 không còn chỗ «nền báo xong rồi ngừng nói»; AC-4 thêm vế grep cụm đó; (3) giả định 1 (goal gặp cổng người lần đầu trong lịch sử kit) có phép thử rẻ đã gọi tên (phiên con `claude -p`) mà không AC, không task, không dòng Known limits nào giữ — hoặc thành một task S3 ghi transcript vào hồ sơ, hoặc thành dòng Known limits ngay bây giờ; (4) trần «15 turns» được cỡ cho S2→S4, nay phủ thêm S1 — ghi vào cùng dòng Known limits |
| P1 | contract AC-1 «khớp TỪNG BYTE» + evals E1 | «Byte» chưa định nghĩa cho bản JS. Hai bản md là khối trong rào ``` sau marker HTML-comment; P85 hiện rút bằng regex rồi `.strip()` — tức «từng ký tự» của P85 ĐÃ là «bằng nhau sau strip» (hôm nay: raw bằng nhau, 6 dòng, 431 byte UTF-8 / 377 ký tự, không dấu cách thừa, không backtick, không `${`, có `<`/`>` ở `<slug>` ×2). Bản JS có thể là template literal nguyên văn, chuỗi nối `'…' + '…'`, mảng dòng `.join('\n')`, hay chuỗi có `\n` — ba dạng sau «bằng» về giá trị nhưng không bằng về byte nguồn; một phép so «đã chuẩn hoá» (bỏ dấu cách, gộp dòng) vẫn khai được «từng byte». Marker JS (`// <<<GOAL-TEMPLATE` … `// GOAL-TEMPLATE>>>`, khuôn blockOf của lmcms) cũng khác cú pháp marker md, nên «rút qua marker từ ba file» thật ra là hai regex | Ai đó viết hằng JS bằng chuỗi có `\n` hoặc thụt lề 2 dấu cách, test so giá trị đã gộp dòng → xanh, hợp đồng khai «từng byte» → thẻ 2.8.0 tin ba bản là một; sau đó một bản sửa chữ «— để dừng» thành «- để dừng» ở JS, phép so chuẩn hoá vẫn xanh | Định nghĩa đo được, đề xuất: bản JS = template literal nguyên văn nằm trong khối marker JS; payload = chuỗi ký tự NGUỒN giữa «`\n» mở và «`» đóng, không thụt lề, không escape; ba payload sau `.strip()` phải bằng nhau theo byte UTF-8 (ghim số dòng = 6 và số byte đọc từ bản SKILL, không gõ literal); «gọi tên bản lệch» = bản khác hai bản còn lại; đột biến một ký tự lần lượt trên BA bản trong bộ nhớ, mỗi lần thông điệp ghim đúng tên file. AC-2 là mối nối giá trị↔nguồn (goal_line == payload gộp dòng) nên bản JS viết bằng escape sẽ đỏ ở đó — ghi rõ điều này để hai AC không bị coi là trùng | proposed: viết định nghĩa trên vào AC-1 (một câu) và vào `expected` của E1 |
| P1 | design D3(c) + contract AC-4 vế «Gate 1.5 có bước in» + Coverage trục B | Gate 1.5 không có mục riêng: nó là một mệnh đề trong S2#3 (SKILL dòng 143 «**T3: GATE 1.5** — trình tóm tắt plan … chờ duyệt»), không thẻ, không lệnh, không trường (audit 22/08 A6; approve.md:19 từ chối ghi). Nên với T3 — đúng hạng vòng đã đo (#136) — điểm vũ trang cuối trước S3→S4 chỉ có LỚP LỜI (D3(c) + D4), và mọi lượt dừng có vết của #136 đều nằm SAU Gate 1.5 (findings 02/09: Task dừng khi còn Task 5→9 + S4). Goal dán ở Cổng 1 kết ngay tại Gate 1.5 theo khuôn. Tức lớp VẬT (D1/D2) phủ 0 trong số các lần dừng có vết; phần phủ được là lớp đã trượt 02/09 | Vòng T3 kế: owner dán goal từ thẻ Cổng 1 → Gate 1.5 hỏi → goal kết → phiên quên in lại (lời dặn) → S3 dừng như cũ; ba dòng số 2.8.0 ghi «goal đã bật» vì thẻ có in — số 0 mục tiêu mất nghĩa cho T3 | Mỗi điểm vũ trang phải khai LỚP của nó (vật hay lời) và số lần dừng có vết mà lớp đó phủ | proposed: ghi vào Known limits một dòng số: «T3: vật phủ 0/2 lần dừng có vết; Gate 1.5 chỉ lớp lời»; nếu muốn một tay vịn rẻ hơn hook: khuôn `PLAN-SUMMARY-TABLE-TEMPLATE` (human-facing-language.md dòng 65) thêm ô GOAL — vẫn lớp lời nhưng nằm trong khuôn máy chép, và là chỗ ba dòng số 2.8.0 đọc «có in hay không» |
| P1 | contract Đường đo câu «Chính vòng này: S4 qua Workflow acceptance-verify, không agent tay» + ledger 1001 | Không AC nào đo; không trường nào tự xưng: `verified_by` là chữ tự do (evidence-core.cjs 249–250 «attribution only, không phải verifier»). Nhưng dấu vết máy-giữ ĐÃ CÓ SẴN, 0 chi phí dựng: workflow đúc run_id `minted-<slug>-<evalId>-r<round>` (acceptance-verify.js 702, suite kit không in run_id nên luôn rơi vào nhánh đúc) và mọi dòng run-log một vòng chung một `ts` = invokedAt; hai vòng trước cho hình dạng NGƯỢC LẠI — #136 `lmcms-E3-r1` (viết tắt tay), 2.7.0 `release-2-7-0-E1-r1` với ts lệch nhau 16:59:45 / 17:04:09 — và cả hai `verified_by: implementing session`. Tức lời khai «#136 làm trái» đúng, và «vòng này đi đúng» có thể ĐỌC từ vật thay vì tự khai | Vòng này lại chạy S4 tay (áp lực giờ, phiên dừng), evidence xanh, hợp đồng vẫn hứa «qua Workflow»; ba dòng số 2.8.0 chép lời tự khai — đúng lớp «máy tự khai» kit đi bắt | Đường đo phải trỏ tới một vật: run-log của hồ sơ này có mọi run_id khớp `^minted-vu-trang-goal-luc-goi-ten-E\d+-r\d+$` và một ts mỗi vòng; `verified_by` không phải «implementing session». Lý do MỚI so với 1001: không CỘNG luật, không hook (Out of scope giữ nguyên) — chỉ đọc hình dạng có sẵn | proposed: thêm AC-7 (executor script, đọc run-log + frontmatter) hoặc tối thiểu ghi hình dạng vết đó vào Known limits để mốc 2.8.0 đọc vật, không đọc lời |
| P1 | opportunity «Vấn đề» câu «≥4 trong số đó là máy giao tiến trình nền rồi … ngừng nói (S3 ×1, S4 ×2, một lần khai «đang chạy» sai)» + design D3(a) «S1 … chính ca gây dừng» | Phép tách S3 ×1 / S4 ×2 không có nguồn trong repo. Vết có thật: findings 02/09 trích owner «hai lần trong cùng phiên», cả hai lúc còn «Task 5→9 + S4» — tức HAI lần, đều ở S3; entry 3008 (stage S3, 02:31Z) là vết của lần đầu. «Khai đang chạy sai» là CÙNG sự kiện với lần dừng đầu («Lần đầu Task dừng, tôi hỏi bạn có đang chạy không thì bạn bảo có») — đếm riêng là đếm đôi. Số ≥4 kế thừa từ hợp đồng 2.7.0 (2 dừng + 1 «rớt mạng resume» + 1 khai sai), mà «rớt mạng» không thuộc lớp `/goal` đỡ được (phiên chết thì goal chết); gap-probe 2.7.0 (P1) đã ghi chỉ ≥2 có vết. S4 ×2: không vết nào — hai lần dừng ở S4 có vết là dừng-vá r2/r3 (entry 4002/6002), tức PHANH thiết kế, chính hình v28-01 cũng tách chúng khỏi «Task dừng». «S1 gap-probe là chính ca gây dừng»: không vết nào ở S1 của #136 | Mốc 2.8.0 so «0» với «≥4» và tuyên goal đỡ được 4 lượt/vòng, trong khi số có vết thuộc lớp goal đỡ được là 2 (cả hai S3); tệ hơn, chỗ vũ trang được thiết kế theo ca S1 «chính ca gây dừng» không có vết | Mỗi số trong Vấn đề trỏ một vết (entry, findings, commit) và khai lớp (goal đỡ được hay không) | proposed: sửa thành «≥2 có vết, cả hai ở S3 (findings 02/09, entry 3008), cùng sự kiện với lần khai «đang chạy» sai; rớt mạng 1 lần — ngoài lớp goal; S4 chưa có vết dừng ngoài thiết kế»; bỏ vế «chính ca gây dừng» ở D3(a) hoặc đổi thành «ca CÓ THỂ dừng» |
| P2 | contract AC-2 + evals E2 «GL01» | Khuôn có `<slug>` HAI lần (`Feature <slug>` và `_acceptance/<slug>/contract.md`); AC-2 viết «thay `<slug>` bằng slug thật» không nói «mọi lần». `String.replace('<slug>', s)` chỉ thay lần đầu; nếu test dựng kỳ vọng bằng cùng một `.replace` thì hai bên cùng lỗi → xanh (thước tự dối kiểu «bên đọc chép bên viết») | Thẻ in goal còn `_acceptance/<slug>/contract.md` nguyên chữ; owner dán, checker không bao giờ thấy status verified của file thật → goal chạy tới trần 15 lượt | Kỳ vọng dựng bằng replaceAll VÀ hai assert độc lập: goal_line không chứa `<slug>`; số lần xuất hiện slug trong goal_line = số lần `<slug>` trong khối rút qua marker (đếm, không gõ 2) | proposed: thêm hai vế đó vào AC-2 và `expected` E2 |
| P2 | design D2 + contract AC-3 vế «nằm ngay dưới dòng lệnh duyệt» + câu thẻ «Sau khi duyệt, dán dòng này» | «Ngay dưới» chưa có nghĩa đo được trong HTML (dòng lệnh là `<div class="mach">…<b>one_shot</b></div>`, gate-card.js 648); GL02 phải tìm dòng goal bằng một mỏ neo — chưa khai. Câu thẻ nói «sau khi duyệt» trong khi D2 in cả trên thẻ đỏ (người trả lời «sửa») và AC-3 đối chứng thẻ đỏ. Về làn V: SKILL 104 render thẻ ở mọi Cổng 1, SKILL 10 làn V đi tiếp không hỏi, entry 7002 của 2.7.0 tự khai «owner không đọc phạm vi mốc trước khi máy chạy» — thẻ Cổng 1 làn V không ai đọc lúc đó, nên dòng goal trên thẻ làn V là TAY VỊN khi phiên dừng (thẻ là vật mặt người cuối trước đoạn máy), không phải hành động cổng; vật phục vụ làn V thật sự là điểm in S0 (xem P0). AC-3 không sai — «in ở MỌI thẻ» là đúng vì thẻ đủ rẻ — nhưng hợp đồng chưa nói vật nào phục vụ làn nào | GL02 neo vào một chuỗi chú thích rồi ai đó đổi chú thích → ca chết im; owner đọc thẻ đỏ thấy «sau khi duyệt» và không dán vì mình đang «sửa» | Đẳng thức có mỏ neo: phần tử goal đứng NGAY SAU div `.mach` (regex kề nhau, không phép chứa); câu thẻ trung tính với câu trả lời | proposed: AC-3 định nghĩa «ngay dưới» = phần tử kề sau `.mach`; câu thẻ đổi thành «Sau khi trả lời, dán dòng này để đoạn máy chạy tới cổng kế»; Context thêm một câu «thẻ phục vụ T3 + T2 bị chặn; làn V phục vụ bởi điểm in S0/brainstorm; trên thẻ làn V dòng goal là tay vịn» |
| P2 | SKILL dòng 121 · GUIDE dòng 312 · tests/plugins/run-tests.sh tiêu đề P85 · feature-loop manifest (mô tả v1.x) — hai nguồn cho một luật sau khi sửa | Câu hỏi 6 đã quét: commands/*.md, skills/acceptance/SKILL.md, workflows, README feature-loop, CONTEXT.md — KHÔNG nguồn nào khác nói thời điểm in goal; bảng COMMAND-NAMES chỉ là bảng tên. Còn lại bốn câu sẽ lỗi thời ngay khi có bản chép thứ ba và điểm in S0: SKILL 121 «hai bản được test giữ khớp từng ký tự», GUIDE 312 «bản runtime nằm ngay trong SKILL mục Gate 1, hai bản được test P85 giữ khớp», tiêu đề P85 «SKILL la nguon runtime, GUIDE la ban nguoi doc», manifest feature-loop «Gate 1 prints the /goal suggestion from the GOAL-TEMPLATE block embedded in the SKILL itself (drift-pinned to GUIDE)». Không AC nào phủ (AC-5 chỉ ghim đoạn «Khi nào» của GUIDE) | Người bảo trì đọc «hai bản» rồi sửa hai, bỏ sót bản JS; hoặc đọc manifest tin bản runtime chỉ ở Gate 1 | Mỗi câu nhắc SỐ bản chép hoặc VỊ TRÍ điểm in phải nằm trong ca P85 nới (grep neo chữ, đúng lớp AC-4/5 đã khai) | proposed: AC-5 thêm vế «SKILL 121 + GUIDE 312 nói BA bản»; sửa tiêu đề P85; manifest ghi ở mục v2.8.0 khi phát hành (mốc phát hành, không phải vòng này) |
| P2 | decisions.jsonl (4 entry) + opportunity `decided_at` — lớp lặp 3 mốc | Kiểm câu 7: commit chứa sổ `de9a4b78` = 2026-09-03T03:17:49Z; `at` cả 4 entry = 03:12:00Z, `decided_at` = 03:10:00Z → ĐỀU TRƯỚC commit, luật cứng giữ được — lần đầu sau ba mốc. Nhưng bốn `at` là số tròn, giống hệt nhau (hình dạng cũ), và id của cả 4 mã hoá `20260903T032500Z` = 03:25:00Z — SAU commit 7 phút và sau chính `at` của nó 13 phút (id là nhãn gõ tay; nếp #136/2.7.0 cũng id ≠ at). Phép đo «at ≤ thời điểm commit» mà gap-probe 2.7.0 đề nghị thay cho lần dặn thứ tư vẫn chưa tồn tại — vòng này chạm tests nên là chỗ rẻ nhất để nó ra đời, nhưng ngoài phạm vi đã khai | Bộ đối chiếu sổ↔git đọc id thấy quyết định «lúc 03:25» được commit lúc 03:17; lần thứ tư dặn-bằng-lời | id rút từ `at` thật (hoặc ngược lại), một đồng hồ; ứng viên phép đo cho cửa sổ kế giữ nguyên tên | proposed: đổi 4 id sang mốc ≤ at (rẻ, chưa ký); không mở thêm phạm vi cho phép đo — ghi sổ là tái phát lần 4 ở dạng nhẹ (id) |
| P2 | opportunity § Nguồn + contract Out of scope — dòng dõi Ngoài-5 | Ý «thẻ render khối /goal» đã có vết chọn của owner từ 02/09: entry `d-20260902T203500Z-ngoai5` của #136 «khối Ngưỡng không lột markdown + thẻ chưa render khối /goal sau duyệt: người ký chốt «mở hợp đồng mới»» — hai mục gộp một quyết. Vòng này lấy nửa sau, không nhắc Ngoài-5, và nửa đầu («khối Ngưỡng không lột markdown») không còn tên ở đâu ngoài hợp đồng 2.7.0 dòng 130 «chờ owner gọi tên». Đã kiểm «vòng meta duy nhất cửa sổ 2.7→2.8»: đúng — không hồ sơ mở nào khác ngoài `vu-trang-goal-luc-goi-ten` | Mục «Ngưỡng không lột markdown» rơi khỏi sổ vì ai cũng tưởng Ngoài-5 đã được mở trọn | Mỗi ô mở phải trỏ entry gốc và nói phần còn lại của entry đó đi đâu | proposed: Nguồn thêm «Ngoài-5 của #136 (nửa sau)»; Out of scope thêm một dòng «khối Ngưỡng không lột markdown — nửa đầu Ngoài-5, vẫn chờ gọi tên» |
| P2 | contract AC-4 chữ «mục Gate 1.5», thứ tự bước S0, và chỗ để Known limits | (a) Không có «mục Gate 1.5» — chỉ có mệnh đề trong S2#3; grep của E4 phải neo vào câu đó. (b) Bước in ở S0 phải đứng SAU S0#3 (slug mới có để thay `<slug>`) và SAU nhánh thoát T1 ở S0#2 (T1 không có đoạn máy) — AC-4 grep «TRƯỚC khi S1» không ghim thứ tự này; bước in đặt ở S0#1 vẫn xanh. (c) Hợp đồng không có `## Notes` — bốn giới hạn đã khai (đo chỉ dẫn ×3, giả định 1 chưa thử) nằm rải trong chữ AC/Coverage; thêm hai giới hạn từ P1 (Gate 1.5 lớp lời, vết workflow) thì cần một chỗ để bản chấm S4 chép sang Known limits | E4 xanh với bước in đặt trước khi có slug; Known limits của bản chấm thiếu dòng vì hợp đồng không có chỗ gom | AC-4: «bước S0 sau bước xác định slug và sau nhánh thoát T1»; E4 grep neo «GATE 1.5» của S2#3 | proposed: sửa chữ AC-4; thêm `## Notes` với các dòng Known limits gom lại |

## Đã kiểm (ngoài các dòng trên)

- **Câu hỏi 1 — khuôn đọc nguyên văn** (SKILL 110–119 = GUIDE 314–323, python so raw:
  bằng nhau, 6 dòng, 431 byte UTF-8): «Loop đã escalate cho user (REJECT quá 3
  round / BLOCKED / chờ input người) cũng coi là HOÀN THÀNH — để dừng». Goal vũ
  trang lúc «làm» → ở Cổng 1 không-làn-V kết (đúng như D3(b)); ở làn V T2 không
  còn lượt chờ người sau câu brainstorm cuối nên chạy tới `verified` rồi tự tắt
  (GUIDE 336) — KHÔNG vế nào của khuôn làm goal kết sớm ở làn V, và không spin ở
  Cổng 2 vì đích là `verified`, trước Cổng 2. Rủi ro spin duy nhất là ở cổng
  người không-làn-V qua vế «mơ hồ = CHƯA hoàn thành» — chưa từng được thử (giả
  định 1). Không cần đổi chữ khuôn: giữ như AC-5; điều cần đổi là danh sách điểm
  vũ trang (P0).
- **Câu hỏi 4 — Gate 1.5 sống ở SKILL S2#3** (dòng 143), không ở commands/
  (approve.md:19 «Plan approval — Gate 1.5 — lives in the feature-loop»), không
  ở skill acceptance; có chỗ để thêm mệnh đề in, nhưng chỉ là lời (P1).
- **AC ↔ eval đủ**: AC-1→E1(+E7/E8 hồi quy) · AC-2→E2 · AC-3→E3 · AC-4→E4 ·
  AC-5→E5 · AC-6→E6; sáu khoá `config:executors.test.{plugins,scripts,hooks,workflows}`
  đều có trong `_acceptance/config.yaml` 30–34; `tests/scripts/run-tests.sh`
  2507 tự chạy mọi `*.test.mjs` nên «file ca mới» được nối tự động (kèm răng
  «*.test.mjs đỏ vẫn PASS» đã đóng 13/08).
- **Hạng T2 đúng**: t3_paths = hooks/** · lib/** · pre-merge-check.sh ·
  recheck-evidence.cjs (config 249–254); gate-card.js, SKILL, GUIDE, tests không
  chạm.
- **Vật thẻ hiện có đúng như hợp đồng tả**: ONE-SHOT-CMD marker (gate-card.js
  91–96), `oneShotG1` + `--extract` Cổng 1 (563–564), khối VIỆC CỦA ANH (648),
  `esc()` (270) — khuôn goal có `<`/`>` ở `<slug>` nên nếu thay slug hụt, HTML sẽ
  là `&lt;slug&gt;` ≠ extract: đẳng thức GL02 đỏ đúng chiều. lmcms có `blockOf`
  (19) + `pickIn` (24) để rút qua marker JS; LM15 (329) là khuôn đẳng thức HTML↔extract
  mà GL02 nên chép.
- **Số ở Vấn đề khớp nguồn**: «≥10 = 4 + ≥6» = hợp đồng 2.7.0 dòng 76; «Hồ sơ
  mốc 2.7.0 gọi tên chỗ cắt» = dòng 95–101; «vũ trang sau Cổng Phạm vi» = SKILL 108
  + GUIDE 303 «Khi nào: ngay sau khi bạn duyệt Gate 1»; «7/14» của findings 26/07
  = 7 đúng thiết kế + 7 tự chèn (dòng 16–17). Chỉ phép tách ≥4 → S3/S4 là không
  có vết (P1).
- **Không kiểm được từ repo, khai thẳng**: «owner gọi tên 03/09 sau khi đọc hình
  v28-01» — vết duy nhất là frontmatter máy điền (`decided_at` «±5 phút») và hình
  cùng commit; vết gần nhất có chữ owner là Ngoài-5 (02/09). Đây là nếp «máy dịch ý
  muốn thành trường hồ sơ» luật (c) cho phép — ghi để không ai tưởng là phép đo.
- **Chưa làm ở phiên này**: không chạy bốn suite; không chạy `gate-card --extract`
  trên hồ sơ (P85 và LM đã đọc mã, không chạy). Sau khi file này tồn tại, thẻ
  Cổng 1 phải TẮT rơi bậc (`roi_bac.on: false`) và hiện 1 P0 — vòng kiểm tại chỗ.

## Tóm tắt

1. Verdict `findings` — P0 1 · P1 4 · P2 6.
2. SAI: «S0 → S1 là đoạn máy / làn V T2 chỉ cần dán một lần» — S1#1 brainstorm
   là lượt chờ người, goal kết ở đó theo chính khuôn (P0).
3. SAI: «≥4 lần dừng do tiến trình nền (S3 ×1, S4 ×2, +1 khai sai)» — có vết: 2,
   cả hai ở S3, lần khai sai trùng lần dừng đầu; S4 chỉ có 2 phanh dừng-vá.
4. SAI theo nghĩa đo: «khớp TỪNG BYTE» ba bản — P85 đã là bằng-sau-strip;
   bản JS chưa có định nghĩa byte (431 byte / 6 dòng là số cần ghim).
5. Lời hứa không vật: «S4 qua Workflow» — vết có sẵn là run_id `minted-<slug>-…`
   + một ts/vòng; #136 và 2.7.0 đều mang hình dạng tay.
6. T3: lớp vật (thẻ) phủ 0/2 lần dừng có vết; Gate 1.5 chỉ lớp lời.
7. Đúng: timestamp `at`/`decided_at` trước commit (lần đầu sau 3 mốc); id sai chiều.
8. Đúng: khuôn không cần đổi chữ; Gate 1.5 ở SKILL S2#3; không nguồn kit nào khác
   nói thời điểm in — bốn câu «hai bản» sẽ lỗi thời.
