## Bài học từ feature trước (advisory)

- [cong-chan-nham-cho#F1] (cong-chan-nham-cho · S1 · finding) Làn V ở pre-merge mở bằng CHUỖI (mo + T2 + vết) không đo QUAN HỆ mo ⇔ xanh-sạch; không chân «mo nhưng report không sạch» — cùng lớp [veto-co-dau-vet#F2] — Report bị sửa thêm UNCERTAIN/Known limits sau S4 hoặc ai gõ tay mo → pre-merge NOTE, exit 0 → t… — fixed: AC-1 đòi (xanh-sạch HOẶC human_signoff), AC-2 thêm (d)+(e), E2 4/4+giữ-gân, design #1
- [cat-khoi-viec-cua-anh-tren-tin#F1] (cat-khoi-viec-cua-anh-tren-tin · S1 · finding) Bộ từ cấm chứa «khuôn» trong khi câu điều khoản gốc chứa «không khuôn»; mutant m2 «chèn từ khuôn» không phân biệt được với baseline; assert «0 xuống dòng» chưa nói chuẩn hoá wrap — Người thi công chép nguyên văn câu → E2 đỏ trên vật ĐÚNG, hoặc bẻ câu… — fixed: AC-2 + E2 viết lại đúng thước đó (đối chứng dương trước, m2 = «vế»)
- [veto-co-dau-vet#F1] (veto-co-dau-vet · S1 · finding) Không AC/eval nào canh chiều GHI vào chính `veto_state` — hook chỉ đo transition của `status`, pre-merge chỉ đọc trạng thái CUỐI — Owner gõ `da-veto`; máy sửa ngược về `mo` hoặc xoá hẳn khoá → `status` không đổi nên hook im, pre-merge thấy `mo` nên i… — **fixed (pre-Gate-1):** thêm **AC-3b** (so với BASE của diff, chiều `da-veto→mo` hoặc khoá-biến-mất = VIOLATION trừ khi có entry sổ quyết định khớp slug) + eval **E3b** ba chân, có chân giữ-gân cho đường xử hợp lệ
- [veto-co-dau-vet#F2] (veto-co-dau-vet · S1 · finding) Danh sách ĐÓNG máy-đọc **thiếu `verdict=PASS`** — văn AC-4 có, khối máy-đọc không; mà E4 tuyên «một bản, hai bên đọc chung» nên bên thi công đọc khối — Report **REJECT** nhưng 5 điều kiện kia sạch → checker đọc khối thấy đủ → hồ sơ TRƯỢT tự merge, kh… — **fixed (pre-Gate-1):** khối thêm `verdict=PASS` (và khoá phải TỒN TẠI); E4 thêm chân verdict≠PASS và chân vắng-khoá-verdict
- [doi-hanh-vi-cong-nguoi#F1] (doi-hanh-vi-cong-nguoi · S1 · finding) Ngăn cách đáp án khỏi agent hành động chỉ là LỜI DẶN — đáp án cùng thư mục đề ca, `question` in thẳng đường dẫn đáp án — Agent hành động có tool đọc file → một lệnh là thấy đáp án → transcript nhiễm, 4/4 judgment PASS oan trên đúng bộ đo BẮT BUỘC của… — **fixed (pre-Gate-1):** giao thức chống rò rỉ vào đầu evals.yaml — agent hành động là phiên KHÔNG TOOL nhận nội dung NẠP THẲNG, không đường dẫn workspace nào trong prompt; đáp án dời sang `giam-khao/` (thư mục riêng, chỉ giám khảo nạp); đường dẫn đáp…
- [luu-kho-codex-va-nghi-le-design#F1] (luu-kho-codex-va-nghi-le-design · S1 · finding) Từ vựng MIRROR vắng khỏi lưới quét (`sync-plugin-packages`, `mirror_sync`, `plugins/`, `P30`), phạm vi quét bỏ sót `CLAUDE.md` + `.github/`, và clause cuối AC-9 ("CLAUDE.md không còn tuyên bố bất biến mirror") không nằm trong mảng của E10 lẫn eval nà… — **fixed (pre-Gate-1):** mảng needle của AC-4 lên **11** (thêm 4 từ vựng mirror + `.agents`), phạm vi quét thêm `CLAUDE.md` + `.github/`; tách **AC-9b** + **E10b** riêng cho clause `CLAUDE.md` với đối chứng dương 4 hit ở mốc. Thêm `.agents` vì đường d…
- [luu-kho-codex-va-nghi-le-design#F2] (luu-kho-codex-va-nghi-le-design · S1 · finding) E7 ghim SÀN `n ≥ 671` cho suite `scripts` trong khi hồ sơ làm suite đó TEO; contract không chứa số nào (671/54/62 chỉ sống trong evals), E12 nói "trừ đúng số case P30" mà không số nào được khai — S4 đo ra ví dụ 664. E7 đỏ vì THƯỚC sai chứ không vì vậ… — **fixed (pre-Gate-1) — nhận KẾT LUẬN, BÁC CƠ CHẾ.** Critic quy cho `tests/codex/` + `tests/design-loop/` bị tính vào suite `scripts`; kiểm trên vật cho thấy KHÔNG phải: hai thư mục đó có runner riêng. Cơ chế thật là `tests/scripts/run-tests.sh:1390-1…
- [d-20260818T003225Z-4501] (siet-rang-cau-ve-hinh · S4-r5 · fix) răng chân diffBase thêm đối chứng dương 'PASS: P197' ở base; E1 bỏ tuyên chiều đỏ không có mã; m4 ghi chú giới hạn (thay cả câu = xoá bản, điểm mù đã khai). Hết quota vòng: xác nhận bằng lane máy re-pin, không vòng 6 — owner quyết ở Cổng 2 — finding trong hợp đồng r5 đóng bằng 1 dòng; đối chứng của dòng đó là suite + răng chạy tại HEAD (lane), không phải một vòng review
- [d-20260817T234355Z-4401] (siet-rang-cau-ve-hinh · S4-r4 · fix) răng AC-5 ghim vào dòng assert 'thong diep chua tung do: [...]' (dòng P197-M in sẵn làm chuỗi có mặt); P90 thêm guard m3b ≠ m3 và raw có ≥2 bản chép — hai finding trong hợp đồng r4 đóng; vòng 5 cần owner cho phép (STOP-PATCHING: r1 và r4 cùng lớp thước-không-gắn-vật trong răng)
- [d-20260817T154517Z-4301] (siet-rang-cau-ve-hinh · S4-r3 · fix) răng: mốc diffBase = merge-base 7d76384 (không phải 8d1e135), ghim 'so P197-M < san' + 'thieu dong P197-M-COUNT' + suite base không có P199; dọn dead code heredoc; docstring hfl_clause khai chiều đỏ-giả. Vòng 3 REJECT vì báo cáo r2 trên đĩa cũ hơn co… — hết 3 vòng máy — dừng trình owner
