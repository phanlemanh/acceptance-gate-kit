---
slug: duong-lui-phai-song
at: 2026-09-07T22:32:48Z
verdict: findings
p0: 1
p1: 3
p2: 1
by: phiên tươi độc lập (subagent context sạch, không chia sẻ context với phiên dựng hồ sơ)
claims_input: ok
---

## Findings

| Sev | Artifact | Thiếu gì | Kịch bản fail | Thước đo | Xử lý |
|---|---|---|---|---|---|
| P0 | design · contract · evals | AC-6 hứa ghi `da-veto` «qua công cụ sửa file (lưới ghi-lúc-viết kiểm)», nhưng luật hook hiện hành TỪ CHỐI đúng chuyển trạng thái đó trên hồ sơ `machine-cleared` đi làn V. Đã chạy `evaluateContractWrite` (lib/evidence-core.cjs) với payload cũ `status: machine-cleared · veto_state: mo · approved_by rỗng` → mới `veto_state: da-veto`: trả `status: machine-cleared with empty approved_by — Gate 1 approval not recorded` (da-veto làm `vOpen=false`, nhánh dòng 629 bắn). Hồ sơ `verified`+mo thì qua. E6 chỉ dựng fixture `verified` và ghi bằng script nên không bao giờ chạm hook. | Sau khi AC-8 ship, hồ sơ làn V đầu tiên vào ô kết `machine-cleared` (chính Đường đo số 3). Owner gõ «veto: lý do» ở lệnh ký → Edit contract bị hook chặn → veto CHẾT đúng trên dân số vòng này sinh ra để cứu, trong khi E6/E12b đều xanh. Cổng Bằng chứng duyệt sai. | E6 nâng thành ma trận viết trước 4 ô: status ∈ {verified, machine-cleared} × Cổng 1 ∈ {approved_by có, làn V mo}; mỗi ô ghi da-veto rồi tự kiểm bằng `evaluateContractWrite(new, old)` (cùng cách AC-8 tự kiểm) và assert 0 failure; ô machine-cleared×mo phải ĐỎ trên cây hôm nay trước khi vá — đó là đối chứng dương của chiều đỏ. Contract AC-6 khai rõ vế «hook cho phép da-veto trên mọi hồ sơ máy-đi-trước». | **fixed**: AC-6 + E6 thành ma trận 4 ô có đối chứng dương trên bản base (`git archive origin/main lib`); `lib/evidence-core.cjs` vào Vật chạm; design §3(a) thêm mục sửa `evaluateContractWrite` |
| P1 | evals · design | E3 và bảng răng §4 (`h1-rong`) ghim thông điệp «có nội dung» cho `# Known limits` h1, nhưng cơ chế (d′) đổi ranh bash về `#{2,6}` «cho khớp section()» sẽ cho h1 là KHÔNG PHẢI tiêu đề → bash in `mục «Known limits» VẮNG khỏi báo cáo (vắng ≠ rỗng)`; và `khong-can-nguoi.mjs` HÔM NAY đã trả `vang` cho h1 (sectionState dùng `#{2,6}`), không phải `có nội dung`. Kết quả «không xanh-sạch» thì đúng, nhưng chuỗi ghim sai. | Chân `h1-rong` đối chứng dương đỏ ngay lượt chấm đầu (bash nói VẮNG, eval đòi «có nội dung»); hoặc tệ hơn, S3 nắn mã để in «có nội dung» cho h1 — tức nhận h1 làm tiêu đề, ngược với thiết kế «khớp section()», và LV5 lệch với mjs. Đốt một vòng chấm trên trần ba vòng. | E3 ghim đúng vật: bash `__VANG__` ↔ mjs `why` = «mục «Known limits» VẮNG khỏi báo cáo (vắng ≠ rỗng)», assert HAI chuỗi bằng nhau từng ký tự; AC-3 sửa «Then cả hai trả «có nội dung»» thành «cả hai trả cùng một lý do không-sạch, không xanh-sạch». | **fixed**: AC-3, E3, design §3(d′) và §4 về thông điệp VẮNG; cơ chế `#{2,6}` giữ; khai HAI dòng gỡ |
| P1 | design · contract · evals | (d) chỉ đóng nhánh làn V khi `verified_commit` «giải được». Nhánh pin-ma (commit không tồn tại, clone đầy đủ) và nhánh vc rỗng vẫn `continue` ở NOTE xanh-sạch, trong khi hồ sơ có chữ ký ở cùng chỗ bị VIOLATION (luật P184, dòng 1048). Đây là cùng lớp «làn V thoát kiểm hoá cũ» của chính ô (d), không phải tính năng mới; E2 ma trận 3 ô không có ô «trong-diff, pin không giải được». | Hồ sơ làn V re-pin vào SHA nhánh rồi squash-merge (đúng cảnh đã đo: 136/136 pin ma) → chạm hồ sơ ở PR sau → khối mới thấy vc không giải được → im lặng, NOTE xanh-sạch, merge. Tấm lưới vẫn thủng đúng ô (d) mà mọi eval xanh. | E2 thêm ô thứ 4 code-sinh: hồ sơ làn V trong diff, `verified_commit` = SHA hợp lệ về hình thức nhưng không có trong repo (clone đầy đủ) → phải in VIOLATION cùng họ với P184; ô thứ 5: vc rỗng → NOTE có tên (không phải NOTE xanh-sạch). Số assert = 5 khai trước. | **fixed**: AC-2 mở rộng, E2 5 ô, design §3(d) ma trận 5 ô |
| P1 | evals · contract | E4/E5 đo «thủ tục 7b→7c / bước 8 mở rộng» bằng script fixture do người viết răng CHÉP LẠI lời lệnh; không dòng nào rút từ khối `SIGNOFF-LANE-CLAUSE`. E10 chỉ so hai bản chép bằng nhau, không ghim NỘI DUNG. Phần sống duy nhất là `repin-lane.mjs exit 1` — vật ĐÃ CÓ. (thước không gắn vào vật được giao — CLAUDE.md, hình dạng 1) | Ai sửa 7b thành `--write` hay bỏ vế «làn đỏ → KHÔNG commit» ở cả hai bản chép cùng lượt: E10 xanh, E4 xanh, Đường đo «CI đỏ hậu-chữ-ký 2→0» mất chỗ tựa mà Cổng Bằng chứng không thấy. | Khối `SIGNOFF-LANE-CLAUSE` chứa dòng lệnh nguyên văn trong fence; `rang.sh --chan ky-lan-song` RÚT dòng lệnh từ marker rồi chạy nó trên kho git tạm (writer→reader round-trip); mutant đổi cờ trong khối → E4 đỏ gọi tên. | **fixed**: AC-4 thêm vế «lệnh làn là một nguồn trong khối», E4/E5 rút lệnh từ marker, design §3(e) khai khối chứa lệnh nguyên văn |
| P2 | contract · design | 38 hồ sơ mở cửa veto đang ở `verified` KHÔNG được dời sang ô kết: AC-8/AC-9 chỉ ghi cho vòng mới, Out of scope không khai «không backfill»; và backfill bất khả nếu không ghim lại — chạm contract 38 hồ sơ → vào diff → chính khối (d) bắn stale cho cả 38. | Owner duyệt Cổng Phạm vi tin bảng điều khiển hết nhập nhằng; sau vòng, 38 hồ sơ vẫn «chờ tôi». Lượt kế ai chạy `--write` hàng loạt gặp 38 VIOLATION làn V stale — «sử liệu đỏ hàng loạt» đúng giả định sinh tử #2, do chính vòng này tạo điều kiện. | Out of scope khai đích danh «không dời 38 hồ sơ cũ; dời phải đi kèm chiến dịch ghim lại ở mốc kế». Đường đo số 3 giữ «0 → ≥1 ở vòng mới». | **fixed**: một gạch Out of scope + một dòng Known limits |

## Đã kiểm (ngoài các dòng trên)

- (i) DV5 additive-only: `ALLOWED_REMOVALS` so nguyên văn từng dòng. Ranh `#{1,6}` nằm ở HAI dòng bash (368 và 369) — design/contract nói «dòng gỡ duy nhất» → đã sửa thành hai dòng. (c) chèn VIOLATION trước hai dòng NOTE ở 1225/1229 và (d) chèn khối trước `continue` 956 đều làm được thuần THÊM.
- (ii) Hai bản dựng xanh-sạch: bash dùng `#{1,6}` ở cả `has` lẫn strip; mjs `#{2,6}`. LV5 đo đẳng thức trên `MAT_CAT` 21 ô, `kl-h1-co` bị loại trừ có chủ ý. Đưa ca vào và tăng `SO_MAT_CAT` thì LV5 SỐNG. Không bị làm mù → finding 2 chỉ về thông điệp.
- (iii) AC-4/AC-5: thủ tục fixture là chép tay lời lệnh → finding 4. `repin-lane.mjs` có `--allow-dirty` và đòi `feature_loop.suite_keys` — fixture phải khai khoá này. Kho kit: dòng định tuyến ngoài T1 → chữ ký kit LUÔN kéo AC-5; đúng thiết kế 9005.
- (iv) AC-6: đã CHẠY THẬT `evaluateContractWrite` với 4 chuyển trạng thái → ô machine-cleared×mo bị chặn → finding 1 (P0). Lưới 1268 chặn `da-veto` và chiều ghi-ngược 1278–1283 khớp mô tả E6.
- (v) AC-11 mẫu số: `STALE-DIFF-SCOPE-GUARD` và khối mới đều theo `slug_in_diff`; trên cây thật với `--base origin/main` chỉ hồ sơ này trong diff → vế «họ làn-V = 0» là hằng cho mục đích «sử liệu không đỏ hàng loạt»; không che VIOLATION đúng-thiết-kế của (c). Gộp vào finding 5.
- (vi) T3: contract `risk_tier: T3`, không `veto_state` (hook cấm mo trên T3), lưới T2-only → hồ sơ này không đi làn V, người ký; design §6 có Gate 1.5, trần 4 lượt. AC-8 «T3 → exit 2» khớp luật hook 624. SẠCH.
- AC ↔ eval: 12 AC đều có eval; Coverage A×B×C: 6 ô Core có AC; Later/Never khai. Ngưỡng opportunity ↔ Đường đo khớp; hạt giống (b) «gate-card nhận thêm hai khoá» đã lỗi thời vì gate-card đọc `MAY_THONG` từ status (S4-r10) → design bỏ đúng. SẠCH.
- Ma trận `expected`: E1 3 mũi × 2 chế độ + đối chứng; E8 5 ô; E2 3 → 5 ô (finding 3); E6 1 → 4 ô (finding 1). E1 arm «node vắng»: các tầng khác của lưới rơi về NOTE/regex, «tổng tăng 1» khả thi nếu fixture không khai `gap_probe: required`.
- Fixture viết tay đúng khuôn bên đọc: E7 dùng lời mời cổng do `gate-card.js` render từ fixture (P192 hai chiều) — SẠCH; E4/E5 → finding 4.
- Đo chỉ dẫn thay vì đầu ra: E9 cố ý đo văn bản SKILL, khai ở Known limits; E10 so bản chép — SẠCH về đẳng thức.
- Hardcode ROOT: E11 dựng base bằng `git archive origin/main scripts lib` (đúng bài P150); `rang.sh` chưa tồn tại — kiểm ở S4.
- Khoá config `dlps_*` chưa có trong `_acceptance/config.yaml` — cùng lớp [thuoc-khai-mot-dang-do-mot-neo#F1]; khoá sinh ở S3 cùng rang.sh, `config.yaml` đã thêm vào Vật chạm.
- Sổ 9001–9008: không lật.
