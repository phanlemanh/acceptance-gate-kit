---
slug: lop-bang-chung-nhin-thay
at: 2026-09-07T21:50:26Z
verdict: findings
p0: 0
p1: 2
p2: 3
claims_input: ok
---
## Findings
| Sev | Artifact | Thiếu gì | Kịch bản fail | Thước đo | Xử lý |
|---|---|---|---|---|---|
| P1 | contract | AC-4 tính `present` từ evals.yaml (∃ executor ui-check) mà không đọc verdict của chính eval đó trong evidence-report — trong khi thẻ Cổng 2 đã parse evidence-report. Ca mở đầu hồ sơ (oneflow E10: ui-check KHAI rồi bị descope ở S4 vì hạ tầng chụp) là ca «có eval ui-check trong evals.yaml, không frame» — không phải ca «agent chết → BLOCKED» mà Notes viện làm răng riêng. Giới hạn khai «chưa đếm frame trong evidence/» không phủ vế này: đọc verdict trong report không phải đếm frame. Lý do MỚI so quyết approach d-…357Z: neo executor vẫn đúng, nhưng ở Cổng 2 neo phải đọc trên báo cáo, không trên bản khai | Hợp đồng `[web]`, evals có E10 ui-check; S4 hạ tầng chụp hỏng, E10 ghi descoped/N-A trong evidence-report, các eval khác PASS, status verified. gate-card Cổng 2: coUiObserved → true → in «có 1 eval ui-check», KHÔNG cờ. Người ký đọc thẻ tin là có frame → ký. Đúng kịch bản 01/09 tái diễn với răng mới xanh | AC-4 + E4 thêm ca: fixture verified mà eval ui-check duy nhất mang verdict descoped/N-A/FAIL trong evidence-report → `ui_observed.present` false (hoặc trường riêng `present_in_report`) + HTML cờ «KHÔNG có» kèm id eval; đối chứng dương: cùng fixture verdict PASS → không cờ, dòng nêu số eval ui-check PASS. Ghim CỤM CHỮ, không chỉ số | fixed: AC-4 viết lại — present đọc trên block báo cáo (exit_code 0 + screenshot:), thêm declared/passed; E4 có ca E10 khai-mà-không-đạt + đối chứng PASS |
| P1 | evals | Lớp assertion âm-tính-một-mình không kèm bằng chứng «fixture đã được quét»: E2 (d)(e)(g)(i) «không dòng W8», E4 `[cli]` «không có cụm», E5 (c)(d)(f) «không dòng NOTE». E2 chỉ cấm «ghim exit code một mình», chưa cấm «ghim vắng-chuỗi một mình». Đối chứng dương ở ca (a) chứng lint CHẠY trên fixture (a), không chứng nó chạy trên fixture (d)/(e)/(g) — mỗi ca dựng fixture riêng | Hàm sinh fixture ghi contract sai đường (ví dụ thiếu `_acceptance/<slug>/`) hoặc frontmatter hỏng YAML → lint/pre-merge bỏ qua hồ sơ, 0 dòng W8/NOTE → ca (d)(e)(g) xanh dù vị từ laMatNguoiNhin có coi `[mobile]` là mặt người nhìn. Ca (i) cây thật: lệnh sai cú pháp exit 127 → 0 dòng W8 → xanh | Mỗi ca vắng-chuỗi ghim thêm MỘT dấu hiệu quét dương cùng lần chạy: exit code + dòng chú giải «W8 =» hoặc dòng đếm hồ sơ đã quét có tên slug fixture (lint); `rules ran=` hoặc dòng gated nêu slug (pre-merge); `--extract` `ui_observed.applicable=false` (gate-card). Ca (i) ghim exit 0 và số hồ sơ quét ≥ số hồ sơ thật trong `_acceptance/`. Chiều đỏ của lớp: một fixture cố ý ghi sai đường → ca phải ĐỎ vì thiếu dấu hiệu quét, không xanh | fixed: AC-2 (d)(e)(g)(j) chèn AC ngưỡng để W1 nổ làm dấu hiệu quét; AC-4 (e) ghim applicable=false; AC-5 (c)(d)(f) ghim rules ran= + dòng gated; chiều đỏ lớp: fixture sai đường phải đỏ |
| P2 | evals | D4 «một nguồn cho mặt người nhìn» chỉ được đo bằng HÀNH VI (coNguoiDungCuoi(`[web]`) true, gate-card `[web]` applicable) — không ca nào chứng nguong-o-co-hoi / gate-card / pre-merge ĐỌC bảng alias từ lop-nhin-thay.cjs thay vì chép `web` vào danh sách riêng. Trôi hai-nguồn chính là lớp lỗi hồ sơ này sinh ra để trị (`web`/`web-ui` trôi khỏi enum) | Implementer thêm `'web'` thẳng vào SURFACE_NGUOI_DUNG của nguong-o-co-hoi.cjs (đúng hành vi, E1 xanh); tháng sau alias thêm `browser` ở lib → răng chống lách «không đo được» không thấy `[browser]`, W8 thấy. Hai bộ đọc lệch nhau, không lưới nào kêu | E1 thêm mutant một-nguồn: bản sao lib bỏ `web` khỏi SURFACE_ALIAS, nạp nguong-o-co-hoi + gate-card + lint trỏ bản sao → cả ba phải ĐỔI kết quả cho `[web]` (false / applicable:false / không W8); đối chứng dương: bản lành cả ba true. Số assert = 3 bộ đọc × 2 chiều | fixed: AC-1 thêm mutant một-nguồn — bản sao lib bỏ web → ba bộ đọc cùng đổi kết quả (6 assert); E1 cập nhật |
| P2 | contract | Ma trận lint W8 (AC-2, 9 ca) thiếu chiều DESCOPE trong khi hằng `UI_OBSERVED_DESCOPE` có ba bộ đọc (gate-card · pre-merge · lint?). Hành vi lint trước entry `bỏ ui-observed — ` không khai; đối chứng seam dấu-hai-chấm chỉ đo ở gate-card (AC-3), không ở pre-merge (AC-5 b) và lint. Theo [loi-moi-cong-may-sinh#F1]: số assert phải bằng số ô, ô descope × lint và ô dấu-hai-chấm × pre-merge đang trống | Lint so tiền tố bằng `startsWith('bỏ ui-observed')` (không dấu gạch), pre-merge dùng grep lỏng, gate-card dùng hằng đúng: entry viết «bỏ ui-observed: hỏng chụp» được lint + pre-merge coi là đã bỏ (im lặng, NOTE nêu id) nhưng thẻ Cổng 1 vẫn fwarn — ba bộ đọc nói ba điều, người duyệt không biết tin thẻ hay log; số đếm ngưỡng lệch theo bộ đọc | AC-2 thêm (j) fixture (a) + entry descope đúng chuỗi → hành vi khai rõ (W8 vẫn in kèm «đã bỏ theo <id>» hoặc im — chọn một) và (k) entry dấu hai chấm → như không descope; AC-5 thêm ca dấu hai chấm → NOTE như (a), không nêu id. Ba ca đều rút chuỗi từ hằng lib (round-trip), không gõ tay | fixed: AC-2 thêm (j)(k)(l) descope đúng tiền tố / dấu hai chấm / --files; AC-5 thêm (i) dấu hai chấm; chuỗi rút từ hằng lib |
| P2 | design | Ngưỡng đang đếm của D3 («2 hợp đồng ký không frame trong một mốc», đọc từ NOTE của `--recheck-all`) chưa có phép đọc sống: (a) AC-5 chỉ chạy `--base <ref>`, không chạy `--recheck-all`; (b) NOTE không mang mốc ký, mà chiến dịch ghim lại mỗi release kéo hồ sơ CŨ `[web]` không ui-check (oneflow) vào diff → cùng một hồ sơ phát NOTE lại mỗi mốc. Chính lý do D1 bác neo-nhãn («NOTE giả ở chiến dịch ghim lại làm hỏng số đếm») áp vào neo-executor với hồ sơ cũ mà chưa được xử | Mốc kế: `--recheck-all` in 0 NOTE vì nhánh NOTE chỉ nằm trong đường diff → số đếm = 0 vĩnh viễn, giới hạn khai không có ngưỡng thật (CLAUDE.md: khai giới hạn PHẢI kèm một ngưỡng đang đếm). Hoặc ngược lại: hai mốc liên tiếp đếm oneflow + một hồ sơ cũ khác → ngưỡng «2» nổ trên tồn đọng, mở vòng siết VIOLATION mà chưa có hợp đồng MỚI nào ký không frame | AC-5 thêm (h) cùng fixture, `--recheck-all` → dòng NOTE giống (a); NOTE ghim thêm `approved_at`/ngày signoff của hồ sơ (đọc từ frontmatter/evidence-report, không gõ tay) để mốc phát hành đếm «ký trong cửa sổ» bằng grep một dòng; ca (i) fixture hồ sơ cũ signoff trước mốc → NOTE có ngày cũ để loại được. Thiết kế §2 D3 ghi rõ quy tắc đếm: distinct slug, signoff trong cửa sổ | fixed: AC-5 thêm (h) --recheck-all và NOTE ghim approved_at; Notes contract + design D3 ghi quy tắc đếm: slug phân biệt, approved_at trong cửa sổ |
