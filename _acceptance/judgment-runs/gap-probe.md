---
slug: judgment-runs
at: 2026-08-04T01:10:20Z
verdict: findings
p0: 1
p1: 2
p2: 2
---

## Findings

| Sev | Artifact | Thiếu gì | Kịch bản fail | Thước đo | Xử lý |
|---|---|---|---|---|---|
| P0 | contract | Triệu chứng khởi nguồn — dòng `## Variance` ghi "none" — không AC nào đo ở ĐẦU RA. AC-5 chỉ đo chuỗi trong PROMPT gửi tới `synthesize:report`; không tiêu chí nào đo văn bản report thật sự sinh ra. Đúng lớp "đo chỉ dẫn thay vì vật được giao" mà CLAUDE.md và [findings-section-boundary#F1] nêu tên | Implementer nhét dữ liệu vào prompt là E5 xanh; synthesizer vẫn viết `## Variance: none`. Cả 11 eval xanh, ký Cổng 2, mà artifact người đọc vẫn nói y nguyên câu sai đã sinh ra feature này. Workspace này KHÔNG tự phát hiện được vì evals.yaml của nó cố ý không có eval inert nào ⇒ round thật luôn có inertFields rỗng | Thêm AC + eval đo VĂN BẢN report với fixture do code sinh có eval inert; ghim Variance nêu đích danh evalId + field và không chứa "none" | **fixed:** ba lớp. (1) AC-5 viết lại — `inertNote` là LITERAL do JS tính, prompt bắt chép nguyên văn (khuôn `verified_commit` sẵn có), nên bên viết deterministic. (2) **AC-12 mới** — round-trip writer→reader: rút literal từ writer thật rồi cho `scripts/gate-card.js` đọc; kiểm chứng lộ thêm rằng reader lọc `/^none/i` nên literal bắt đầu bằng "none" sẽ bị nuốt lần hai — đã ghim thành assertion. (3) **Dogfood** — E10 của chính workspace này cố ý mang `runs: 3` để round S4 chạy nhánh inertFields khác rỗng, người ký thấy hành vi trên vật thật |
| P1 | contract | Mặt Cổng 2 chỉ được đo bằng CHỮ trong SKILL.md. AC-10/E10 đọc file hướng dẫn; không tiêu chí nào đòi một gói Cổng 2 THẬT chứa `inertFields`. Cùng hình dạng [ngon-ngu-mat-nguoi#F1] | Judge chấm PASS vì đoạn hướng dẫn đủ điều kiện, nhưng đường render thẻ không đọc key nào cho field inert nên gói người ký nhìn thấy vẫn trống — trong khi Coverage tự khai trục C đã phủ. Cổng 1 duyệt một Coverage sai sự thật | Thêm AC buộc gói Cổng 2 sinh bởi code chứa mục field-inert, đo trên đầu ra đường render thật | **fixed:** AC-12 đo đúng đường render thật (`scripts/gate-card.js`) chứ không đo chữ. Kiểm chứng xác nhận card CÓ đọc `## Variance` (dòng 373) — nên đường tới người ký là có thật và đo được |
| P1 | design + contract | Bất đối xứng hai harness: AC-8 sửa chữ ở cả bản codex, nhưng AC-10 chỉ ràng buộc SKILL.md của harness Claude. Bản codex không có tiêu chí nào buộc trình `inertFields` cho người | Implementer sửa ba chỗ mô tả (E8 xanh) rồi thêm bước Cổng 2 vào một harness. Người dùng codex nhận mô tả đúng nhưng KHÔNG bao giờ thấy cảnh báo field inert ở cổng ký — lỗ im lặng còn nguyên trên một harness, mọi eval vẫn xanh | Mở rộng AC-10 sang cả hai harness và đo bằng cùng một luật; hoặc ghi codex vào Out of scope có tên | **fixed:** AC-10 mở sang cả hai harness; E10 nhận thêm input 2 là SKILL.md codex, và điều kiện PASS (1) nói thẳng "một bên có, một bên thiếu = FAIL" |
| P2 | evals | E9 counter quá lỏng: chỉ FAIL khi quét ra 0 eval; lớp lỗi có HAI thành viên mà counter không đòi mỗi hình dạng một mẫu. Kết luận "verdict KHÔNG phải BLOCKED" là assertion âm-tính-một-mình, không đối chứng — [findings-section-boundary#F2] | Regex chỉ khớp `runs:` mà sót `paths:` (khác hình dạng YAML). Counter thấy 10 eval nên xanh; nhánh `paths` trên judgment chưa từng chạy qua workflow. Bản vá sau đẩy eval mang `paths` vào failedEvals ⇒ stale-cascade thật trên workspace đã ký, mà E9 vẫn xanh | Counter tách theo hình dạng (≥1 `runs` VÀ ≥1 `paths`), ngưỡng viết thẳng vào expected; thêm đối chứng dương chứng minh case đỏ được | **fixed:** E9 viết lại đúng cả hai vế — counter tách hình dạng với ngưỡng viết thẳng, cộng đối chứng dương bằng args bị tiêm gây BLOCKED thật |
| P2 | contract | Trục C liệt kê 6 mặt, `run-log.jsonl` không có AC nào và cũng không nằm trong Out of scope ⇒ ô trục rơi im lặng | Implementer không ghi gì vào run-log; mọi eval xanh. Bản ghi bền vững của round vẫn im lặng đúng như trước feature, mà Cổng 1 đã duyệt một Coverage trông như phủ kín | Thêm AC nhỏ cho run-log kèm eval ghim khoá, hoặc thêm dòng Out of scope nêu đích danh | **fixed:** **AC-13 mới** — dòng `kind: "inert"`, cùng khuôn dòng `kind: panel`/`baseline` sẵn có, không mang `run_id` nên consumer cũ không vỡ. Coverage nay khai rõ 6/6 mặt trục C có AC |
