---
slug: ghim-lai-tren-lop-cu
at: 2026-09-11T16:00:15Z
verdict: findings
p0: 0
p1: 3
p2: 2
claims_input: ok
---

## Findings

| Sev | Artifact | Thiếu gì | Kịch bản fail | Thước đo | Xử lý |
|---|---|---|---|---|---|
| P1 | contract + evals | AC-7 hứa «đi lối tiếp mà THÔNG ĐIỆP in ra» nhưng E7 gõ tay --ag-root, không lấy gì từ stderr, và không eval nào chạy lệnh dò resolve-plugin.mjs in trong thông điệp | Thông điệp in lệnh dò với đường tương đối gốc kit; người chạy đứng ở --root chạy lệnh thì hỏng, mà E7 vẫn xanh vì tự gõ đường | Round-trip: trích dòng lệnh dò khỏi stderr, chạy với cwd = --root và HOME = plugin cache code-sinh, dùng ĐÚNG đường in ra làm --ag-root; bản sao in đường tương đối → đỏ ghim | fixed: AC-7 và E7 thành round-trip ba bước rút từ stderr (dòng `lệnh dò:`), cwd = --root, HOME = cache code-sinh chứa bản chép cây đang đo; thêm chiều đỏ (a) đường tương đối gốc kit ghim «lệnh dò không chạy được từ --root»; design §4 + Notes khai đường TUYỆT ĐỐI và định dạng dòng `lệnh dò:` |
| P1 | evals + design | Ma trận xoá-export chạy trên MỘT hồ sơ mọi eval exit 0 nên nhánh chỉ chạy khi evals_exit khác 0 không bao giờ được với tới; ô (G) chỉ đòi exit 0 + recheck xanh, không so nội dung đã ghi | Một lối đọc mới `?.` hoặc `?? mặc định` trong làn lặng lẽ rơi về mặc định mà vẫn xanh — đúng lựa chọn (b) đã loại, lọt vào qua cửa sau | Ma trận × HAI hồ sơ (exit 0 và expected_exit 2); ô (G) so byte dòng repin + mục Re-pin với đối chứng sau chuẩn hoá run_id/ts/ngày; phép QUAN HỆ tên core.X + tên rút từ lib ⊆ hàng bảng; bản sao recheck thêm core.fakeX → đỏ | fixed: AC-3 và E3 chạy ma trận × hai hồ sơ, (G) đòi bằng byte sau chuẩn hoá, số ô = số export × 2, thêm phép quan hệ ⊆ khối AG-ENGINE-TABLE với chiều đỏ core.fakeX; design §4 viết lại đoạn coupling; entry fix trong sổ quyết định cho chi phí ma trận gấp đôi |
| P1 | contract + evals | AC-2 hứa liệt kê TRỌN mọi mục thiếu nhưng E2 chỉ đòi «ít nhất» hai dòng ở HAI tệp khác nhau; tập kỳ vọng không tính lúc chạy; không fixture nào thiếu hai export CÙNG tệp | Làn dừng ở mục thiếu đầu tiên của mỗi tệp: trên 0b5c5b37 vẫn in đúng hai dòng hai tệp nên E2 xanh, trong khi lớp thiếu nhiều export cùng tệp chỉ báo một | Tập kỳ vọng tính lúc chạy từ hàng bảng × require() lớp cũ, so đẳng thức tập và số dòng; ô xoá hai export cùng evidence-core.cjs; bản sao dừng ở mục đầu mỗi tệp → đỏ ghim | fixed: AC-2 và E2 so đẳng thức tập tính lúc chạy (hàng AG-ENGINE-TABLE vắng khi require() lớp 0b5c5b37), số dòng = số phần tử; thêm ô hai-thiếu-cùng-tệp và chiều đỏ ghim «thiếu mục cùng tệp»; Coverage trục B thêm giá trị «thiếu nhiều export cùng tệp» |
| P2 | design + contract | Design hứa cổng bộ máy chạy TRƯỚC kiểm cây sạch nhưng không Then nào nói vậy và mọi fixture là kho sạch | Người chạy ở kho tiêu thụ có cây bẩn nhận thông điệp cây bẩn, commit rồi chạy lại mới biết bộ máy cũ — một vòng phí; thứ tự bị đảo mà không eval nào đỏ | Ô kho có tệp đã theo dõi sửa chưa commit với --ag-root = 0b5c5b37 → thông điệp thiếu export, KHÔNG thông điệp cây bẩn; đối chứng dương cùng kho bẩn với cây đang đo → thông điệp cây bẩn hôm nay | fixed: AC-1 và E1 thêm ô cây bẩn (thiếu export, không «cây bẩn ngoài _acceptance/») + đối chứng dương cùng kho bẩn ra đúng thông điệp cây bẩn; Coverage cross-cutting trỏ ô này; design §4 nêu lý do thứ tự |
| P2 | evals | (a) không expected nào ghim «đúng MỘT dòng kết quả mang GLxx», và phép chặn «GLLC_CASES khop 0 ca» không có eval chạy chiều đỏ; (b) đối chứng dương E5/E6 nói «qua cổng kiểm bộ máy» (vắng thông điệp), không ghim exit 0 — lại là assertion âm tính một mình [thuoc-khai-mot-dang-do-mot-neo#F1] | Khoá gllc_* trỏ nhầm ca hoặc bộ lọc khớp 0 ca → exit 0 với 0 dòng mà eval xanh; E5/E6 xanh cả khi làn chết vì lý do khác sau cổng | Mỗi E1–E7 ghim đúng một dòng kết quả mang mã ca của nó; ca GL08 chạy GLLC_CASES=GL99 đòi exit khác 0 + «GLLC_CASES khop 0 ca»; E5/E6 đối chứng dương ghim exit 0 cùng kho tạm | fixed: header evals + mỗi E1–E7 ghim đúng một dòng kết quả mang GLxx của nó; thêm E13 (ca GL08, khoá gllc_bo_loc_rong) chạy chiều đỏ của bộ lọc; AC-5/AC-6 và E5/E6 đối chứng dương ghim exit 0 cùng kho tạm; AC-8 nói bộ chọn ca sống |
