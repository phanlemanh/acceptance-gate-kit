---
schema_version: 1
feature: Gom đúc kết 08/09 thành một vòng T3 trước mốc 2.10.0 — nợ C1 (7 Known limits của lop-bang-chung-nhin-thay), K1 null-guard workflow, K2 danh tính hết trạm thu phí, K3 W6/W8 thu phạm vi, K5 S5 mặc định PR, K8 làn conventions chỉ chấm file đổi
slug: gom-duc-ket-2-10-0
owner: phanlemanh@gmail.com
risk_tier: T3               # C1 chạm lib/eval-yaml.cjs + lib/lop-nhin-thay.cjs + lib/context-glossary.js + scripts/pre-merge-check.sh (t3_paths)
surfaces: [cli]
status: verified
approved_by: Manh Phan
approved_at: 2026-09-08T15:20:00Z
design_doc: docs/superpowers/specs/2026-09-08-gom-duc-ket-2-10-0-design.md
---

# Acceptance Contract: gom-duc-ket-2-10-0

## Context

Vòng `lop-bang-chung-nhin-thay` (PR 158) tốn 578M token, 8h43 làm-xong→quyết-được, 15 lượt gọi
người so trần 4 — đúc kết bản 3 (08/09) chỉ ra tám phát hiện F1–F8 và đề xuất K1–K8 + nợ C1/C2.
Owner gọi tên tối 08/09: gom hết vào MỘT vòng T3 trước mốc 2.10.0 (luật (b): tối đa một vòng meta
giữa hai mốc). Vòng này cũng là chỗ ĐO nếp N1–N7 đối chiếu mốc cùng hạng T3 của kit (usage-report:
TB 55,6M / 3,25 round; Iterations TB 5,5). Mốc BASE-LNT cho chiều đỏ có sẵn của C1: `334d5d52`
(cha của merge PR 158 — bản trước khi có `lib/lop-nhin-thay.cjs`).

## Criteria

- AC-1: Given cây sau hồ sơ, When chạy hai ca đo LNT1 và LNT6 của `tests/plugins/lop-nhin-thay.test.mjs`, Then LNT1 chứng được bản sao mutant ĐÃ CHẠY trước khi tin nhánh nghĩa vụ vắng: `gate-card.js` của bản sao (bỏ alias `web`) trả `ui_observed.token_la` chứa `web` trong khi bản lành trả `[]` — bản sao không chạy thì ca ĐỎ nêu «dấu hiệu bản sao»; LNT6 (iv) rút CẶP alias từ câu «Alias máy đọc: `web`, `web-ui` → `ui`» của term **Surface** trong CONTEXT.md và so BẰNG với `SURFACE_ALIAS` của lib (bản sao CONTEXT đổi `→ ui` thành `→ mobile` → đỏ nêu «alias»); LNT6 (vi) rút đoạn 3b của `commands/acceptance-init.md` và chứng câu nhắc `@playwright/cli` nằm trong CÙNG đoạn với `capture.ui` và lệnh ba dòng chứa `playwright-cli` (bản sao dời câu sang 3c → đỏ nêu «3b»); chiều đỏ có sẵn của LNT1 neo `git show 334d5d52:lib/nguong-o-co-hoi.cjs` (mốc bất biến), không neo `main`.
- AC-2: Given cây sau hồ sơ, When đọc bốn bộ đọc surfaces và chạy pre-merge trên ba bản sao hỏng, Then (a) `lib/nguong-o-co-hoi.cjs` không còn xuất `LNT_AVAILABLE` (bản tại `0d5b2dd8` — commit ĐÃ THÊM cờ, tìm bằng `git log -S` — có nó: đối chứng dương của phép đo vắng mặt); (b) `scripts/pre-merge-check.sh` in NOTE lớp nhìn-thấy nêu ĐÚNG nguyên nhân: bản sao không có `node` trên PATH → «thiếu node»; bản sao xoá `lib/lop-nhin-thay.cjs` → «thiếu lib/lop-nhin-thay.cjs»; bản sao lib ném lỗi → «lib lỗi (exit <n>)» — ba thông điệp phân biệt nhau, exit 0 cả ba; (c) `lib/eval-yaml.cjs` xuất `stripComment(s)` (cắt `#` khi đứng đầu hoặc có khoảng trắng đứng trước, giữ `x#y`), và ba bộ đọc `lib/lop-nhin-thay.cjs` (`classify`), `scripts/eval-coverage-lint.js` (W8), `scripts/gate-card.js` (`--extract ui_observed`) trả CÙNG kết quả trên bốn fixture `surfaces: [ui] # c` · `[ui]\t#c` · `[ui]#x` · `[ui]` (applicable true/true/false/true — `[ui]#x` là token lạ, không phải `ui`); (d) `html()` của `tests/plugins/lop-nhin-thay.test.mjs` không trả ký tự ESC (bản sao gate-card in một chuỗi ANSI → `html()` vẫn sạch, `raw` vẫn có ESC — cặp dương/âm cùng fixture).
- AC-3: Given Workflow S4 mà agent `capture:provenance` chết (harness trả `null`), When chạy `acceptance-verify.js`, Then workflow TRẢ VỀ (không ném) với `verdict: BLOCKED`, `blocked` có mục `{cmd: 'capture:provenance', reason}` với reason bắt đầu đúng chuỗi `capture:provenance agent bi skip/chet — khong co ket qua`, agent `synthesize:report` KHÔNG được gọi với `prov.enforcement_mode` rỗng; đối chứng dương: provenance sống → không mục blocked nào tên `capture:provenance`.
- AC-4: Given ba văn bản mang khối `IDENTITY-ECHO-RULE` (nguồn `skills/acceptance/references/human-facing-language.md` + hai bản chép byte-đúng `commands/approve.md`, `commands/signoff.md`), When đọc khối và thân lệnh, Then (a) khối chứa «GHI THẲNG» và «(từ <nguồn suy>)» và KHÔNG chứa «Enter xác nhận» hay «chờ xác nhận» ở bất kỳ ca nào — ca duy nhất được hỏi là **CẠN**; (b) thân `approve.md`/`signoff.md` KHÔNG còn câu «CẢNH BÁO» khi tên «không có trong `signoff.approvers`»; (c) thân `signoff.md` có nấc «cổng trước» nói: tên ở `approved_by` thắng `git config user.name` khi tác giả commit ghi `approved_by` bằng tên git hiện tại; (d) thân cả hai nói người sửa danh tính «bằng một câu» sau khi ghi; (e) chiều đỏ: bản sao khối tiêm đuôi « — Enter xác nhận» → bộ kiểm ĐỎ nêu «nhánh chờ»; bản sao thân tiêm câu CẢNH BÁO approvers → đỏ nêu «cảnh báo approvers»; ba bản khối vẫn byte-đúng (LM17 giữ); neo `enter-xac-nhan` của P191 và `khuon Enter-xac-nhan` của P194 đổi thành cặp: neo dương «GHI THẲNG» + neo âm «Enter xác nhận» (bản sao mang chuỗi cũ → P191/P194 đỏ).
- AC-5: Given `scripts/eval-coverage-lint.js` sau hồ sơ, When chạy trên fixture code-sinh và trên cây thật, Then (a) W6 chỉ quét section `## Criteria` — alias trong `## Context`/`## Notes` không kêu, cùng alias trong Criteria kêu (cặp cùng fixture); (b) ĐỊNH DANH ASCII gạch nối (`ui-check`, `pre-merge`, `re-pin`: hai bên gạch đều là chữ Latin/số không dấu) là MỘT từ — alias `check` không khớp trong `ui-check`; ghép gạch nối tiếng Việt (`check-lại`, `thẻ-cổng-2`) KHÔNG phải định danh → alias bên trong VẪN kêu; alias tự là định danh (`pre-merge`) vẫn khớp nguyên vẹn — ba chiều cùng fixture; (c) sáu từ đa nghĩa (`thẻ` · `tag` · `hook` · `engine` · `test` · `check`) được GỠ KHỎI `_Avoid_` của CONTEXT.md — khai thẳng là không còn cấm — và `_Allow_` giữ nguyên hợp đồng của nó (chỉ carve-out CỤM nhiều từ; không mục `_Allow_` nào là từ trần, vì `allow` là toàn cục nên một từ trần ở đó vô hiệu luật của MỌI term); hợp đồng thật của kit dùng «`thẻ` Cổng 2» không còn kêu; (d) W8 KHÔNG còn nhánh token-lạ: `surfaces: [ui, kiosk]` → không dòng W8 token, nghĩa vụ `ui-check` vẫn kêu khi thiếu; nhãn lạc chỗ vẫn kêu; L47 gỡ, L42m viết lại thành «mutant bỏ alias → cả hai nhánh im, và gate-card bản sao trả token_la [web] chứng bản sao đã chạy»; (e) chân đo cây thật `rang.sh --chan cay-that [--dev-root <thư mục>]` (mặc định thư mục cha của kit, không hardcode) là CHỨNG-MỘT-LẦN, KHÔNG phải bất biến thường trực: lint chạy được trên kit và trên mọi cây tiêu thụ CÓ MẶT (artifact-platform · oneflow · crm) — lint chết (exit ≥2) hoặc không có dấu hiệu quét thì ĐỎ có tên, cây vắng in `SKIP <tên>: vắng` có tên; số W8-token, W8 nghĩa vụ và W6 từng cây chỉ IN (trước 127 / 140·15·4 / 16·2·9, sau ghi ở bằng chứng + Notes), không ghim vào eval thường trực.
- AC-7: Given SKILL feature-loop và khuôn config, When đọc S5, Then (a) khối marker `S5-SHIP-DEFAULT` trong `feature-loop/skills/feature-loop/SKILL.md` nói: mặc định MỞ PR, không bày menu; chỉ hỏi khi `feature_loop.ship_default: ask`; giá trị hợp lệ `pr` (mặc định) · `merge` · `branch` · `ask`; (b) khuôn config trong `commands/acceptance-init.md` khai cùng khoá và cùng bốn giá trị (rút từ SKILL và so BẰNG — một nguồn); (c) bản sao gỡ khối → đỏ nêu «S5-SHIP-DEFAULT»; bản sao đổi mặc định thành `ask` → đỏ nêu «mặc định».
- AC-10: Given round S4 ≥ 2 với `args.deltaFiles` (danh sách file đổi so anchor, do `s4-args.mjs --carry-anchor` tính và ghi vào args), When dựng prompt làn `review:conventions`, Then prompt chứa câu «CHI cham cac file DA DOI so round truoc» + đúng danh sách MỌI ĐUÔI (cả `.md` lẫn code — lọc theo .md làm làn câm khi vòng sửa chỉ chạm code); vắng `deltaFiles` (round 1 hoặc `--no-carry`) → prompt là toàn diff như cũ; `s4-args.mjs` ghi `deltaFiles` vào tệp args khi có `--carry-anchor` (fixture git cho cả hai chiều).

## Coverage

Quét theo preset test-matrix (chân sản phẩm: `[SUY-TỪ-REPO: CLAUDE.md]` north star + luật (a)(b)(c);
chân ngành: `[NGÀNH: mutation testing — Stryker/PIT: mutant «killed» chỉ khi chứng mutant đã chạy]`
cho hình dạng 4, `[NGÀNH: Nx/Bazel affected-graph]` cho stale theo paths, `[NGÀNH: DORA lead time
for changes]` cho làm-xong→quyết-được).

- Trục A — nhóm vật: C1-đo | C1-nhỏ | K1 | K2 | K3 | K5 | K8 [thước CE: bảng K/C của đúc kết bản 3 — 7/7 nhóm CÒN trong vòng có AC; K4·K6·K7 đã rút, lý do ở Out of scope]
- Trục B — bộ đọc bị chạm: workflow | lib | pre-merge | lint | gate-card | SKILL/commands | `test harness` [thước CE: bảng «Chỗ chạm» trong design doc; mỗi bộ đọc có ≥1 AC hoặc lý do gạch]
- Trục C — chiều đo: dương | đỏ ghim thông điệp | đọc-cũ (bản sao thiếu `lib/lop-nhin-thay.cjs` quay về regex cũ · pre-merge thiếu node/lib vẫn in NOTE nêu đúng nguyên nhân · W6 thiếu `md-section.cjs` chấm cả tài liệu và nói ra một dòng rơi bậc) | mutant (bản sao gỡ khối · tiêm nhánh chờ · xoá lib · lib ném lỗi) | số đo trên cây thật (K3) [thước CE: MEASURE-BIRTH-CLAUSE + sáu hình dạng CLAUDE.md]
- Ô gạch có lý do: gate-card × K2 (thẻ không in danh tính) · hooks/ × mọi nhóm còn lại (không nhóm nào chạm hook) · carry-plan × K8 (parser paths riêng, hợp nhất là ô kế).

## Out of scope

- **K4 (phạm vi hoá-cũ theo `paths:`) — RÚT khỏi vòng này (owner quyết 09/09 sau lượt chấm 4).** Bốn lượt chấm cho ba lỗi fail-open liên tiếp ở cùng một hàm, lỗi cuối nặng nhất: `paths` khai THƯ MỤC TRẦN (`scripts`, `hooks`, `skills`…) không khớp file nào bên trong, nên tám hồ sơ thật của kho sẽ KHÔNG BAO GIỜ hoá cũ — yếu hơn trạng thái trước K4. Lưới trở lại luật cũ «cả cây». Mở ô riêng sau mốc: chuẩn hoá `paths` (thư mục trần phải khai thành mẫu bao trùm cả cây con), đọc lời khai TỪ `verified_commit` chứ không từ cây làm việc, và hợp nhất ba bộ đọc `paths:`.
- **K7 (triage theo tác hại) — RÚT khỏi vòng này (cùng quyết định).** Nâng finding `harm=behavior` vào hợp đồng làm lỗi mức trung bình biến mất khỏi CẢ vòng vá (verdict REJECT chỉ bật bởi `high`) LẪN thẻ Cổng Bằng chứng (thẻ chỉ render khối «Ngoài hợp đồng») — ngược đúng mục tiêu của nó. Lỗi hành vi trở lại lối cũ: lên thẻ cho người quyết ba lối. Ô riêng sau mốc phải sửa CẢ hai bên đọc trước khi nâng.
- **K6 (`scripts/loop-health.mjs` — máy đếm ba dòng số của luật (c)) — RÚT khỏi vòng này (owner quyết 09/09 sau lượt chấm 5).** Công cụ suy mốc cổng bằng `git log -S`, tức khớp CHỮ ở bất kỳ đâu trong thân hợp đồng; bốn hồ sơ trích nguyên văn `status: signed-off` trong tiêu chí nên mốc ký bị tính trước mốc làm xong: đo tại chỗ ra `premerge-unjudged-pass -64` và `ra-co-ten-lam-va-trao -98` phút. Dòng thứ ba cũng sai lớp: `infra_burned` đếm số lần xuất hiện chữ «BLOCKED» trong văn xuôi thay vì đếm round, ra 297 trên cây này. Luật (c) vốn viết ĐẾM TAY, nên K6 là tối ưu hoá hỏng chứ không phải yêu cầu; luật (a) cấm mở vòng đo-thước-của-thước để chữa nó. **File `scripts/loop-health.mjs` và `tests/scripts/loop-health.test.mjs` Ở LẠI cây** vì hồ sơ ĐÃ KÝ `ra-co-ten-lam-va-trao` khai miễn trừ đích danh hai đường dẫn đó — xoá đi thì ratchet hai chiều đỏ. Chúng đi lối «có mặt, chưa tin được»: mốc 2.10.0 đếm tay. Ô riêng sau mốc: đọc `status` từ frontmatter của TỪNG revision (không grep thân), đếm round BLOCKED theo neo `Round <n>`, và chặn giá trị âm.
- **AC-5(d)(e) — hai răng của K3 khai GIỚI HẠN, không tuyên đã chứng (cùng quyết định).** (d) Ca PV5 tự khai có «chiều đỏ của phép quét» nhưng dựng `probe` bằng cách loại mọi dòng khớp `L42m` rồi assert `probe` không còn dòng nào khớp `L42m` — hằng đúng theo cấu tạo, không nội dung nào của `tests/scripts/run-tests.sh` làm nó đỏ được; nghĩa là bộ dò ca L42m chết thì không phép đo nào biết. Phần THÂN của (d) vẫn đứng: W8 hết nhánh token-lạ và mutant bỏ alias chứng được đã chạy — do E1 (LNT1, `token_la` chứa `web`) và PV4 đo, không do PV5. (e) Nhánh `FAIL` khi còn dòng «W8 surfaces carry token» của `rang.sh --chan cay-that` không còn đường sinh nào sau khi (d) gỡ nhánh đó khỏi `scripts/eval-coverage-lint.js`, nên nó là assert âm-tính-một-mình; giữ lại như lưới hồi quy rẻ, KHÔNG tính là bất biến đã chứng. Ô riêng sau mốc: cho PV5 chạy lại chính bộ dò trên bản sao đã gỡ ca và ghim thông điệp «không tìm thấy ca L42m».

- Chiến dịch sửa 16 `config.yaml` / ≈99 hồ sơ hai tên (K2 làm lệch tên vô hại; không chạm hồ sơ đã ký).
- Hợp nhất parser `paths` của `carry-plan.mjs` về `lib/eval-yaml.cjs` (ô kế: `paths-mot-nguon`).
- Đếm lượt gọi người bằng máy từ bản ghi phiên (ngoài repo).
- K8 đo ở repo tiêu thụ — chưa có số ngoài kit; khai ngưỡng đếm ở Notes, không dựng phép đo mới.
- Chia phiên theo giai đoạn (N1) — nếp máy, không phải vật kit; đo ở ba dòng số.
- Mốc 2.10.0 (bump manifest, ghim lại, rollout) — hồ sơ mốc riêng, làn V, sau khi hồ sơ này gộp.
- Sửa `carry-plan.mjs`, `hooks/`, `recheck-evidence.cjs` — không chạm dù T3.

## Notes

- K8 mở dưới luật nới 07/09 (CỘNG có trace: nguyên tố 2, người hưởng owner ở round). Bằng chứng chỉ ở kit (13/34 finding chữ lặp). **Ngưỡng đang đếm ở repo tiêu thụ:** hai mốc phát hành liên tiếp không có round ≥2 nào ở tiêu thụ → xét gỡ K8.
- Số đo K3 (chứng-một-lần 09/09, `rang.sh --chan cay-that --dev-root /Users/manh-macmini/dev`): W8-token 0 ở cả bốn cây (trước: kit 0 · artifact-platform 140 · oneflow 15 · crm 4); W8 nghĩa vụ giữ nguyên 16 / 2 / 9 đúng như đếm tay; W6 kit 127 → 15, artifact-platform 36 → 18, crm 17 → 5. Mười lăm dòng W6 còn lại của kit là từ có nghĩa để kêu (`spec`, `log`, `result`, `tool`, `UI`, `tracking`…), không phải từ đa nghĩa.
- Chạy `rang.sh` từ worktree thì thư mục cha không chứa cây tiêu thụ → in `SKIP <tên>: vắng` và bất biến chỉ áp cho cây có mặt; số ba cây tiêu thụ lấy bằng `--dev-root` khi đo tay (đã ghi ở gạch trên).
- Đối chiếu số máy ↔ số tay (K6, 09/09 — GIỮ làm ghi chép cho lần đếm tay của mốc, không còn là AC) — ba chỗ lệch, đã truy: (1) **script sai, đã sửa**: token S4 phải cộng cả `cache_create`, mà bảng của `wf-usage.mjs` không có cột đó (nó nằm ở dòng tổng per-model) — sau khi sửa, T2 khớp đúng 43,8M của đúc kết; (2) **định nghĩa khác nhau**: T2 round TB 3,00 (máy khử trùng section cùng số round — section «resume» sau khi workflow sập không phải một vòng chấm mới) so 3,06 đếm tay (đếm cả section resume); (3) **số tay không tái lập được**: «Iterations TB T3 = 5,5» không ra được bằng bất kỳ định nghĩa nào thử (max Round 6,6 · đếm dòng Round 6,5 · tính cả hồ sơ 0 round 6,0) — máy chốt định nghĩa MAX (đếm dòng đếm dư: một hồ sơ có 19 dòng Round cho 12 vòng vì báo cáo nhắc lại round cũ).
- Vá lượt 4 (owner duyệt 09/09 sau khi luật dừng-vá nổ): chín lỗi HÀNH VI trong hợp đồng còn mở qua ba lượt chấm đã đóng theo LỚP — (1) dòng tổng kết lượt ghi lại theo verdict thật khi lane xuất-xứ chết; (2) nhánh trả về đó khai đủ trường như hai đường BLOCKED có sẵn; (3) `staleScope` normalize qua `stripComment` + fail-closed với executor lạ; (4) làn rà soát quy ước lọc theo MỌI file đã đổi, không riêng `.md` (bản đầu làm làn câm khi vòng sửa chỉ chạm code); (5) thẻ Cổng Bằng chứng sắp MỘT LẦN rồi ba nơi đánh nhãn theo cùng thứ tự; (6) sáu từ đa nghĩa gỡ khỏi `_Avoid_` thay vì bịt bằng `_Allow_`; (7) require chéo trong `lib/` guard + rơi bậc có tiếng; (8) hai dòng chèn giữa câu định nghĩa đã viết lại; (9) `clean` của thẻ dùng chung luật cắt chú thích cho mọi trường frontmatter.
- Lỗi QUY TRÌNH của máy (khai để đếm vào ba dòng số): ba lượt đầu máy đọc danh sách «trong hợp đồng» từ đầu ra thô của agent phân loại thay vì kết quả cuối của workflow, nên vá thiếu — K7 nâng lỗi hành vi trong `paths` lên trong-hợp-đồng SAU bước agent. Đây là nguyên nhân thật của việc luật dừng-vá nổ ở lượt 3.
- Chứng-một-lần (gap-probe P1): số đo cây tiêu thụ (E5b) chỉ IN, neo ở tập cây CÓ MẶT lúc đo — không ghim thành bất biến thường trực; kết quả ghi ở bằng chứng và hồ sơ mốc 2.10.0.
- Ba dòng số của luật (c) cho mốc 2.10.0 ĐẾM TAY (đúng chữ CLAUDE.md), không lấy từ `scripts/loop-health.mjs` — K6 đã rút, lý do ở Out of scope.
- Nếp đo ở vòng này (N2–N7): mục tiêu ≤2 round S4, S4 ≤ 40M token, ≤4 lượt gọi người trong thiết kế + 0 ngoài, 0 vòng bị hạ tầng đốt — ghi ở hồ sơ mốc.
