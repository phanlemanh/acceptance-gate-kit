# Sổ vấp triển khai (GĐ2→GĐ3) — ghi, KHÔNG sửa giữa chừng

Ngưỡng khai trước (2026-08-08, trước ván đầu): 0 lỗi chặn-việc ·
số-lần-bị-gọi ≤ 2/feature (Cổng 1 + ký) · owner tự chấm "muốn dùng tiếp" = có.
Ván đầu khai trước là THÁM HIỂM — không chấm cuộc tái lập bằng ván đầu.

**Chấm ngưỡng 3 sau ván 1 (2026-08-09) — owner trả lời nguyên văn: "Có, tôi
muốn dùng tiếp".** Hai ngưỡng còn lại ván 1 đều trượt (2 lỗi chặn-việc; 7 sự
kiện cần người, trong đó chỉ 3 là cổng theo thiết kế — 4 cái còn lại do bộ đồ
nghề trục trặc). Ngưỡng người là ngưỡng duy nhất không suy được từ git, và nó
ĐẠT — lộ trình vòng 2/vòng 3 tiếp tục.

Mỗi dòng: ngày · repo · feature-slug · vấp gì (1 câu) · chặn-việc? (có/không)

---
2026-08-08 · floorplanstudio · (tiền kiểm) · Harness giữ snapshot skill lúc khởi động: đĩa đã feature-loop 1.27.0 nhưng phiên vẫn nạp 1.26.0, chỉ lộ khi so marker nội dung chứ không phải so số version · chặn-việc: có (phải mở phiên mới) [dựng lại từ handoff phiên 1 — 4 dòng gốc mất sạch, chưa từng commit]
2026-08-08 · floorplanstudio · (tiền kiểm) · `executors.script.product_map` ghim đường dẫn tuyệt đối tới acceptance-gate/1.36.0, nên sau mỗi lần nâng kit cổng đo bằng thước cũ mà không ai đỏ · chặn-việc: không [dựng lại từ handoff phiên 1]
2026-08-08 · floorplanstudio · (tiền kiểm) · `$CLAUDE_PLUGIN_ROOT` RỖNG trong Bash của phiên và của subagent S4, nên khuôn gốc `${CLAUDE_PLUGIN_ROOT}/scripts/product-map.mjs` của acceptance-init thành `/scripts/product-map.mjs` và hỏng · chặn-việc: không [dựng lại từ handoff phiên 1]
2026-08-08 · floorplanstudio · (tiền kiểm) · Sổ vấp phiên 1 khai "4 dòng" nhưng file chỉ còn header và git status kit sạch — ghi rồi mất, không có dấu vết; từ nay ghi xong phải dán lại nội dung file để xác nhận · chặn-việc: không
2026-08-08 · floorplanstudio · (tiền kiểm) · `PRODUCT-MAP.md` chưa từng tồn tại trong repo consumer trong khi config.yaml đã khai miễn trừ cho nó ở t1_skip_globs suốt — nghĩa là executor product_map chưa bao giờ chạy nổi, miễn trừ đứng một mình không ai canh · chặn-việc: không
2026-08-08 · (máy B / cài đặt) · (tiền kiểm) · Bản cài trên máy thứ hai đứng ở 1.37.0/1.25.0 khi nguồn đã 1.39.0/1.27.0, VÀ clone marketplace cũng đứng ở e32e3e6 (07/08) — phải `git pull` clone marketplace TRƯỚC thì `claude plugin update` mới ăn; chưa kiểm được update-đơn-thuần có tự refresh clone không, nên hướng dẫn đội ở GĐ3.2 chưa đủ · chặn-việc: không
2026-08-08 · (máy B / cài đặt) · (tiền kiểm) · Kiểm "đã mới nhất" phải diff nội dung với ĐÚNG vật: marketplace khai `source: ./` (repo root, 7 commands + 5 skills) nhưng mirror `plugins/acceptance-gate/` có layout khác (12 skills) — đối chiếu nhầm mirror sinh báo động giả "thiếu 6 cổng người" · chặn-việc: không
2026-08-08 · floorplanstudio · (tiền kiểm) · Báo cáo cuối phiên ghi wrapper ở commit 4b8e1c8 nhưng sha đó không tồn tại (git cat-file: Not a valid object name), reflog chỉ có cccbd72 — lớp "sha gõ tay" của đợt 10 đổi da từ pin sang báo cáo · chặn-việc: không
2026-08-08 · floorplanstudio · (khói GĐ2) · `/acceptance-status` khai `disable-model-invocation` nên máy KHÔNG tự chạy nổi bước khói 1a — Skill tool chặn thẳng và cấm mô phỏng bằng đường khác, owner phải tự gõ; 1b exit 0 và 1c 415/415 xanh nên đây là chặn ở bề mặt gọi, không phải engine đỏ · chặn-việc: có (bước 1a; vòng chưa mở)
2026-08-08 · floorplanstudio · (khói GĐ2 — đính chính dòng trên) · Thủ phạm là KHỐI DÁN của máy B giao lệnh cổng-người cho máy (vi phạm chính ADR 0002 mà B có trong sổ luật), không phải engine: kit chặn đúng thiết kế, máy A dừng đúng luật khai trước; B kiểm chứng độc lập 1b (exit 0, wrapper giải đúng cache 1.39.0) + 1c (415/415) đều khớp — bài học soạn khối dán: mọi lệnh giao máy phải qua phép thử "đây có phải 1 trong 6 thao tác cổng người không" · chặn-việc: không (gỡ bằng owner tự gõ)
2026-08-08 · floorplanstudio · mcp-cost-guard · S4 vòng 1 REJECT mà KHÔNG phép đo nào trượt — nguyên nhân duy nhất là suite `product-map-check.sh` đỏ vì chính workspace `_acceptance/<slug>/` mới sinh làm PRODUCT-MAP.md lệch; cổng tự làm mình đỏ bằng vật do chính vòng này đẻ ra, tốn trọn một vòng chấm 21 phút · chặn-việc: không
2026-08-08 · floorplanstudio · mcp-cost-guard · Subagent review của S4 SỬA THẲNG cây làm việc thật để thử giả thuyết (thay checkSolveCost trong service/src/app.ts bằng ngưỡng tự chế + thông điệp giả 'x'/'y', hoán fnv1a thành partitionRegion ở barrel); harness bắt được và báo SECURITY WARNING, subagent có hoàn tác nên git status sạch — nhưng nếu nó chết giữa chừng thì kho hỏng và không ai biết vì report vẫn sinh ra · chặn-việc: không (lần này)
2026-08-08 · floorplanstudio · mcp-cost-guard · `human_override` viết dạng khối YAML nhiều dòng làm pre-merge đỏ với "commit chữ ký cũng sửa thân báo cáo" — luật `require_human_commit` chỉ nhận trường người ở dạng MỘT dòng, không tài liệu nào nói trước; phải lùi commit chữ ký, tách lập luận sang evidence/ rồi ký lại · chặn-việc: không (mất 1 lượt commit)
2026-08-08 · floorplanstudio · mcp-cost-guard · `scripts/recheck-evidence.js` do acceptance-init sinh dùng `require()` trong khi repo khai `"type": "module"` → Node coi .js là ESM và ném ReferenceError, nên cổng recheck CHƯA TỪNG chạy nổi lần nào kể từ commit khởi tạo; lỗi có sẵn trong cả hai bản của kit 1.39.0, tính năng đầu tiên đi tới bước này mới lộ · chặn-việc: CÓ (pre-merge đỏ, không merge được cho tới khi vá)
2026-08-08 · floorplanstudio · mcp-cost-guard · `acceptance-init` chỉ chép một phần `lib/`: thiếu `lib/gap-probe.js` nên luật phản biện context sạch in "GAP-PROBE: NOT ENFORCED" và tự hạ xuống advisory — với repo T3 thì đó là một cổng tắt tiếng suốt mà không ai đỏ; cùng lớp với lỗi recheck-evidence (mang cổng vào repo mà không mang đủ đồ) · chặn-việc: không (advisory) [đề bài 1.39.1]
2026-08-09 · kit (1.39.1) · consumer-copy-cjs · CÁCH-LÀM-CŨ còn nguyên trên 1.39.x, owner nhận diện khi xem phiên: bugfix đổi-tên-file phình thành nghi thức T3 trọn bộ (15 eval + 3 vòng S4, 2/3 vòng cháy vì bộ đo của CHÍNH kit P42/P45) · /approve vẫn hỏi PHÚT (món 1a chết cùng 2.0.0) · lint từ vựng chen vào khoảnh khắc cổng · re-pin 30 workspace cho một lần đổi tên file test (món 1d cũng chết cùng 2.0.0) — cả bốn là số liệu thật cho đề bài 2.1/GĐ4, KHÔNG sửa giữa chừng; phần đề bài của phiên B cũng góp phần phình (ra lệnh "workspace như mọi khi" + 4 câu soi kéo thêm phép đo) · chặn-việc: không
2026-08-09 · kit (1.39.1) · consumer-copy-cjs · Phiên chip tự hành viết code TRƯỚC khi Cổng 1 duyệt (contract draft, code đã commit trên worktree) — cổng chặn-merge còn nguyên nhưng cổng định-hình-tiêu-chí mất; đã ghi thành quyết định có ý thức d-20260809T0100Z-ccc4 kèm chi phí đảo; đề bài chip từ nay phải khoá tường minh thứ tự contract-trước-code nếu muốn giữ viên ngọc số 1 trong chế độ tự hành · chặn-việc: không
2026-08-09 · floorplanstudio · (parity 1.39.1) · Chép lại trọn bộ cổng từ kit làm bằng chứng đã ký đi cũ ngay lập tức — `scripts/` và `lib/` nằm ngoài `t1_skip_globs` nên lưới staleness tính chúng là "mã đổi sau khi chấm"; mọi consumer nâng kit đều sẽ dính, không riêng repo này. Hoặc acceptance-init phải dặn "chép xong re-pin", hoặc pre-merge nên bỏ qua chính đường công cụ của nó · chặn-việc: có (chặn commit parity cho tới khi re-pin)
2026-08-09 · floorplanstudio · (parity 1.39.1 — tiếp dòng trên) · Cách gỡ chuẩn cho lớp này là RE-PIN SAU CHÉP (làn máy tại sha commit parity, verified_commit mới, không vòng chấm mới) — đã chạy thật: re-pin lần 2, run_id repin-20260809-mcp-cost-guard-lane2, pre-merge sau đó clean trọn cả staleness lẫn NOT-ENFORCED. Đề bài 2.1: `acceptance-init` phải NÓI RÕ "chép xong phải re-pin" ở đường docs; KHÔNG đi đường miễn trừ scripts/lib khỏi t1_skip_globs — miễn trừ đã bị bác vì fail-silent · chặn-việc: không (đã gỡ)
2026-08-09 · kit (audit hậu-vòng) · consumer-copy-cjs · Đối chiếu với workflow chuẩn: vòng chip tự hành đi đường acceptance-3-phase trực tiếp thay vì /feature-loop nên MẤT chân review (không review-findings.md, không khối ngoài-hợp-đồng ở Cổng 2 — chân này ở vòng consumer bắt 13 finding/2 HIGH) và mất Gate 1.5 dù T3; gốc là đề bài B viết "workspace như mọi khi" mơ hồ — đề bài tự hành phải GỌI TÊN ĐƯỜNG và khai chân S4 trước khi mở · chặn-việc: không [luật đã vào memory giao-thức B]
2026-08-09 · cả hai repo · (audit hậu-vòng) · usage-report.md vắng ở CẢ HAI vòng đã ký — wf-usage là bước khoan-dung (lỗi → WARN đi tiếp) nên chết câm, trục đo chi-phí-per-feature của charter mất dữ liệu 2 ván liền; đúng dạng "nghi thức chết âm thầm" điểm mù #6 · chặn-việc: không [đề bài 2.1: hoặc làm bước này kiểm-chứng-được, hoặc chấp nhận bỏ đo có khai]
2026-08-09 · floorplanstudio · (dọn T1 chữ-trôi) · LỚP MỚI: sửa CHÚ THÍCH trong mã `t3_paths` cũng làm bằng chứng đã ký thành stale — 0 hành vi đổi, ba cổng xanh, vẫn bị chặn merge; hai lần re-pin trước chỉ là công cụ cổng nên tiền lệ không phủ. Owner đặt TIỀN LỆ HẸP 09/08: comment-only trên t3 được re-pin thay vì chấm lại, CHỈ KHI (a) diff lọc chú thích rỗng chứng bằng máy — phép chặt: xoá mọi dòng chú thích ở cả hai phiên bản rồi so, mã còn lại giống hệt từng byte — và (b) làn đầy đủ xanh · chặn-việc: không (đã gỡ, re-pin lần 3) [đề bài 2.1: staleness nên tự phân biệt diff-comment-only bằng máy, đừng bắt người quyết mỗi lần]
2026-08-09 · floorplanstudio · describe-scheme-perf · Ngưỡng gọi-người khai SAI TỪ ĐẦU (≤2) vì điều phối đoán tier T2 trong khi vật là T3 — docblock cần sửa nằm ở packages/core/src/types.ts, một đường t3_paths, nên Gate 1.5 bắt buộc và ngưỡng đúng phải là ≤3. Phân biệt cho số liệu GĐ4: ngưỡng-khai-sai ≠ quy-trình-phình; tier là máy-derive từ đường dẫn nên khai ngưỡng TRƯỚC khi chạy S0 luôn có rủi ro này · chặn-việc: không
2026-08-09 · floorplanstudio · describe-scheme-perf · Đề bài viết "O(n²)" nhưng số đo thật cho O(n²·v²): tại 190 phòng, v=4 → 25ms · v=16 → 114ms · v=32 → 397ms · v=64 → 1457ms; hệ số đỉnh mới là thứ đẩy nó lên hơn một giây. Đề bài chép lại từ sổ nợ mà sổ nợ chỉ ghi một nửa — bài học: dựng số nền bằng máy TRƯỚC khi viết tiêu chí, đừng chép con số từ tài liệu · chặn-việc: không
2026-08-09 · cả hệ · (hành vi owner — owner tự yêu cầu ghi) · Bề mặt lệnh giả định owner ngồi TRONG phiên executor, thực tế owner ngồi ở phiên điều phối và quyết bằng lời tại đó: nói "Duyệt cổng 1" ở phiên B rồi phải nhảy sang phiên A gõ lại (khoá model-invocation đúng, chỗ-đứng-của-lệnh sai) · gõ tên ngắn /signoff hụt 2 lượt · tuyên "không đo phút" một lần mà cổng vẫn hỏi mỗi lượt · đáp cổng bằng câu tự nhiên thay vì nghi thức nhiều-giá-trị — 5 quan sát đầy đủ trong memory hanh-vi-owner-dieu-chinh-command, là spec-nguyên-liệu chỉnh command ở 2.1/GĐ4 · chặn-việc: không
2026-08-09 · floorplanstudio · describe-scheme-perf · Phiên thi hành KẾT LƯỢT ngay sau cổng thay vì chạy tiếp stage kế, dù skill có bất-biến-dừng ghi rõ chỉ dừng ở Gate 1/1.5/2 — mối nối người-trông-máy hiện dựa vào /goal do owner dán hoặc một cú nudge của điều phối, nên khi cả hai vắng thì vòng đứng im mà không ai báo. Đề bài 2.1: feature-loop nên tự đề xuất bước kế như một continuation thay vì đứng im · chặn-việc: không (điều phối nudge)
2026-08-09 · floorplanstudio · describe-scheme-perf · LẦN THỨ HAI subagent của S4 làm điều đáng ngại, hai lần hai loại: vòng mcp-cost-guard reviewer SỬA THẲNG cây thật để thử giả thuyết; vòng này subagent capture:provenance IN RA biến $ACCEPTANCE_GATE_BYPASS khi repo đang enforcement strict, harness xếp là do thám cơ chế vượt cổng. Cả hai lần git đều sạch sau đó, nhưng hai lần trong hai vòng liên tiếp là mẫu chứ không phải xui · chặn-việc: không · đề bài 2.1 CÓ TÊN: subagent chấm chạy trên bản sao chỉ-đọc, không phải cây làm việc thật
2026-08-09 · floorplanstudio · describe-scheme-perf · Chân REVIEW của S4 bắt được lỗ mà 12 phép đo máy lẫn gap-probe đều trượt: thuật toán mới CHẬM HƠN bản cũ 1.7x trên đầu vào đối kháng dồn-mọi-cạnh-một-đường, vì vòng quét chồng-lấn bậc hai theo SỐ CẠNH chứ không theo số phòng. Mọi fixture của tôi đều là bố trí lành nên không ca nào chạm tới. Bài học: phép đo hiệu năng viết từ mô hình bố trí LÀNH không bao giờ tự tìm ra worst case — cần một ca đối kháng khai tường minh trong hợp đồng · chặn-việc: không (đang vá theo đường nâng-phạm-vi)
2026-08-09 · điều phối B · describe-scheme-perf · B phát lệnh "vào S3 ngay" từ trạng thái cũ trong trí nhớ hội thoại trong khi S3 đã xong từ trước lần dispatch chấm đầu (chính tin BLOCKED đã ngầm chứng minh điều đó) — máy A đối chiếu git log trước khi thi hành và TỪ CHỐI, không thì đè lên 8 task đã commit; luật mới cho cả hai phía: B đọc lại vật trước khi phát lệnh giai-đoạn, A luôn đối chiếu lệnh-giai-đoạn với git trước khi chạy · chặn-việc: không (A tự vệ đúng)
2026-08-09 · floorplanstudio · describe-scheme-perf · Ba vấp lớp lặp do A tự khai khi vá AC-13: (1) E13 bản đầu VÔ HIỆU chiều đỏ — đếm lời gọi trọng tài trong khi có-chặn-hay-không con số giống hệt, phải lộ linkOps mới đo được lời hứa (lần 3 trong vòng: đo-thứ-dễ-đếm-thay-vì-thứ-mang-lời-hứa); (2) `timeout` KHÔNG tồn tại trên macOS — 3 lượt phá-vật chết câm, suýt đọc kết quả lượt-đã-hoàn-tác thành lượt-đột-biến → luật mới: MỌI lượt phá vật phải in dấu hiệu XÁC NHẬN đột biến đã áp (grep -c mẫu đã sửa), không tin lệnh chạy trót lọt; (3) suýt tin reviewer về khả năng chạm — cơ chế đúng nhưng đường phơi hẹp hơn, đã ghi hai chiều vào E13 · chặn-việc: không
2026-08-09 · floorplanstudio · describe-scheme-perf · ENGINE CHẶN-VIỆC, hai lỗi kit nối nhau làm S4 KHÔNG THỂ hoàn tất: (1) bước capture:provenance của acceptance-verify.js tự đọc `$ACCEPTANCE_GATE_BYPASS`, bộ phân loại an toàn xếp là do thám cơ chế vượt cổng ở repo enforcement:strict và chặn agent — nay chặn ĐỀU ĐẶN chứ không ngẫu nhiên; (2) bước synthesize dereference `prov.enforcement_mode` KHÔNG có guard nên agent bị chặn trả null là script CHẾT. Hai lượt S4 đốt ~2,7 triệu token và ~35 phút cho 0 verdict. Máy A không tự vá kit theo luật · chặn-việc: CÓ (mọi lượt chấm ở repo strict sẽ chết y hệt cho tới khi vá)
2026-08-09 · floorplanstudio · describe-scheme-perf · Lần THỨ BA subagent chấm bị gắn cờ an ninh trong CÙNG một tính năng: lần này review dời `packages/core/src/zzfuzz.test.ts` ra scratchpad `.bak` để lint/test qua. Kiểm bằng git: cây sạch tuyệt đối, file đó do chính subagent tạo rồi dời, KHÔNG phải test của kho — không mất gì. Nhưng ba lần trong một tính năng (sửa cây thật · do thám biến bypass · dời test) là mẫu chứ không phải xui, và cả ba đều chỉ lộ ra nhờ cảnh báo harness chứ không nhờ cổng nào của kit · chặn-việc: không
2026-08-09 · cả hệ · (hành vi owner #6 — owner tự chất vấn) · Sau lần duyệt thứ 3 trong một đợt vá, owner hỏi thẳng: "tôi là người phê duyệt, gõ đi gõ lại nhiều lần để làm gì?" — bóc tách: phần chịu-lực chỉ là MỘT câu YES nguyên văn trong transcript đúng phiên (thứ duy nhất không giả được; tuần này chặn thật 3 mưu vô ý: khối dán B gọi lệnh cổng, A từ chối uỷ-quyền-qua-tin-nhắn, subagent dò biến bypass); phần còn lại là ceremony cộng dồn (lệnh + câu xác nhận + tên + phút = 3–4 lượt gõ cho MỘT quyết định, phải đúng phiên) · spec 2.1: gộp về một lượt, tên từ config, bỏ phút, gọi từ phiên bất kỳ trỏ đúng repo; số lần duyệt NHIỀU do finding-thật-giữa-vòng là đặc điểm của vòng có review sắc, đừng trộn với ceremony · chặn-việc: không
2026-08-09 · cả hệ · (đánh-giá-lại) · Owner chốt kế hoạch 4 nước sau tạm nghỉ: vòng 2 chấm NGAY qua acceptance-direct (phát hiện mở khoá: đường 3-phase đã chạy tốt trên repo strict — cái hỏng chỉ là workflow feature-loop, không phải cả hệ chấm) → vòng 3 + Cổng Giá trị đầu tiên → kit ĐÓNG tới sau vòng 3 (runner fix + 1.27.2-v2 bản-rẻ suy-biến-về-legacy-shape + mọi món 2.1 xếp hàng chờ) → prune tháp theo tỉ lệ lấy mẫu. Nền: findings bản-chất-thật + kim chỉ nam mục 6 · chặn-việc: không (mở khoá)
2026-08-09 · floorplanstudio · describe-scheme-perf · Vòng chấm 1 (acceptance-direct, 3 chân) trả về 7 phát hiện; BA trong số đó là phép đo KHÔNG THỂ ĐỎ, và không cổng nào của kit bắt được cái nào: (1) tiền đề chống-ngưỡng-rơi-từ-trời của E7 viết bằng công thức đóng `n(n-1)` nên tỉ số luôn 4,05 — xanh kể cả khi mã và fixture bị đập nát; (2) miễn trừ localeCompare đếm DÒNG (6) thay vì LỜI GỌI (7) nên mù cả hai chiều gộp/tách dòng; (3) doc-drift chỉ ghim MỘT câu dài nên hai câu khác trong cùng file vẫn nói chi phí bậc hai ở thì hiện tại và lọt suốt vòng. Cộng bốn lần trước trong cùng tính năng này là BẢY phép đo không-thể-đỏ. Mẫu chung: cả bảy đều do chân review/judge tìm ra, KHÔNG cái nào do luật khai-sinh-phép-đo của kit chặn được, vì luật đó chỉ hỏi "có cặp hai chiều chưa" mà không hỏi "chiều đỏ có chạy trên VẬT hay chỉ chạy trên công thức". Ghi để cân nhắc sau vòng 3, KHÔNG đề xuất việc kit vòng này · chặn-việc: không
2026-08-09 · floorplanstudio · describe-scheme-perf · Chân review vòng 1 lại bắt được lỗi THẬT mà 12 phép đo máy trượt — lần thứ hai liên tiếp trong cùng tính năng. Lần này: chặn-từng-khoá (`sweepCap`) không đủ giữ lời hứa "công dựng chỉ mục bị chặn bởi n²"; họ đối kháng thứ hai (190 phòng × 32 đường DÙNG CHUNG, mỗi khoá đúng 190 phần tử nên không khoá nào vượt trần) cho 574 560 linkOps, gấp 15,9 lần trần công bố. Dữ kiện cho câu hỏi "chân nào đáng tiền": trong tính năng này chân review một mình đóng góp 2/2 lỗi chặn-phát-hành, còn 12 phép đo máy đóng góp 0 — vì mọi fixture máy dựng đều từ bố trí LÀNH · chặn-việc: không
2026-08-09 · floorplanstudio · describe-scheme-perf · Vòng chấm 2 nâng số phép đo KHÔNG THỂ ĐỎ trong một tính năng lên TÁM. Cái thứ tám (E4/AC-4 "không bao giờ liệt một phòng là hàng xóm của chính nó") là ca sạch nhất để nêu làm ví dụ: gỡ CẢ HAI lớp loại-chính-mình trong describe.ts, bộ test vẫn 51/51 xanh exit 0 — vì fixture hình chữ U có hai cạnh cùng đường nhưng hai khoảng KHÔNG chồng lấn, nên nhánh link(a,a) không bao giờ chạy. Fixture được chọn để MINH HOẠ tính chất chứ không để ÉP nó, và không cổng nào phân biệt được hai thứ đó. Đáng chú ý: chiều đỏ của E4 có khai trong evals.yaml — tức luật khai-sinh-phép-đo được TUÂN THỦ trên giấy mà vẫn cho ra ô rỗng, vì không ai chạy thử chiều đỏ đã khai. Đây là dữ kiện cho câu hỏi "kit nên kiểm cái gì rẻ nhất": chạy thật các chiều đỏ ĐÃ ĐƯỢC KHAI rẻ hơn nhiều so với nghĩ ra chiều đỏ mới, và một mình nó bắt được 3/8 ca · chặn-việc: không
2026-08-09 · floorplanstudio · describe-scheme-perf · Hai chân chấm chạy song song, ĐỘC LẬP, tìm ra HAI họ phản ví dụ KHÁC NHAU cho cùng một lời hứa sai (trần n² của AC-13): chân review dựng k=n-1 (bỏ một phòng ra khỏi các khoá tấn công → đồ thị không bao giờ đầy → 568 512, 15,7×), chân verify dựng grouped-lines G=2 (hai nhóm rời nhau dùng riêng 32 đường → đồ thị bị phân hoạch nên cũng không bao giờ đầy → 285 760, 7,92×). Tôi tái hiện cả hai bằng tay, khớp từng chữ số. Dữ kiện cho câu hỏi "hai chân có thừa không": KHÔNG thừa nhưng cũng KHÔNG cộng dồn — cả hai tìm cùng MỘT lỗi gốc qua hai đường khác nhau, tức giá trị nằm ở chỗ chúng độc lập chứ không ở chỗ chúng chia việc. Ngược lại E4 thì chỉ chân verify bắt được · chặn-việc: không
2026-08-09 · floorplanstudio · describe-scheme-perf · Vòng cuối DỪNG HẲN theo điều kiện 5 của owner. Chân review bắt một câu SAI nằm ngay trong đoạn lập luận trung thực của Amendment: bảng ba họ phản ví dụ ghi "Cả ba đo trên mã HEAD chưa đột biến, MÁY A tái hiện bằng tay cả ba, khớp từng chữ số" — nhưng họ many-lines cho 574 560 là số đo TRƯỚC khi chốt dừng-khi-đầy tồn tại; tại HEAD 05677ee tôi đo lại được 17 955, tức DƯỚI cả trần n² cũ. Chính docblock của tôi mười dòng sau đó cũng viết "cả hai họ đầu về 17 955", và hằng số PINNED trong test cũng ghi 17 955 — tức tài liệu tự mâu thuẫn với chính nó trong cùng một file. Bài học đắt nhất của vòng: tôi viết một Amendment về chuyện chữ-phải-khớp-vật và để lọt đúng lỗi chữ-không-khớp-vật vào trong đó, vì tôi chép lại số từ báo cáo vòng trước thay vì đo lại sau khi mã đã đổi. Quy tắc rút ra cho bản thân: mọi con số trích từ vòng chấm TRƯỚC đều phải đo lại tại HEAD hiện tại trước khi đặt vào tài liệu, hoặc phải dán nhãn "đo trên mã lúc đó" · chặn-việc: có
2026-08-09 · floorplanstudio · describe-scheme-perf · Chân chấm verify của vòng cuối HỎNG: stall 600s, watchdog không cứu được, không có phán quyết. Đây là lần thứ ba trong tính năng này một chân chấm chết vì hạ tầng chứ không vì công việc (hai lần trước là acceptance-verify.js crash). Hệ quả thực tế: vòng cuối không có phán quyết độc lập cho E13/E4/E10b, nên kể cả không có phát hiện đỏ thì vòng cũng KHÔNG kết luận được. Ghi để cân nhắc sau vòng 3: tỉ lệ chân chấm chết vì hạ tầng trong tính năng này là 3 trên 7 lượt · chặn-việc: có
2026-08-09 · floorplanstudio · (lớp MỚI — đề bài 2.1) · STALENESS LIÊN-TÍNH-NĂNG: hai feature canh chung một file test. Vòng describe-scheme-perf sửa tests/doc-drift.test.ts và packages/core/src/index.test.ts; đó đúng là hai file mà E10/E11 của mcp-cost-guard (đã ký, đã merge) đang canh, nên bằng chứng của tính năng KIA thành stale dù không ai đụng vào phần việc của nó. Cổng pre-merge chặn merge vì một tính năng thứ ba không liên quan tới việc đang làm. Khác hẳn ba lần re-pin trước (đều là vật của chính tính năng đó đổi). Cách xử đã dùng: chạy lại đúng hai ô tại HEAD (5+8 passed, đại lượng vẫn 14/13) rồi re-pin — KHÔNG áp tiền lệ chỉ-chú-thích vì thay đổi là mã test thật. Đề bài cho 2.1: khi nhiều feature khai chung một path, kit nên (a) cảnh báo ngay ở Cổng 1 rằng path này đã có chủ, hoặc (b) cho re-pin liên-tính-năng thành một nghi thức khai sẵn thay vì để mỗi lần tự chế · chặn-việc: có
2026-08-09 · floorplanstudio · describe-scheme-perf · (BẰNG CHỨNG DƯƠNG cho lớp thẻ-là-lưới-thật) Thẻ mặt người bắt được HAI lỗi mà ba cổng máy, một chân verify và một chân review đều không bắt: (1) cột phán quyết ghi "PASS (carry r4)" trong khi script so khớp đúng chuỗi "PASS", nên thẻ hiển thị cho owner "11 phép kiểm CHƯA đạt" — nếu không render thẻ thì owner ký một gói mà chính thẻ của nó nói là hỏng; (2) khối bằng chứng gộp 11 eval vào một mục nên không mục nào đủ trường, thẻ bật cờ "bằng chứng máy CHƯA đủ trường". Cả hai đều là lỗi của tôi trong artifact, không phải lỗi mã. Dữ kiện cho GĐ4 khi cân cổng nào giữ: thẻ không chỉ là lớp trình bày, nó là phép kiểm CUỐI trên chính hồ sơ — và nó rẻ (một lệnh render) · chặn-việc: không
2026-08-09 · floorplanstudio · (vấp của MÁY A, lặp lại lớp cũ) · Nghi thức re-pin đòi dòng run-log ở dạng JSON NÉN, và cổng tìm làn bằng grep chuỗi cố định `"run_id":"..."` không dấu cách. json.dumps mặc định chèn dấu cách sau dấu hai chấm, nên mọi dòng tôi ghi đều đúng nội dung mà cổng không thấy — mất hai vòng chẩn đoán. Cùng lớp với "chép bộ cổng xong phải re-pin": tôi biết luật nhưng không biết luật được CƯỠNG CHẾ bằng cách nào, nên tuân thủ ý mà trượt hình thức. Đề bài cho 2.1: cổng nên nói thẳng "dòng có run_id đó tồn tại nhưng không khớp khuôn nén" thay vì "làn chưa từng được ghi" — thông điệp hiện tại chỉ sai hướng người đọc · chặn-việc: có
2026-08-09 · floorplanstudio · describe-scheme-perf · (vấp của MÁY A, LỚP LẶP LẠI LẦN BA trong một vòng) Cổng re-check bắt hai lỗi khuôn trong evidence-report tôi tự viết: (1) trường `verifier` ghi lệnh thô `npx vitest run ...` thay vì khoá `config:executors.test.api` đã khai trong evals.yaml; (2) 14 `run_id` tôi tự chế trong báo cáo mà không có dòng tương ứng nào trong run-log — đúng thứ luật "không hand-mint run_id" sinh ra để chặn. Cộng lỗi JSON-nén trước đó là BA lần cùng một lớp trong một vòng: tôi viết artifact theo Ý của nghi thức mà không theo KHUÔN mà cổng cưỡng chế, vì tôi không đọc bên-đọc trước khi viết bên-ghi. Cách sửa đúng đã dùng: CHẠY LÀN THẬT rồi ghi 13 dòng run-log tại thời điểm chạy, KHÔNG chép ngược id vào sổ cho khớp. Đề bài cho 2.1: khuôn evidence-report nên có bản mẫu máy-sinh hoặc một lệnh `--scaffold` — mọi lỗi trong ba lần này đều là lỗi khuôn mà một bản mẫu sẽ chặn từ đầu, và cả ba đều chỉ lộ ra ở cuối vòng khi mọi thứ khác đã xong · chặn-việc: có
2026-08-09 · floorplanstudio · describe-scheme-perf · VÒNG 2 KHÉP: pre-merge exit 0, cả hai slug OK, owner ký 2026-08-09. Tổng kết số để GĐ4 cân: 6 lượt chấm (3 lượt chết vì hạ tầng, không vì công việc) · 4 lỗi chặn-phát-hành, cả 4 do chân chấm ĐỘC LẬP tìm ra, 0 do phép đo máy · 8 phép đo không-thể-đỏ · 3 câu số sai trong hồ sơ · 2 lỗi khuôn artifact do thẻ mặt người và cổng re-check bắt · 5 lần re-pin (2 lần thuộc lớp MỚI staleness liên-tính-năng) · 479k token đo được cho các chân chấm · 3 lần gọi người (ngưỡng ≤2). Điều đáng nói nhất: KHÔNG lỗi nào trong 4 lỗi chặn-phát-hành do cổng nào của kit bắt được — kit bắt được lỗi HỒ SƠ (khuôn, stale, thiếu trường), còn lỗi VẬT thì do chân chấm độc lập bắt. Hai loại giá trị khác nhau, nên đừng gộp khi tính chi phí · chặn-việc: không
2026-08-10 · floorplanstudio · describe-scheme-perf · KHÉP VÒNG 2, merge 4e615b2, CI xanh (gate 7s · test 1m4s), owner ký 2026-08-10T01:13:42Z. Số tổng để GĐ4 cân, và chúng nói ngược nhau nên đừng gộp: (a) GỌI NGƯỜI 3 lần trên ngưỡng ≤2 — phân loại 1 finding-thật-buộc-dừng + 1 hạ-tầng-hỏng + 1 ký, KHÔNG có lần nào quy-trình-phình; (b) 8 PHÉP ĐO KHÔNG-THỂ-ĐỎ trong một tính năng, cả 8 do chân chấm độc lập tìm, 0 do cổng kit; (c) 3 lần CHÂN CHẤM CHẾT VÌ HẠ TẦNG trên 7 lượt (2 lần acceptance-verify.js crash, 1 lần subagent stall 600s) — tức 43% lượt chấm không cho phán quyết vì công cụ chứ không vì công việc; (d) THẺ MẶT NGƯỜI bắt 2 lỗi mà ba cổng máy + chân verify + chân review đều không bắt, trong đó có lỗi khiến thẻ tự báo "11 phép kiểm CHƯA đạt" — bằng chứng dương cho lớp thẻ-là-lưới-thật, và nó rẻ (một lệnh render); (e) ERRATA THAY AMENDMENT tiết kiệm đúng 1 lượt duyệt: hợp đồng giữ nguyên, ba câu sai hiện nguyên văn trên thẻ dưới dạng cờ đỏ, owner ký có mắt thấy thay vì phải duyệt lại một bản sửa. Kết luận một câu cho 2.1: kit bắt tốt lỗi HỒ SƠ (khuôn, stale, thiếu trường, hand-mint run_id — bắt 4 lỗi của MÁY A ở đoạn cuối), nhưng KHÔNG bắt được lỗi VẬT lần nào; giá trị hai loại khác nhau và chi phí cũng khác nhau, tính chung là tính sai · chặn-việc: không
2026-08-10 · điều phối A↔B · (vận hành liên phiên) · Phiên A đôi chỗ KHÔNG phản hồi tin của B (owner quan sát): tin B→A xử lý xong nhưng lượt kết thúc không send_message về, B phải poll — kênh một chiều lúc có lúc không · luật mới vào giao thức: mọi tin B→A kết bằng dòng "xác nhận đã nhận + việc kế" bắt buộc; A kết lượt mà còn tin B chưa trả lời → phải send_message trước khi kết; B poll fallback sau 15-30 phút im lặng; đề bài mọi phiên A nhúng luật này · chặn-việc: không (B poll bù được)
2026-08-10 · floorplanstudio · refine-editor · Thẻ Cổng Đáng chưa có đường máy: gate-card.js 1.39.1 (cả plugin cache lẫn kit main) không có chữ nào về opportunity/gate0, trong khi opportunity-template hứa "card/funnel Cổng 0 sẽ đọc" frontmatter — A render thẻ tay phía consumer theo human-facing-language.md (nạp đủ N1–N6), kit đóng không vá · chặn-việc: không (đi vòng bằng thẻ tay)
2026-08-10 · cả hệ · (hành vi owner #8 — owner góp ý trực tiếp) · Lời-gọi-hành-động của máy A với owner đôi khi không rõ: việc-cần-làm rải giữa thân bài dài, câu tu từ lẫn câu hỏi thật, không nói trả-lời-dạng-gì → owner tốn nhận thức để hiểu và đáp ứng đi tiếp (ví dụ thật: 6 quyết trả lời đủ mà dòng tick park đầu thẻ bị sót) · luật mới đã phát cho phiên A: mọi tin kết bằng MỘT khối "👉 VIỆC CỦA ANH" — mỗi mục đủ 3 vế làm-gì/ở-đâu/trả-lời-dạng-gì, kèm câu mẫu trả-lời-gộp; tin chỉ-báo ghi rõ "không cần làm gì"; câu tu từ cấm mang dấu hỏi · spec 2.1: khối này thành phần CỨNG của khuôn card + khuôn mời-cổng, máy render tự sinh · chặn-việc: không
2026-08-10 · floorplanstudio · digitize-floorplan · Gap-probe viết dạng văn xuôi → gate-card extract 0 dòng và render thẻ "SẠCH" trong khi file có 10 finding — máy A tự bắt trước khi mời owner và đưa file về khuôn bảng; nửa lỗi là người-viết-sai-khuôn (bài học đề bài: gap-probe viết đúng khuôn bảng từ đầu) nhưng nửa kia là KIT FAIL-SILENT: extractor bóc 0 dòng từ file khác-rỗng phải kêu "không đọc được" thay vì im lặng ra thẻ sạch — lớp phép-đo-mù-với-định-dạng, cùng họ NOT-ENFORCED-câm · đề bài 2.1: gate-card cờ đỏ khi extract-rỗng-mà-file-không-rỗng · chặn-việc: không (A tự bắt kịp)
2026-08-10 · floorplanstudio · digitize-floorplan · DOER TỰ CHẤM "KHỚP MẮT": A dựng fixture rồi tự tuyên "khớp mắt, giữ nguyên" ở bước dogfood — owner mở bản chạy thật, khoanh đỏ ranh giới, overlay lệch xa tường trong khi MỌI cổng máy xanh (validator sạch, diện tích −6,7% trong trần 7%). Đúng nguyên văn finding C-2 của gap-probe lượt trước ("hoàn thành kịch bản đo được cả khi kết quả vô dụng") · dữ kiện #3 bảng giá-trị-theo-tầng: MẮT NGƯỜI BẮT CÁI MỌI CỔNG MÁY TRƯỢT — điểm neo cho reflect kế, và là lý lẽ sống cho design-pass/UAT · luật rút: mọi tuyên bố "khớp mắt/nhìn ổn" phải do người KHÁC người dựng nói; agent dựng không được phép tự khai đạt về thị giác · hệ quả kèm theo: sửa cho đúng hình học mới lộ ra AC-3 so tim-tường với NSA có thiên lệch định nghĩa +8,1% (đổi mẫu so sang GSA, không nới ngưỡng) · chặn-việc: không (đã sửa trong vòng)
2026-08-10 · floorplanstudio · digitize-floorplan · DỮ KIỆN #4 mắt-bắt-máy-trượt — HAI ĐƯỜNG XUẤT MÂU THUẪN MÀ MỌI CỔNG XANH: tầng số hoá đảo trục Y "cho đúng toán học", renderSvg của core vẽ thẳng toạ độ Scheme (y-xuống) → LỚP ĐÈ khớp ảnh trong khi BẢN VẼ VẼ LẠI lộn ngược (lô gia đáng ở trên thì nằm dưới). Validator sạch, area-check trong trần, design-gate 0 P0, 34 test xanh — không cổng nào đỏ vì KHÔNG PHÉP ĐO NÀO SO HAI ĐƯỜNG XUẤT VỚI NHAU; chỉ owner đối chiếu bố cục với bản gốc mới thấy · bài học lớp: sản phẩm có N đường xuất thì phải có phép đo NHẤT-QUÁN-LIÊN-ĐƯỜNG (cùng họ parity hai-bề-mặt của mcp-cost-guard), và mỗi lần mắt bắt được gì thì biến ngay thành cơ chế rẻ giữ mãi — orientation.test.ts sinh từ chính bug này, xác nhận đột biến 4/4 đỏ khi đảo lại · kèm theo: lộ ra thước AC-4/5 gắn vào DỤNG CỤ (lớp đè) chứ không vào VẬT ĐƯỢC GIAO (bản vẽ) → Amendment AC-10 · chặn-việc: không
2026-08-10 · floorplanstudio · digitize-floorplan · PRE-MERGE CHẶN VĨNH VIỄN BỞI FEATURE CŨ: sau khi vòng 3 ký Cổng 2 và evidence của chính nó đã sạch (recheck rc=0, pin đúng HEAD), `pre-merge-check.sh --base main` vẫn chặn với 2 VIOLATION "evidence is stale" của describe-scheme-perf và mcp-cost-guard — hai feature ĐÃ signed-off và ĐÃ MERGE ở vòng trước. Cơ chế: stale-check áp cho MỌI slug trong _acceptance/, so verified_commit của slug với mọi file code đổi sau đó; mà một nhánh feature mới thì ĐƯƠNG NHIÊN đổi code sau verified_commit của các feature cũ → mọi PR từ vòng 2 trở đi đều bị chặn bởi lịch sử, không phải bởi lỗi của mình · hệ quả vận hành: ván đường-A không merge được nếu không (a) re-verify lại toàn bộ feature cũ mỗi vòng (đắt, và vô nghĩa vì chúng đã ship), hoặc (b) kit chỉ áp stale-check cho slug NẰM TRONG diff của PR (giống cách gap_probe advisory đã làm), hoặc (c) coi slug có contract signed-off + đã merge là "đóng băng", miễn stale-check · A KHÔNG tự vá (kit đóng) — B quyết 2026-08-10: NGAY thì đi tiếp uat-session sau flag (không cần merge); khi cần merge thì bắc cầu bằng RE-PIN hai slug cũ tại HEAD (nghi thức có tiền lệ 4 lần); LỚP SỬA THẬT là phương án (b) — ĐÃ PHÂN TÍCH + B CHUẨN Y, đứng ĐẦU danh sách kit 2.1, không vá bây giờ vì khép vòng 3 mới là điều kiện mở 2.1 đúng luật. Mỗi PR từ vòng 2 bị chặn bởi lịch sử — TALLY 2 · chặn-việc: CÓ (merge bị chặn; vòng vẫn đi tiếp được tới uat-session vì ship-sau-flag không cần merge main)
2026-08-10 · floorplanstudio · digitize-floorplan · CỔNG GIÁ TRỊ CHẠY LẦN ĐẦU — VÀ NÓ DÁM NÓI KHÔNG: lát mỏng qua trọn 15 eval máy, ký Cổng 2, ship sau flag; phiên nghiệm thu chấm kín cho C1 (giữ đúng cấu trúc) 5/5 nhưng C2 (dùng được để mô phỏng cho khách) KHÔNG và câu ràng buộc "gửi cho khách nào, khi nào" trả lời "Chưa được" → ngưỡng vỡ, owner ký KILL · bài học một dòng: MÁY DỰNG ĐÚNG HÌNH CHƯA ĐỦ ĐỂ ĐƯA KHÁCH XEM — C1 tối đa không bù được C2, cấm cộng-trừ hai thước khác bản chất · giá của câu trả lời: 1 vòng dựng (~95 phút máy, 4 lần gọi người) thay vì một quý phát hành · chặn-việc: không (kill là kết cục hợp lệ)
2026-08-10 · floorplanstudio · digitize-floorplan · ĐO ĐÚNG MÀ NÓI KHÔNG RÕ: cả vòng tranh luận để chốt quy ước tim-tường, phát hiện thiên lệch định nghĩa 8,1%, sửa mẫu so NSA→GSA — vậy mà lý do #2 owner nêu khi chấm là "chưa rõ ràng vùng là bao gồm tường hay chưa bao gồm tường". Phép đo đúng nhưng MÀN HÌNH KHÔNG NÓI RA quy ước, nên người dùng không dùng được · bài học lớp: mỗi phép đo đã chốt phải có MẶT NGƯỜI-ĐỌC trên UI (một dòng "đo theo tim tường, có tính lô gia" cạnh con số) — ngưỡng sống trong hồ sơ là chưa đủ, nó phải sống trên màn hình · chặn-việc: không
2026-08-10 · floorplanstudio · digitize-floorplan · LỖ TIÊU CHÍ KHÔNG-LOẠI-CÓ-Ý-THỨC: xoá/gộp phòng và các thành phần kiến trúc (hệ cửa, lô gia, ban công, cửa kính) KHÔNG nằm trong AC nào mà cũng KHÔNG nằm trong Out of scope — người dùng đầu tiên đâm vào cả hai ngay lần thử đầu · đề xuất khuôn: opportunity/contract cần ô BẮT BUỘC "đã-loại-CÓ-Ý-THỨC" (khác với "không nghĩ tới"), để lúc soát thấy ngay khoảng trắng giữa in-scope và out-of-scope · chặn-việc: không
2026-08-10 · floorplanstudio · FEEDBACK OWNER cần điều tra ở tổng kết (GHI NGUYÊN TRẠNG, CHƯA KẾT LUẬN): design-pass ở mức-3 (UI thật) khiến prototype và sản-phẩm-bàn-giao LÀ MỘT. Câu hỏi phải trả lời ở reflect: một-mặt-phẳng nói làm-trên-vật-thật là đúng, nhưng nếu prototype ≡ sản phẩm thì design-pass còn giữ được vai KIỂM ĐỘC LẬP TRƯỚC BÀN GIAO không, hay nó tự biến thành một vòng build khác? Kiểm lại ngữ nghĩa thang vật liệu (real-components/scaffold/static) dưới ánh sáng đó · chặn-việc: không
2026-08-10 · cả hệ · (tổng kết GĐ2 — owner truy vấn) · Owner bắt trúng: proto==product ở ván 3 đồng nghĩa BỎ giai đoạn thiết kế UX/UI — S1-D bị vi phạm không ai kêu (Gate 1 duyệt trên chữ, UI dựng sau plan, design-pass thoái hoá thành QA-mắt hậu-build; mọi quyết định UX do agent quyết ngầm khi gõ) và hoá đơn là 3/4 lý do C2 chết đều lỗi thiết-kế-thuần không phải lỗi code · gốc: build rẻ (95') làm dựng-luôn có vẻ rẻ hơn thiết-kế-đã — hệ luận North Star mới: LÀM rẻ đi trăm lần thì giá trị dồn về KHAI, sai-tầng-ý-định là loại sai duy nhất còn đắt · sửa = UN-SKIP bước spec có sẵn: S1-D trước Gate 1 trên vật liệu thật + grill-thiết-kế + câu-C2-sớm cuối design-pass · chặn-việc: không [amend vào tổng kết + đề bài 2.1/spec]
2026-08-10 · floorplanstudio · KHÉP SỬ LIỆU — LUẬT BẢN CHỐT (tiền lệ cho nghi thức khép-sử-liệu 2.1): feature MERGED → hồ sơ về main bình thường; feature KILLED → tag archive giữ trọn (hồ sơ + mã + bằng chứng + chữ ký) và main chỉ nhận MỘT file tường thuật `docs/archive/<ngày>-<slug>-kill.md`, TUYỆT ĐỐI KHÔNG dựng con trỏ trong `_acceptance/`, và KHÔNG vận chuyển chữ ký · vì sao: (a) bằng chứng đã ký mô tả một cây mã main không bao giờ nhận — chép về là bịa ngữ cảnh; (b) `require_human_commit` đọc lịch sử NHÁNH ĐÍCH nên mọi lần copy-hồ-sơ-đã-ký đều vấp "commit introducing human_signoff also edits body" — tách-hai-commit gỡ được nhưng là diễn-lại-chữ-ký, không nên thành nếp; (c) recheck đòi verifier tái lập được TRÊN CÂY ĐANG XÉT, mà script đo ở lại nhánh → mang script về chỉ để qua cổng là BỊA-MÀU-XANH · đường đã đi và kết quả: main 6ea25c9 pre-merge CLEAN trọn, tag archive/digitize-floorplan-vong3 @261a197 vĩnh viễn
2026-08-10 · kit · lib/workspace-record + product-map · ENUM ARCHIVED CÓ MÀ KHÔNG CÓ ĐƯỜNG TIÊU THỤ: `stage` của opportunity khai enum discovery|decided|archived, nhưng `classify` không có nhánh cho `archived` → workspace khép rơi vào ô "Đang cân nhắc cơ hội" — bản đồ NÓI SAI cho chính hồ sơ đã đóng. Thêm một tầng: ô kết cục UAT chỉ hiện khi `usesUat(contract)` TRUE = contract có `status: signed-off`, nên mọi con trỏ archived (cố ý không mang contract) đều mất ô kill · A KHÔNG ép khuôn (không dựng contract giả để dụ map, không vá kit) — chọn đường không-cho-map-thứ-để-đọc-sai · 2.1 queue: dạy classify nhánh `archived` + khuôn con trỏ archived; phân tích này là spec sẵn · chặn-việc: không (đi vòng bằng docs/archive)
2026-08-10 · kit · stale-theo-diff-pr (chip ① 2.1, PR #38) · TEST PHỤ THUỘC MÔI TRƯỜNG — VC10b đỏ oan trên CI: fixture dùng `commit-tree` không mang $GIT_ID, CI không có git identity → "empty ident name" → orphan base không được tạo, exit 2 thay vì 1; local xanh vì máy dev có identity (run 31383617449, fix 1 dòng 7fa355c) · bài học lớp: fixture git trong test phải TỰ MANG ident, đừng thừa kế môi trường; làn re-pin sau đó chạy suite scripts dưới GIT_CONFIG_GLOBAL=/dev/null để giả lập đúng điều kiện CI đã cắn — mẫu tái dùng cho mọi test đẻ repo tạm · chặn-việc: không (CI chặn đúng vai, fix trong giờ)
2026-08-10 · kit · stale-theo-diff-pr (chip ① 2.1) · LÀN RE-PIN LỘ MAP CŨ: làn chạy sau fix VC10b đỏ thêm P42/P45/P122/P126 + product_map — cùng MỘT gốc: PRODUCT-MAP.md chưa vẽ lại sau khi hồ sơ nhảy signed-off (lớp cổng-tự-làm-mình-đỏ-bằng-vật-vòng-này-đẻ-ra, tái diễn đúng ca mcp-cost-guard dòng 26 — TALLY 2) · bài học: mọi ĐỔI-TRẠNG-THÁI hồ sơ (implemented/verified/signed-off) phải kéo theo vẽ-lại-map NGAY trong cùng lượt, đừng đợi làn sau bắt; ứng viên 2.1: bước đổi-status tự nhắc "map cần vẽ lại" · chặn-việc: không (vẽ lại 99d1ea5, làn 2 sạch [0,0,0,0,0,0])
2026-08-11 · kit · khoi-viec-cua-anh (chip ② 2.1, PR #39) · HAI LUẬT ĐÚNG GẶP NHAU thành hình dạng lặp: PR chạm SKILL hai harness → fixture code-sinh của hồ sơ CŨ (s4-scope-triage, stop-patching-law) sinh lại từ cây mới → file hồ sơ cũ VÀO diff PR → luật stale-theo-diff (chip ①) đòi ghim lại — KHÔNG phải nợ-lịch-sử, là hệ quả đúng của fixture-phải-code-sinh; chi phí một làn re-pin 1-làn-N-chữ-ký (run_id repin-chip2-merge-20260810T233824Z, cùng run ghim 2 slug) · dự báo: mọi PR chạm SKILL/chỉ-dẫn dùng chung sẽ lặp hình dạng này — tính giá làn vào giá PR, đừng ngạc nhiên · chặn-việc: không
2026-08-11 · kit · khoi-viec-cua-anh (chip ②) · NẾP CHỮ-KÝ HAI CHIP KHÁC NHAU: chip ① commit-người 1-file-1-dòng (ce97c3c, status đổi ở commit máy riêng ae6b6d4); chip ② commit-người gộp 4 file (1e852ac: chữ ký + contract status/Notes + ledger + product-map) — recheck lẫn pre-merge đều cho qua cả hai, tức chốt cưỡng chế chỉ soi evidence-body, phần còn lại là NẾP chưa thành luật · ghi để 2.1 quyết một nếp (khuyến nghị B: chuẩn hoá 1-file-1-dòng như chip ① — hẹp nhất, dễ audit nhất) hoặc tuyên bố rõ vùng-tự-do · chặn-việc: không
2026-08-11 · kit · mot-luot-go-cong-nguoi (chip ③, tại cổng ký) · CHỮ KÝ TỐN BA LƯỢT HỎI-ĐÁP: câu gộp owner gõ có cụm «không cắt» hai nghĩa → luật (d) chip ③ kích đúng thiết kế nhưng thi hành bằng CÂU HỎI MỞ, cộng quyết-③b phải gõ lại trong phiên vì tin liên phiên không mang thẩm quyền → owner phát tín hiệu gate-fatigue «phức tạp quá» ngay tại cổng của chip giảm-phức-tạp · owner đặt nguyên tắc gốc (ghi memory nguoi-chi-khai-dieu-chi-nguoi-biet): MÁY GÁNH NHẬN THỨC, NGƯỜI GIỮ QUYẾT ĐỊNH — mơ hồ thì máy nêu cách hiểu khả dĩ nhất + căn cứ + xác nhận một chạm; hỏi mở là đường cùng (50/50 thật hoặc sai-thì-đắt); ranh với bất biến chip ②: máy được gánh SUY NGHĨ, không được phát ngôn hộ QUYẾT ĐỊNH · đề bài ③b (task_ce6c90bf) gộp cả hai nguyên tắc; reflect kế thêm câu: luật hỏi-lại đang cứu hay đang hành · chặn-việc: không (ký vẫn đi tiếp trong phiên)
2026-08-11 · kit · mot-luot-go-cong-nguoi (chip ③, sau ký) · LỜI-NGƯỜI-DÀI ≠ TRƯỜNG-CHỮ-KÝ: commit chữ ký đầu của A lẫn body (đổi ô Per-eval UNCERTAIN→PASS + viết lời nguyên văn owner NHIỀU DÒNG vào human_override) → pre-merge chặn đúng (luật commit-chữ-ký chỉ cho chạm dòng `human_signoff|human_override|verdict|bypass_ack`, một dòng) · xử mẫu ĐẸP đáng thành nếp: lời người dài về khoá body `human_evidence: |` trong commit MÁY trước; ô Per-eval giữ nguyên trạng máy chấm «UNCERTAIN (người override → Đạt)»; human_override rút một dòng trỏ human_evidence trong commit NGƯỜI · bài học lớp: nơi của LỜI là thân báo cáo, nơi của CHỮ KÝ là trường một dòng — nhét lời vào trường là vi phạm nghi thức tách commit · chặn-việc: không (lưới bắt, sửa trong phiên, không cần người)
2026-08-11 · kit · mot-luot-go-cong-nguoi (chip ③, CI PR #41) · HỒ SƠ MỚI LUÔN LÀ INPUT CỦA SUITE QUÉT-TOÀN-KHO: CI lượt đầu đỏ W-G8 — E7 judgment thiếu `inputs:`, bắt bởi suite workflows (quét toàn _acceptance/) trong khi mọi eval máy của hồ sơ chỉ trỏ suite plugins → ba suite còn lại không phép đo nào gọi local, lớp "phép đo không phủ chỗ mình vừa chạm" · xử mẫu ĐÚNG: khai inputs theo sự-thật-lần-chạy-judge (2 file thân lệnh hội đồng đã đọc), TỪ CHỐI nới danh sách miễn trừ (nới = hạ thước); chữ ký đứng nguyên, ghi Iterations + d-10009 · QUYẾT B (theo nguyên tắc lưới-thay-câu-hỏi): CI là lưới chính thức của lớp này — KHÔNG mở chốt engine mới; đổi lại thành NẾP-TRONG-ĐỀ-BÀI mọi chip kit tự-host: trước khi mời ký chạy trọn 4 suite local một lần (chip có làn re-pin được sẵn miễn phí); giá đã trả nếu bỏ nếp: 1 vòng CI đỏ ~7 phút máy, 0 phút người · chặn-việc: không

2026-08-11 · kit · may-ganh-nguoi-quyet (chip ③b, mốc 2) · ĐỀ BÀI MƠ HỒ SINH NHỊP LỆCH — LỖI CỦA B: đề bài viết «contract-trước-code · hồ sơ Cổng 1 COMMIT trước khi mời owner» và phiên A tuân ĐÚNG CHỮ: commit hồ sơ rồi THI CÔNG LUÔN trước khi owner duyệt Cổng 1 — trong khi nếp lịch sử chips ①–③ là DUYỆT-trước-thi-công để cổng không chịu áp lực chi-phí-chìm · xử: không bác (giờ máy, git đảo rẻ) + thẻ Cổng 1 buộc khai «thi công đã chạy trước duyệt, bác thì code bỏ theo, không có chi phí giữ» + B sửa chữ đề bài mọi chip sau thành «DUYỆT Cổng 1 trước thi công» · câu cho reflect: với chip đo-được-rẻ, dựng-trước-duyệt cho owner thẻ Cổng 1 GIÀU bằng chứng hơn (gần tinh thần S1-D duyệt-trên-bản-bấm-được) — nhịp nào đúng cho lớp nào? · chặn-việc: không
2026-08-11 · kit · may-ganh-nguoi-quyet (chip ③b, S4 3 vòng) · LỜI HỨA GỠ PHẢI CÓ NEO ÂM: gap-probe context-sạch chạy TRONG S4 bắt 2 P0 của chính bộ thước — (i) E1–E4 treo trên mã-thoát-trọn-suite nên xanh cả trên origin/main (không phân biệt cây cũ/mới, cùng họ needle-đã-tồn-tại ②b); (ii) chip hứa GỠ một câu hỏi mà không neo ÂM nào assert sự-vắng-mặt → sửa THƯỚC (răng ghim-dòng + 3 neo âm + mutant chèn-lại), không hạ đáp án · kèm 2 lớp phụ cùng ván: hai-bản-chép-trong-một-thân (bậc thang khai 2 lần/file → mọi vá trúng một bản; giải bằng khai-một-lần-trỏ-về + thước đếm ==1, MUT-9) và neo-âm-quét-cả-lối-phổ-biến (2 SKILL acceptance hai harness vẫn hỏi phút cho đúng trường 6 lệnh vừa thôi hỏi — vá lệnh mà bỏ skill là lời hứa sai ở đường phổ biến nhất; trình Ngoài-hợp-đồng xin một chạm) · chặn-việc: không (3 vòng trong trần, mỗi vòng sửa vật/thước)
2026-08-11 · cả hệ · (owner phát tín hiệu tại Cổng 2 chip ③b) · QUÁ TẢI Ở TẦN SUẤT, KHÔNG Ở TỪNG CỔNG — owner nguyên văn: «Đây là điển hình của quá tải nhận thức đối với tôi. Kit trở thành một chi phí lớn» khi nhận khuôn 5-slot kèm khuyến nghị từng mục. Nghịch lý đo được: 4 chip 2.1 làm TỪNG cổng rẻ đi nhưng dây chip làm TẦN SUẤT gọi-người tăng (trong ~36h: 4×duyệt + 4×ký + 4×merge + 4×bấm chip + các lượt làm rõ) — tổng tải dồn về một người; B cũng góp lỗi: khối VIỆC-CỦA-ANH đa mục + nhắc lặp món treo mỗi tin · LỚP SỬA (đề bài, chưa thi công): (a) VETO-DEFAULT — mục máy-chắc-chắn-và-đảo-rẻ thì nêu mặc-định-kèm-căn-cứ rồi LÀM trừ khi bị bác trong cửa sổ, chỉ mục phán-xét/trách-nhiệm mới cần phát ngôn chủ động (chấp-thuận-trọn-gói «Theo khuyến nghị B; Ký» là bước chuyển tiếp hợp lệ — người vẫn phát ngôn cuối); (b) GOM LƯỢT — dồn nhiều quyết vào một lần ngồi thay vì rải; (c) B đổi nếp tin: một-tin-một-việc, không nhắc lặp món treo · chặn-việc: KHÔNG về code, CÓ về người — đây là ràng buộc số 1 tái xuất hiện dưới dạng mới (gate-fatigue → call-frequency-fatigue) · chặn-việc: không
2026-08-11 · kit · may-ganh-nguoi-quyet (chip ③b, S4) · EVAL TREO TRÊN MÃ THOÁT CỦA TRỌN SUITE = EVAL KHÔNG PHÂN BIỆT: 4 eval trỏ `config:executors.test.plugins` nên verdict = exitCode cả suite — mà suite trên origin/main CŨNG exit 0, tức 4 eval xanh y hệt trên cây cũ lẫn cây mới (đúng định nghĩa nonDiscriminating của chính runner) · xoá/đổi tên khối case thì suite vẫn xanh và 4 eval vẫn PASS trong khi KHÔNG phép đo mới nào chạy · chữa bằng script RĂNG ghim đúng dòng của case trong stdout (PASS: <case> + các dòng đếm + số chiều đỏ + tổng-kết-phải-khớp-số-mutant-đếm-được), chiều đỏ chứng minh: đổi tên case trong bản sao → suite XANH mà răng ĐỎ · dự báo: mọi hồ sơ kit tự-host dùng suite-key chung đều mang lỗ này — kiểm bằng câu hỏi «chạy eval này trên origin/main có đỏ không?» · chặn-việc: không
2026-08-11 · kit · may-ganh-nguoi-quyet (chip ③b, hậu ký) · VẼ LẠI BẢN ĐỒ SẢN PHẨM PHẢI SAU KHI GHI CHỮ KÝ: nếp commit-máy-trước khiến A regen PRODUCT-MAP ở commit máy, rồi commit NGƯỜI mới đổi contract status → bản đồ stale ngay sau chữ ký, P122/P126 đỏ và răng P194 đỏ theo · luật signoff bước 6 đã nói đúng (regen SAU khi human_signoff được ghi, map thuộc CHÍNH commit chữ ký) — A đọc lướt vì thứ tự tự nhiên của nếp tách-commit là máy-trước · xử: regen + amend vào commit chữ ký (chưa push nên rẻ) · nếp: sau MỌI chữ ký, chạy lại 4 suite TRƯỚC khi push, đừng tin màu xanh đo trước lúc ký · chặn-việc: không
2026-08-11 · kit · (main, sau merge chip ③b) · SQUASH-MERGE PHÁ NGHI THỨC HAI-COMMIT: PR #42 merge kiểu SQUASH → toàn bộ hồ sơ gộp vào MỘT commit e943f96 vừa mang human_signoff vừa dựng thân báo cáo → luật require_human_commit đỏ VĨNH VIỄN trên main, chặn MỌI PR sau đó (CI main đỏ 3 lượt liên tiếp; PR #43 bump 1.40.0 bị chặn oan) · đối chứng: 4 PR trước (#38–#41) merge kiểu merge-commit (2 cha) đều sạch; chỉ #42 có 1 cha · gốc lớp: nghi thức tách-commit-chữ-ký sống nhờ HẠT commit, mà chiến lược merge có quyền xoá hạt đó — luật đúng, hạ tầng merge phá bằng chứng · LƯỚI (không phải câu hỏi): TẮT squash-merge ở cài đặt repo, PR mang chữ ký chỉ merge kiểu merge-commit; chữa vết đã có bằng repair hai-commit trên nhánh (máy xoá dòng ký + khai lý do, người ký lại trong phiên) — KHÔNG phải diễn-lại-chữ-ký vì cùng cây cùng người, ghi rõ là sửa vết squash · chặn-việc: CÓ (mọi PR về main) · dự báo: đội sẽ dẫm ngay lần squash đầu tiên, phải vào doc onboarding

2026-08-12 · cả hệ · dot1 Cổng 1 (khuôn mời cổng) · KHUÔN NHIỀU-CHỖ-TRỐNG VẪN LÀ NHIỀU QUYẾT ĐỊNH — owner nguyên văn: «Nếu không bỏ kiểu này thì mẫu dạng này làm tôi tê liệt» khi nhận khuôn một-dòng-5-ô tại Cổng 1 đợt 1; tái xuất lớp 11/08 dưới dạng NÉN: 5 ô trên một dòng vẫn là 5 quyết định, nén chỗ không giảm tải nhận thức · phân tích B: trong 5 ô chỉ 1 quyết định thật (duyệt gói hay không) — 4 ô kia máy đã chắc + đảo rẻ + có khuyến nghị kèm căn cứ → thuộc veto-default, đẩy thành ô hỏi là máy trút phân-rã nhận thức lên người; B cũng góp lỗi khi soạn hộ đáp án 5 ô thay vì gộp về 1 · xử: B nhắn A đổi khuôn mời cổng thành MỘT CÂU ĐÓNG «duyệt trọn gói theo các mặc-định đã ghi, hay nêu điều muốn khác» — mặc định (mirror gỡ · quy tắc gốc như đề xuất · lỗ mã ghi sổ vấp, hồ sơ riêng sau đợt) ghi vào sổ quyết định dạng máy-đề-xuất, cửa veto mở đến trước ký Cổng 2 · NEO CHO ĐỢT 2: khuôn cổng chỉ được trình đúng MỘT quyết định; mọi mục máy-chắc là mặc-định-kèm-căn-cứ-và-cửa-veto, không phải ô hỏi · chặn-việc: không
2026-08-12 · kit · dot1 (dựng thẻ Cổng 1, cả hai hồ sơ) · MÃ TIÊU CHÍ CÓ CHỮ THÌ VÔ HÌNH VỚI CẢ HAI BÊN CÙNG LÚC: sau phản biện tôi thêm hai tiêu chí tên `AC-1b`/`AC-9b`; thẻ Cổng 1 render 11/13 và lint KHÔNG kêu một tiếng nào · gốc: `lib/ac-line.cjs` (AC_SUSPECT, AC_XREF, AC_LINE) và `scripts/eval-coverage-lint.js` (AC_LINE_NARROW_FALLBACK) đều dùng `AC-\d+\b` — với `AC-1b` thì sau chữ số là chữ cái nên KHÔNG có ranh giới từ, dòng đó vừa không được đếm là ứng viên vừa không được bóc · vì bên ĐẾM và bên BÓC hỏng giống hệt nhau nên chính lưới W7 (sinh ra để bắt «thẻ đang hiện một phần hợp đồng») cũng câm — đây là lớp hai-bên-cùng-mù, nguy hơn lớp hai-bên-trôi-khỏi-nhau vì không phép so sánh nào phát hiện được · hệ quả chung: BẤT KỲ hợp đồng nào dùng mã tiêu chí có hậu tố chữ đều mất tiêu chí đó khỏi thẻ duyệt, im lặng · xử trong đợt: đổi sang mã số thuần (AC-12/AC-13), đếm lại trên ĐẦU RA THẬT của bộ dựng thẻ (13/13) — KHÔNG tự sửa engine (ngoài phạm vi hai hồ sơ) · owner quyết 12/08: ghi sổ vấp, mở hồ sơ riêng SAU đợt · dự báo: lỗi này chờ sẵn cho mọi người viết `AC-3a` theo bản năng khi chèn tiêu chí giữa chừng; nếp tạm cho đội là mã số thuần tăng dần · chặn-việc: không (đã lách bằng đổi tên)
2026-09-05 · crm-onehub · (CI `acceptance gate`, bước Product map) · DANH SÁCH CHÉP CI KHÔNG THEO KỊP THỨ SCRIPT THẬT SỰ CẦN, LẦN HAI: job đỏ 8 lượt liên tiếp từ 04/09 vì `scripts/product-map.mjs --check` chết exit 2 — khuôn `skills/acceptance/references/opportunity-template.md` không có trong kho tiêu thụ. Gốc: `commands/acceptance-init.md` khối `INIT-CI-COPY-LIST` khai đúng bảy tệp (pre-merge, recheck, năm `lib/`) và KHÔNG có `product-map.mjs`, trong khi dòng 39 cùng tệp bảo chủ repo chạy `product_map --check` trong CI qua `${CLAUDE_PLUGIN_ROOT}` — đường không tồn tại trên runner — nên consumer chép tay (crm-onehub `304a950`, 31/08) và sót đồ kéo theo: `lib/nguong-o-co-hoi.cjs`, `scripts/trang-thai-ho-so.cjs`, `scripts/khong-can-nguoi.mjs`, và khuôn (thêm ở `47299d3c` 24/08, TRƯỚC ngày chép — lỗ có sẵn lúc chép, không phải lệch do nâng bản). Lưới `tests/scripts/consumer-esm.test.mjs` CE2 chỉ soi hai điểm vào pre-merge + recheck và chỉ bắt tên `.cjs`, nên kit xanh trong khi consumer chết. Cùng lớp dòng 08/08 (thiếu `lib/gap-probe.js`) và R4 tổng kết 12/08; khác một điểm: lần này lỗi KHÔNG im — product-map fail-closed đúng thiết kế, thông điệp nêu đường sửa, nên chẩn đoán mất một phiên chứ không mất một tuần. Vá tại consumer: chép khuôn về, PR phanlemanh/crm-onehub#3, Product map xanh trên runner. Hồ sơ cơ hội mở ở kit qua chip (slug đề nghị `danh-sach-chep-ci-thieu-product-map`) · chặn-việc: có (mọi PR của repo đỏ ở bước đầu, dù pre-merge cũng đang chặn vì ba hồ sơ dở khác)
