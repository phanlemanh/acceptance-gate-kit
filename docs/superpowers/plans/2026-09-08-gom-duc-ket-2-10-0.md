# gom-duc-ket-2-10-0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Đóng nợ C1 và đưa K1–K8 của đúc kết 08/09 vào kit trong một vòng T3, mỗi nhóm có commit + eval riêng, mọi phép đo mới có cặp hai-chiều.

**Architecture:** Sáu nhóm task độc lập về file (C1 · K1+K2 · K3 · K4 · K5+K6 · K7+K8), chạy TUẦN TỰ trong phiên chính (tránh xung đột ở `acceptance-verify.js`, `pre-merge-check.sh`, `run-tests.sh`), mỗi nhóm kết bằng chạy eval của nhóm + tự-soi sáu hình dạng (N5) rồi commit. Ca kiểm mới đặt ở file `tests/scripts/*.test.mjs` (suite scripts tự nạp mọi `*.test.mjs`), ca workflow là `W37–W39` trong `tests/workflows/acceptance-verify.test.mjs`.

**Tech Stack:** Node ≥ 20 (không thư viện ngoài), bash/POSIX cho pre-merge, harness vm-realm `tests/workflows/harness.mjs`.

**Spec:** `docs/superpowers/specs/2026-09-08-gom-duc-ket-2-10-0-design.md` · hợp đồng `_acceptance/gom-duc-ket-2-10-0/contract.md` · evals `_acceptance/gom-duc-ket-2-10-0/evals.yaml`.

## Global Constraints

- Mọi phép đo mới: cặp hai-chiều cùng fixture + thông điệp ghim (MEASURE-BIRTH-CLAUSE); fixture code-sinh; đường dẫn suy từ `import.meta.url`; mốc cố định `334d5d52` thay `main`.
- Bản chép byte-đúng: khối `IDENTITY-ECHO-RULE` ở ba file phải giống nhau từng ký tự (LM17).
- Không sửa file nào của hồ sơ đã ký ngoài một dòng `revisit` vào `_acceptance/lop-bang-chung-nhin-thay/decisions.jsonl` (K3).
- Không chạm `hooks/`, `scripts/recheck-evidence.cjs`, `feature-loop/scripts/carry-plan.mjs`.
- Lệnh commit: `git branch --show-current` trước; KHÔNG ghép ` -n` (kể cả `grep -n`, `sed -n`) vào cùng lệnh bash với `git commit`.
- Mỗi nhóm xong: chạy eval của nhóm (khoá `gdk_*` trong `_acceptance/config.yaml`), rồi tự-soi sáu hình dạng CLAUDE.md, rồi commit.

---

## Nhóm 1 — C1 (nợ Known limits)

### Task 1.1: `stripComment` một chỗ + ba bộ đọc dùng chung (AC-2c)

**Files:**
- Modify: `lib/eval-yaml.cjs` (thêm export `stripComment`)
- Modify: `lib/lop-nhin-thay.cjs:29-35` (`tokensOf`), `:52` (`fieldVal`), `:71` (`frontLine`)
- Modify: `scripts/eval-coverage-lint.js:128`, `:217`
- Modify: `scripts/gate-card.js:235` (`clean`)
- Test: `tests/scripts/lnt-no.test.mjs` (ca NO3)

**Interfaces:**
- Produces: `stripComment(s: string): string` — cắt từ `#` khi `#` ở đầu chuỗi hoặc có khoảng trắng đứng trước; `x#y` giữ nguyên. `lib/eval-yaml.cjs` exports `{ parseEvals, BLOCK_RE, stripComment, pathsOf }` (`pathsOf` ở Task 4.1).

- [ ] **Step 1: Viết ca NO3 (đỏ trước)** — `tests/scripts/lnt-no.test.mjs`, khung:

```js
import { spawnSync } from 'node:child_process'; import { mkdtempSync, writeFileSync, mkdirSync, cpSync, rmSync } from 'node:fs';
import path from 'node:path'; import os from 'node:os'; import { fileURLToPath } from 'node:url'; import { createRequire } from 'node:module';
const HERE = path.dirname(fileURLToPath(import.meta.url)); const ROOT = path.resolve(HERE, '..', '..');
const require = createRequire(import.meta.url); let failures = 0;
const pass = (id, m) => console.log(`  PASS: ${id} — ${m}`); const fail = (id, m) => { console.log(`  FAIL: ${id} — ${m}`); failures++; };
const tmp = () => mkdtempSync(path.join(os.tmpdir(), 'lnt-no-'));
const only = (process.env.NO_CASES || '').split(',').filter(Boolean); const want = id => !only.length || only.includes(id);
// NO3: 4 fixture × 3 bộ đọc — kỳ vọng applicable: true,true,false,true
const FIX = [['[ui] # c', true], ['[ui]\t#c', true], ['[ui]#x', false], ['[ui]', true]];
```
  Mỗi fixture dựng workspace `_acceptance/x/{contract.md,evals.yaml}` (contract từ khuôn `CONTRACT-FRONTMATTER-TEMPLATE` — chép cách `contractOf` của `tests/plugins/lop-nhin-thay.test.mjs`), rồi đọc bằng (a) `node lib/lop-nhin-thay.cjs classify <dir>` cột 1; (b) `node scripts/eval-coverage-lint.js <ws>` có/không dòng «W8 surfaces include»; (c) `node scripts/gate-card.js --root <ws> --slug x --extract` → `ui_observed.applicable`. 12 assert, mỗi assert nêu tên bộ đọc + fixture.

- [ ] **Step 2: Chạy `node tests/scripts/lnt-no.test.mjs`** → kỳ vọng FAIL ở `[ui]#x` (bộ đọc hiện tại `[ \t]+#` giữ `ui]#x`? kiểm thực tế; ghi kết quả đỏ vào commit message).
- [ ] **Step 3: Thêm vào `lib/eval-yaml.cjs`:**

```js
// Cắt chú thích YAML đúng luật: `#` là chú thích khi đứng đầu hoặc có khoảng trắng đứng trước.
// MỘT chỗ cho mọi bộ đọc surfaces/evals (Ngoài-6 của lop-bang-chung-nhin-thay: [ \t]+# vs \s*#).
const stripComment = s => String(s == null ? '' : s).replace(/(^|\s)#.*$/, '').trim();
module.exports = { parseEvals, BLOCK_RE, stripComment };
```
  Thay regex cục bộ ở `lop-nhin-thay.cjs` (3 chỗ), `eval-coverage-lint.js` (2 chỗ), `gate-card.js` `clean` bằng `require('../lib/eval-yaml.cjs').stripComment` (gate-card giữ phần bỏ quote sau khi strip).
- [ ] **Step 4: Chạy lại NO3** → PASS 12/12.
- [ ] **Step 5: Commit** `feat(c1): stripComment một nguồn cho ba bộ đọc surfaces`.

### Task 1.2: NOTE pre-merge ba nguyên nhân + gỡ `LNT_AVAILABLE` + `html()` bỏ ANSI (AC-2a, b, d)

**Files:**
- Modify: `scripts/pre-merge-check.sh` khối W8 (tìm «lớp nhìn-thấy không kiểm được»)
- Modify: `lib/nguong-o-co-hoi.cjs:84-92` (gỡ `LNT_AVAILABLE` khỏi export + comment)
- Modify: `tests/plugins/lop-nhin-thay.test.mjs:40` (`html`)
- Test: `tests/scripts/lnt-no.test.mjs` (NO1, NO2, NO4)

- [ ] **Step 1: Viết NO1/NO2/NO4 (đỏ trước).** NO1: `grep -c LNT_AVAILABLE lib/nguong-o-co-hoi.cjs` == 0 VÀ `git show 334d5d52:lib/nguong-o-co-hoi.cjs` chứa chuỗi (đối chứng dương). NO2: fixture git gated (chép `mk_git_repo` kiểu VC01 của `tests/scripts/run-tests.sh` sang JS: repo + config + hợp đồng `surfaces: [ui]` signed-off + evidence PASS) rồi chạy `pre-merge-check.sh --base HEAD~1` ba lần trên ba bản sao `scripts/`+`lib/`: (i) `PATH=/usr/bin:/bin` không có node → NOTE «thiếu node»; (ii) xoá `lib/lop-nhin-thay.cjs` → «thiếu lib/lop-nhin-thay.cjs»; (iii) ghi đè lib bằng `process.exit(7)` → «lib lỗi (exit 7)»; ba chuỗi khác nhau, exit 0 cả ba. NO4: bản sao `scripts/gate-card.js` chèn `process.stdout.write('\x1b[31mX\x1b[0m')` đầu main → import `html`-helper logic (tách helper ra hàm `stripAnsi` trong test) kiểm không còn ESC trong khi raw có.
- [ ] **Step 2: Chạy → FAIL** (NOTE hiện gộp; `LNT_AVAILABLE` còn; html chưa strip).
- [ ] **Step 3: Sửa pre-merge:**

```bash
    if ! command -v node >/dev/null 2>&1; then
      echo "NOTE [$slug]: lớp nhìn-thấy không kiểm được — thiếu node; NOTE này không chặn."
    elif [ ! -f "$LNT_LIB" ]; then
      echo "NOTE [$slug]: lớp nhìn-thấy không kiểm được — thiếu lib/lop-nhin-thay.cjs (mang cổng vào repo phải copy CẢ lib/); NOTE này không chặn."
    else
      lnt_line="$(node "$LNT_LIB" classify "$dir" 2>/dev/null)"; lnt_rc=$?
      if [ "$lnt_rc" -ne 0 ]; then echo "NOTE [$slug]: lớp nhìn-thấy không kiểm được — lib lỗi (exit $lnt_rc); NOTE này không chặn."; lnt_line=""; fi
    fi
```
  (giữ nhánh đọc `lnt_line` bên dưới; `exit 3` no-contract giữ nguyên là «lib lỗi (exit 3)» có tên). Gỡ `LNT_AVAILABLE` ở `nguong-o-co-hoi.cjs`. `html = root => gc(root).stdout.replace(/\x1b\[[0-9;]*m/g, '')`.
- [ ] **Step 4: Chạy NO1–NO4 → PASS; chạy `bash tests/scripts/run-tests.sh` cả suite** (PM-LNT-g ghim «không kiểm được» vẫn xanh).
- [ ] **Step 5: Commit** `fix(c1): NOTE lớp nhìn-thấy nêu đúng nguyên nhân; gỡ LNT_AVAILABLE; html() bỏ ANSI`.

### Task 1.3: LNT1 dấu hiệu bản sao + neo mốc cố định; LNT6 (iv)(vi) đo quan hệ (AC-1)

**Files:**
- Modify: `tests/plugins/lop-nhin-thay.test.mjs:52-108` (LNT1), `:216-234` (LNT6)

- [ ] **Step 1: LNT1** — sau `gcM`/`gcG`: `eq(id, uoM.token_la, ['web'], 'dấu hiệu bản sao đã chạy: mutant token_la [web]'); eq(id, uoG.token_la, [], 'lành token_la []')`; thay `'main:lib/nguong-o-co-hoi.cjs'` bằng hằng `const BASE_LNT = '334d5d52'` (+ comment «mốc bất biến, cha của merge PR 158») và bỏ điều kiện `if (old.status === 0)` thành `fail` khi không show được (chiều đỏ có sẵn phải sống). Thông điệp PASS cuối: thêm «dấu hiệu bản sao đã chạy».
- [ ] **Step 2: LNT6 (iv)** — thay mệnh đề chuỗi bằng: rút cặp từ CONTEXT `**Surface**` block bằng regex ``/Alias máy đọc:\s*((?:`[^`]+`,?\s*)+)→\s*`([^`]+)`/`` → `{web:'ui','web-ui':'ui'}`; `deepEqual` với `L.SURFACE_ALIAS`; mutant: bản sao CONTEXT thay ``→ `ui` `` bằng ``→ `mobile` `` → đỏ nêu «alias». (vi): `cut(t, /^3b\./m, /^3c\./m)` phải chứa cả `@playwright/cli`, `capture.ui`, `playwright-cli`; mutant: dời câu chứa `@playwright/cli` xuống sau `3c.` → đỏ nêu «3b». Thông điệp PASS LNT6 thêm «quan hệ alias».
- [ ] **Step 3: Chạy `LNT_CASES=LNT1,LNT6 node tests/plugins/lop-nhin-thay.test.mjs`** → PASS; phá thử: sửa tạm `SURFACE_ALIAS` trong bản sao → LNT6 đỏ «alias» (không commit bản phá).
- [ ] **Step 4: Chạy eval nhóm:** `gdk_lnt_do`, `gdk_c1_nho`. Tự-soi sáu hình dạng. **Commit** `test(c1): LNT1 ghim dấu hiệu bản sao + mốc 334d5d52; LNT6 đo quan hệ alias/3b`.

## Nhóm 2 — K1 + K2

### Task 2.1: Null-guard `capture:provenance` (AC-3)

**Files:**
- Modify: `feature-loop/workflows/acceptance-verify.js:1022-1044`
- Test: `tests/workflows/acceptance-verify.test.mjs` (W37)

- [ ] **Step 1: W37 (đỏ trước):**

```js
console.log('W37 provenance agent chết → BLOCKED có tên, không TypeError');
{
  let threw = null; let r = null;
  try { r = await runWorkflow(WF, baseArgs(), responder({ 'capture:provenance': () => null })); } catch (e) { threw = e; }
  check('W37 không ném', threw === null, String(threw && threw.message));
  const b = r && r.result.blocked.find(x => x.cmd === 'capture:provenance');
  check('W37 verdict BLOCKED', r && r.result.verdict === 'BLOCKED', r && r.result.verdict);
  check('W37 reason ghim', !!b && b.reason.startsWith('capture:provenance agent bi skip/chet — khong co ket qua'), b && b.reason);
  const synth = r && r.calls.find(c => c.label === 'synthesize:report');
  check('W37 synthesize không nhận prov rỗng', !synth || !/enforcement_mode: (undefined|null)/.test(synth.prompt), '');
  const ok = await runWorkflow(WF, baseArgs(), responder());
  check('W37 đối chứng dương', !ok.result.blocked.some(x => x.cmd === 'capture:provenance'), JSON.stringify(ok.result.blocked));
}
```
  (kiểm `responder` nhận override dạng hàm/giá trị — đọc `tests/workflows/acceptance-verify.test.mjs:35-51` trước khi viết.)
- [ ] **Step 2: Chạy → FAIL** (TypeError bắt ở harness).
- [ ] **Step 3: Sửa workflow** ngay sau `const prov = await agentT(...)`:

```js
if (!prov || typeof prov !== 'object') {
  blocked.push({ cmd: 'capture:provenance', reason: 'capture:provenance agent bi skip/chet — khong co ket qua, khong duoc tinh la pass' })
  verdict = 'BLOCKED'
}
const provSafe = prov && typeof prov === 'object' ? prov : { enforcement_mode: 'n-a', bypass_used: 'n-a' }
```
  và dùng `provSafe` ở dòng 1044; nếu `blocked` đã có mục provenance thì bỏ qua gọi synthesize (trả `report: ''`, `findings: ''`) — main loop xử BLOCKED như mọi BLOCKED khác. Kiểm `verdict` có phải `const` không; nếu có thì đổi thành `let` ở nơi khai.
- [ ] **Step 4: Chạy `node tests/workflows/acceptance-verify.test.mjs`** → W37 PASS, W01–W36 vẫn PASS.
- [ ] **Step 5: Commit** `fix(k1): capture:provenance chết → BLOCKED có tên thay TypeError`.

### Task 2.2: Danh tính hết trạm thu phí (AC-4)

**Files:**
- Modify: `skills/acceptance/references/human-facing-language.md:231-233` (khối nguồn)
- Modify: `commands/approve.md:33-49`, `commands/signoff.md:40-70` (bản chép khối + thân: bỏ CẢNH BÁO, thêm nấc cổng trước ở signoff, câu «sửa bằng một câu», sửa dòng 69-70 «Enter xác nhận chỉ xác nhận danh tính»)
- Modify: `tests/plugins/run-tests.sh:9538-9553` (anchors P191: đổi `["enter-xac-nhan","Enter xác nhận"]` → `["ghi-thang","GHI THẲNG"]` + mảng `anchorsAm = [["enter-xac-nhan","Enter xác nhận"]]` kiểm KHÔNG có), `:9880-9886` (GATE_NEEDLES bỏ dòng Enter; BODY_AM thêm `("enter-xac-nhan-cu", "Enter xác nhận")`), CROSSCHECK bỏ `"không có trong \`signoff.approvers\`"` thay bằng `"cổng trước"` cho signoff (đọc kỹ cấu trúc CROSSCHECK trước khi sửa)
- Test: `tests/scripts/danh-tinh.test.mjs` (DT1–DT5)

- [ ] **Step 1: Viết DT1–DT5 (đỏ trước)** theo `expected` của E4; DT4 tiêm vào BẢN SAO ba file (tmp) rồi chạy chính hàm kiểm của test trên bản sao → phải đỏ nêu «nhánh chờ» / «cảnh báo approvers». DT5 rút mảng `anchors` từ heredoc P191 bằng regex `const anchors = \[([\s\S]*?)\];` và `GATE_NEEDLES = \[([\s\S]*?)\]` + `BODY_AM`.
- [ ] **Step 2: Chạy → FAIL.**
- [ ] **Step 3: Viết lại khối nguồn (nội dung ĐÚNG này, chép byte-đúng sang hai bản):**

```
Suy xong ở BẤT KỲ nấc nào còn tên → GHI THẲNG rồi hiển thị lại một dòng «với danh tính: <tên> <ngày> (từ <nguồn suy>)» — không chờ xác nhận: hai nguồn khớp hay lệch đều không nói gì về ai đang gõ (xuất xứ thật là tác giả commit), nên lượt chờ không thêm bảo đảm nào, chỉ thêm một chạm. Người sửa tên hoặc ngày bằng MỘT CÂU bất kỳ lúc nào sau đó (máy ghi lại cùng lượt). Chỉ ca CẠN mới hỏi.
```
  Thân `signoff.md`: bậc thang mới `--as` → **cổng trước cùng hồ sơ** (`approved_by` của contract, CHỈ khi tác giả commit ghi `approved_by` — `git log -1 --format=%an -S'approved_by: <tên>' -- <contract>` — bằng `git config user.name` hiện tại) → `git config user.name` → `signoff.approvers` một tên → CẠN. Thân `approve.md`: bỏ đoạn CẢNH BÁO; giữ ĐỌC/CHỌN/CẠN. Dòng 69-70 signoff: «Chữ «Ký» vẫn phải do NGƯỜI gõ — dòng danh tính chỉ ghi ai ký, không phải chữ ký.»
- [ ] **Step 4: Chạy DT1–DT5 + `node tests/scripts/gate-card-lmcms.test.mjs` (LM17) + `ONLY_BLOCK=P19 bash tests/plugins/run-tests.sh`** (P191/P194 là inline — chạy cả suite plugins một lần ở cuối nhóm).
- [ ] **Step 5: Commit** `feat(k2): danh tính ghi thẳng ở mọi ca, bỏ CẢNH BÁO approvers, nấc cổng trước; neo P191/P194 thành cặp dương/âm`.

## Nhóm 3 — K3

### Task 3.1: W6 phạm vi Criteria + định danh ASCII gạch nối + `_Allow_`; W8 bỏ token-lạ (AC-5)

**Files:**
- Modify: `lib/context-glossary.js:167-182` (`wordRe`/`spansOf`), `:189-224` (`findViolations` nhận `{sectionOnly}`), `scripts/eval-coverage-lint.js:231-256` (W8 bỏ (c); W6 cắt `## Criteria` bằng `lib/md-section.cjs` `section(text, 'Criteria')`)
- Modify: `CONTEXT.md` (thêm `_Allow_: thẻ` ở Contract/Dấu, `_Allow_: hook` ở Ổ cắm, `_Allow_: engine` ở Executor, `_Allow_: test, check` ở Criterion/Eval, `_Allow_: tag` ở Dấu — mỗi dòng kèm ghi chú «từ đa nghĩa, K3 08/09»)
- Modify: `tests/scripts/run-tests.sh:1209-1229` (L42m viết lại, L47 gỡ)
- Modify: `_acceptance/lop-bang-chung-nhin-thay/decisions.jsonl` (append một dòng `revisit`: «nhánh token-lạ W8 (AC-2h) gỡ ở gom-duc-ket-2-10-0 — 140/15/4 dòng giả ở tiêu thụ»)
- Create: `_acceptance/gom-duc-ket-2-10-0/rang.sh` (chân `cay-that`)
- Test: `tests/scripts/w6-w8-pham-vi.test.mjs` (PV1–PV5)

- [ ] **Step 1: PV1–PV5 đỏ trước.** PV2 fixture Criteria: «- AC-1: Given `ui-check` chạy, When check-lại thẻ-cổng-2, Then pre-merge …» với CONTEXT tạm `_Avoid_: check` (Eval), `_Avoid_: thẻ` (Contract), `_Avoid_: pre-merge` (Cổng) → kỳ vọng: không kêu `check` ở vị trí `ui-check`, kêu `check` (check-lại), kêu `thẻ`, kêu `pre-merge`.
- [ ] **Step 2: Sửa `context-glossary.js`:** trước khi `spansOf`, mask định danh ASCII: `text.replace(/(?<![\p{L}\p{N}_-])[a-z0-9]+(?:-[a-z0-9]+)+(?![\p{L}\p{N}_-])/giu, m => aliasSet.has(m.toLowerCase()) ? m : '_'.repeat(m.length))` — `aliasSet` = mọi alias + allow phrase (định danh trùng alias không bị mask). `findViolations(docText, glossary, opts)`: `opts.section` → cắt bằng `section()` của md-section, giữ `lineNo` gốc (cộng offset dòng).
- [ ] **Step 3: `eval-coverage-lint.js`:** gọi `findViolations(contractText, glossary, { section: 'Criteria' })`; xoá hai dòng nhánh (c). `rang.sh`:

```bash
#!/usr/bin/env bash
# rang.sh — răng hồ sơ gom-duc-ket-2-10-0. --chan cay-that | loop-health-that
set -u; HERE="$(cd "$(dirname "$0")" && pwd)"; ROOT="$(cd "$HERE/../.." && pwd)"; DEV="${DEV_ROOT:-$(dirname "$ROOT")}"
case "${2:-}" in
  cay-that)
    printf 'repo | W8-token | W8-nghia-vu | W6\n'; bad=0
    for r in "$ROOT" "$DEV/artifact-platform" "$DEV/oneflow" "$DEV/crm"; do
      n="$(basename "$r")"; [ -d "$r/_acceptance" ] || { echo "SKIP $n: vắng"; continue; }
      o="$(node "$ROOT/scripts/eval-coverage-lint.js" "$r" 2>&1)"
      tk="$(printf '%s\n' "$o" | grep -c 'W8 surfaces carry token')"; nv="$(printf '%s\n' "$o" | grep -c 'W8 surfaces include a human-visible')"; w6="$(printf '%s\n' "$o" | grep -c '\] W6 ')"
      printf '%s | %s | %s | %s\n' "$n" "$tk" "$nv" "$w6"; [ "$tk" -eq 0 ] || { echo "FAIL: $n còn $tk dòng W8-token"; bad=1; }
    done; [ "$bad" -eq 0 ] && echo "PASS: CAY-THAT" || exit 1 ;;
  loop-health-that) node "$ROOT/scripts/loop-health.mjs" --root "$ROOT" --at 8caa9998 --check-hand "$HERE/mocs-tay.json" ;;
  *) echo "usage: rang.sh --chan cay-that|loop-health-that" >&2; exit 2 ;;
esac
```
  (đọc `--dev-root` qua env `DEV_ROOT` hoặc cờ; giữ đúng một trong hai, ghi vào usage.)
- [ ] **Step 4: Chạy PV1–PV5, `bash _acceptance/gom-duc-ket-2-10-0/rang.sh --chan cay-that`** (ghi số trước/sau vào commit message), `bash tests/scripts/run-tests.sh` (L42m/L47 + L19–L29 W6 vẫn xanh), lint hợp đồng này → 0 dòng W6.
- [ ] **Step 5: Commit** `feat(k3): W6 chỉ Criteria + định danh ASCII gạch nối + _Allow_ từ đa nghĩa; W8 bỏ nhánh token-lạ (kit W6 127→N, AP W8-token 140→0)`.

## Nhóm 4 — K4

### Task 4.1: `pathsOf` + `staleScope` + pre-merge theo paths + câu SKILL (AC-6)

**Files:**
- Modify: `lib/eval-yaml.cjs` (thêm `pathsOf(text): Map<id, string[]>` — inline `[a, b]` và block `- a`; chép logic list-field của `feature-loop/scripts/s4-args.mjs:107-124`)
- Modify: `lib/evidence-core.cjs` (thêm `staleScope(evalsText): string[]|null` — null khi bất kỳ eval executor ∈ {test, script, ui-check} thiếu `paths`; thêm `globToRe` cùng semantics carry-plan; CLI `node lib/evidence-core.cjs stale-scope <evals.yaml>` in một glob mỗi dòng, exit 3 khi null; export cả hai)
- Modify: `scripts/pre-merge-check.sh:506-514` (`stale_files` nhận thêm `<dir>`: nếu `node` + lib có và `stale-scope` exit 0 → chỉ giữ file khớp một glob (dùng `match_globs` với glob đã đổi `**` như PR 157); exit 3 → luật cũ; node/lib vắng → luật cũ + NOTE «luật cũ (thiếu node hoặc lib) — stale tính cả cây»), `:1140` truyền `$dir`
- Modify: `feature-loop/skills/feature-loop/SKILL.md` Staleness guard (câu: «STALE tính theo `paths:` của eval — file đổi khớp ∪paths; hồ sơ có eval máy/ui thiếu `paths` → tính cả cây như cũ; tác giả liệt vào `paths` cả file mà vật được đo `require`/`import` — paths không bắc cầu»)
- Modify: `commands/acceptance-init.md` INIT-CI-COPY-LIST (chỉ cập nhật chú thích của `evidence-core.cjs`/`eval-yaml.cjs` nếu CE2 đòi)
- Test: `tests/scripts/stale-paths.test.mjs` (SP1–SP6)

- [ ] **Step 1: SP1–SP6 đỏ trước.** Fixture git (JS, mẫu `mk_git_repo`/`wr_report` VC01): `src/app.js`, `tests/khac.sh`, config t1 `docs/**`, hợp đồng `feat-sp` signed-off + evals `E1 paths [src/**]` (+ biến thể E2 không paths cho SP3), evidence PASS pin `verified_commit`; commit đổi file; chạy `bash scripts/pre-merge-check.sh --base <sha-trước>` trong fixture với `scripts/`+`lib/` chép từ ROOT (bản sao trọn thư mục, không chép tay).
- [ ] **Step 2: Sửa lib + pre-merge như trên.** `staleScope` bỏ qua eval `judgment`. Glob: `**` → `.*`, `*` → `[^/]*`, anchored.
- [ ] **Step 3: Chạy SP1–SP6; chạy `bash tests/scripts/run-tests.sh` (VC01–VC11 + PM-*), `node tests/scripts/consumer-esm.test.mjs`.** Phá thử: bản sao lib đổi `staleScope` trả `[]` luôn → SP2 phải đỏ.
- [ ] **Step 4: Commit** `feat(k4): stale theo paths của eval (đủ paths → chỉ file khớp; thiếu → cả cây); pathsOf + staleScope một nguồn`.

## Nhóm 5 — K5 + K6

### Task 5.1: S5 mặc định PR (AC-7)

**Files:**
- Modify: `feature-loop/skills/feature-loop/SKILL.md` S5 (khối `<!-- <<<S5-SHIP-DEFAULT -->` … `<!-- S5-SHIP-DEFAULT>>> -->`), `commands/acceptance-init.md` khuôn config (`#   ship_default: pr   # pr (mặc định) | merge | branch | ask — S5 mở PR không hỏi, chỉ hỏi khi ask`)
- Test: `tests/scripts/s5-ship-default.test.mjs` (S5D1–S5D3)

- [ ] **Step 1: S5D1–S5D3 đỏ.** S5D2 rút bốn giá trị từ SKILL (regex `giá trị hợp lệ: \`(\w+)\`…`) và từ dòng `ship_default:` của khuôn init → deepEqual.
- [ ] **Step 2: Viết khối:** «Mặc định MỞ PR — KHÔNG bày menu ba lối của `finishing-a-development-branch` (nếp kit là PR; menu là lượt ngoài thiết kế đã đo). Chỉ hỏi khi `feature_loop.ship_default: ask`. Giá trị hợp lệ: `pr` (mặc định) · `merge` · `branch` · `ask`.»
- [ ] **Step 3: Chạy → PASS; mutant bản sao gỡ khối → đỏ. Commit** `feat(k5): S5 mặc định mở PR, khoá feature_loop.ship_default`.

### Task 5.2: `scripts/loop-health.mjs` (AC-8)

**Files:**
- Create: `scripts/loop-health.mjs`
- Create: `_acceptance/gom-duc-ket-2-10-0/mocs-tay.json` (`{"T2":{"round_usage":3.06,"token_s4":43.8e6},"T3":{"round_usage":3.25,"token_s4":55.6e6,"iterations":5.5},"tol":{"round":0.05,"token":0.5e6,"iterations":0.1}}`)
- Test: `tests/scripts/loop-health.test.mjs` (LH1–LH4)

**Interfaces:**
- CLI: `node scripts/loop-health.mjs --root <repo> [--at <sha>] [--since <ref>] [--all] [--dev-root <dir>] [--json] [--exclude-slug s]... [--check-hand <json>]`
- Produces: `{ window: {slugs:[{slug,tier,implemented_at,decided_at,minutes,human_calls|'đếm tay',infra_burned}]}, tiers: {T2:{n,round_usage,round_iter,token_s4,repin,fix}, T3:{…}} }`

- [ ] **Step 1: LH1–LH4 đỏ.** Fixture git code-sinh 3 hồ sơ: usage-report với `### S4 round N — … ` + bảng có cột `out|in|cache_read` (token = tổng in+out+cache của các dòng, cộng cả round); evidence-report `## Iterations` với `Round 1: … BLOCKED`/`Round 2: … PASS`; run-log 1 dòng repin; decisions 2 `fix` stage `S4-r1`; commit «implemented» rồi «signed-off» với `GIT_COMMITTER_DATE` cách 90 phút; một hồ sơ có `human_calls: 3` trong contract frontmatter.
- [ ] **Step 2: Viết script** (đọc file qua `readAt(sha, rel)` = `git show sha:rel` khi `--at`, else fs; liệt slug bằng `git ls-tree` khi `--at`; tier từ contract `risk_tier`; token S4 = tổng ba cột số của mọi dòng bảng dưới heading `### S4 round`; round_usage = số heading `### S4 round` khử trùng theo số; round_iter = max `Round N` trong `## Iterations`; repin = dòng `"kind":"repin"`; fix = decisions `type:"fix"` có `stage` bắt đầu `S4-r`; minutes = `git log --format=%cI -S'status: implemented' -- contract` (đầu) → `-S'status: signed-off'` hoặc `machine-cleared` (đầu); `--all`: quét `<dev-root>/*/_acceptance`, khử trùng theo `git remote get-url origin`, giữ bản nhiều hồ sơ). `--check-hand`: so từng số với tol, in bảng, lệch → in slug đóng góp lớn nhất (theo |token − TB|) và exit 1; khớp → «PASS: LH-THAT».
- [ ] **Step 3: Chạy LH1–LH4 → PASS; chạy `bash _acceptance/gom-duc-ket-2-10-0/rang.sh --chan loop-health-that`** — số chênh → điều tra theo AC-8(b) (đọc `sweep.mjs`/`tokproj.mjs` trong memory để so cách đếm), ghi kết luận vào commit + Notes nếu số tay sai (kèm entry `fix`).
- [ ] **Step 4: Commit** `feat(k6): loop-health — ba dòng số luật (c) + mốc cùng hạng theo tier, --at sha bất biến, --all xuyên repo`.

## Nhóm 6 — K7 + K8

### Task 6.1: Triage theo tác hại (AC-9)

**Files:**
- Modify: `feature-loop/workflows/acceptance-verify.js` — `TRIAGE_SCHEMA` (thêm `harm: {type:'string', enum:['behavior','measure','']}`), prompt triage (định nghĩa harm: `behavior` = sản phẩm/luật/chữ người dùng đọc sai; `measure` = chỉ độ chặt phép đo), khối `triaged` (sau `matchTriage`: `const inPaths = evalsMatching(f.file)`; nếu `t.harm === 'behavior' && inPaths.length` → `inContract: true, acRef: [...new Set(inPaths.map(e => e.criterion))].sort().join(', ')`), synthesize OOC-ITEM thêm dòng `  Tác hại: {harm}` sau `severity`
- Modify: `lib/out-of-contract.js` `parseFindings` (đọc `Tác hại:` → `harm`, mặc định `''`; export không đổi)
- Modify: `scripts/gate-card.js:920-935` (trước `forEach`: `const order = h => h === 'behavior' ? 0 : h === 'measure' ? 1 : 2; const sorted = [...ooc.findings].sort((a, b) => order(a.harm) - order(b.harm))` — sort ổn định giữ thứ tự viết trong cùng nhóm)
- Test: `tests/workflows/acceptance-verify.test.mjs` (W38), `tests/scripts/ooc-tac-hai.test.mjs` (TH1–TH3)

- [ ] **Step 1: W38 + TH1–TH3 đỏ** theo E9/E9b (args.evals có `paths`; responder `review:` trả 4 finding, `refute:` refuted false, `triage` trả harm theo ca).
- [ ] **Step 2: Sửa ba file.** `evalsMatching(file)` dùng `globToRe` chép từ carry-plan (cùng semantics; ghi chú «bản chép có chủ đích — hợp nhất là ô paths-mot-nguon»).
- [ ] **Step 3: Chạy → PASS; `node tests/scripts/out-of-contract.test.mjs` cũ vẫn xanh. Commit** `feat(k7): triage theo tác hại — behavior trong paths vào vòng vá, thẻ xếp behavior trước`.

### Task 6.2: Làn conventions theo deltaFiles (AC-10)

**Files:**
- Modify: `feature-loop/workflows/acceptance-verify.js:488-495` (conventions prompt: `${Array.isArray(args.deltaFiles) ? `CHI cham file CHU (.md) trong danh sach nay — file chu khong doi so round truoc KHONG cham lai: ${args.deltaFiles.filter(f => f.endsWith('.md')).join(', ') || '(khong file chu nao doi)'}. ` : ''}Review diff …`), khối comment args thêm `deltaFiles`
- Modify: `feature-loop/scripts/s4-args.mjs:304` (giữ `deltaFiles` vào object args: `out.deltaFiles = deltaFiles` khi có anchor)
- Test: W39 + `tests/scripts/s4-args-delta.test.mjs` (SD1: fixture git, chạy `s4-args.mjs --slug x --root <fx> --out <f> --carry-anchor <sha>` → JSON có `deltaFiles`; không anchor (round 1) → không khoá)

- [ ] **Step 1: W39 + SD1 đỏ. Step 2: sửa. Step 3: chạy → PASS. Commit** `feat(k8): làn conventions chỉ chấm file chữ đổi so round trước (deltaFiles từ s4-args)`.

## Kết S3

- [ ] Chạy đủ bốn suite + `product-map --check` + `node scripts/eval-coverage-lint.js .` (kit W6 phải ≤ 10).
- [ ] Tự-soi N5 lần cuối trên MỌI test mới: fixture chép khuôn bên đọc? · so chuỗi thay quan hệ? · âm-tính-một-mình? · ref di động? · ROOT hardcode? · đếm ma trận lệch? — ghi kết quả một dòng mỗi file vào commit `implemented`.
- [ ] Set `status: implemented`, commit, `s4-args.mjs` → dispatch S4 round 1 (kiểm `gh pr list` trống trước).
